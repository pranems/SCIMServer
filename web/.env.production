# Production environment configuration for SCIMServer Web UI
# Use relative URLs when deployed in same container (recommended)
# Remove VITE_API_BASE to use auto-detection from client.ts

# VITE_API_BASE=  # Commented out - uses relative URLs
# ⚠️ Do not bake secrets into the production bundle. The web app now prompts for the
# bearer token at runtime and stores it in localStorage only. Leave this unset.
# VITE_SCIM_TOKEN=