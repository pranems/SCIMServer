## SCIMServer – Condensed Session Memory

This file intentionally trimmed for clarity. Full historic log kept in git history.

### Active Execution Reference

**`docs/DELIVERY_PLAN.md` is the canonical execution plan** for the current `ci/validate-before-push` branch and the 6-week production-hardening + UI-redesign sequence. All upcoming commits should:
- Reference a named defect ID from [DELIVERY_PLAN.md §3](docs/DELIVERY_PLAN.md#3-named-defect-inventory)
- Follow the TDD process from [DELIVERY_PLAN.md §7](docs/DELIVERY_PLAN.md#7-tdd-process-rules)
- Update the [Progress Log in §11](docs/DELIVERY_PLAN.md#11-progress-log) on commit

### Workspace Tenant Isolation (CRITICAL - read before any `az` command)

This dev box hosts TWO projects targeting TWO different Azure tenants. Cross-tenant accidents (deploying to the wrong subscription, leaking secrets, running tests against the wrong identity) are the #1 risk and have a one-line preventative fix.

| Project | Tooling | Tenant | Tenant ID | Subscription |
| --- | --- | --- | --- | --- |
| **SCIMServer** (this VS Code window) | VS Code + integrated `pwsh` + `az` CLI | Provisioning IAM Team 07 (`proviamtest07.onmicrosoft.com`) | `79e329e6-1ece-4499-8381-53a6e43eea5c` | scimserver-rg (prod) / scimserver-rg-dev (dev) |
| **Sync Fabric** (separate window) | Visual Studio + its own MSAL cache | TME Organization (AzureCloud) | `70a036f6-8e4d-4615-bad6-149c02e7720d` | AADSF TME Test Subscription |

Isolation is enforced by per-workspace `AZURE_CONFIG_DIR` so each project gets its own `az` CLI token cache, default subscription, and login state. The setting is committed at [.vscode/settings.json](.vscode/settings.json) and applies automatically to every integrated terminal opened from this workspace - no manual `az account set` per shell needed.

**One-time bootstrap in any new integrated terminal opened from this workspace:**

```powershell
# Verify the workspace setting took effect (should print a path ending in .azure-scimserver)
$env:AZURE_CONFIG_DIR
$env:AZURE_TENANT_ID  # should be 79e329e6-1ece-4499-8381-53a6e43eea5c

# First-time login into the SCIMServer tenant only (won't touch the sync fabric tenant)
az login --tenant 79e329e6-1ece-4499-8381-53a6e43eea5c
az account set --subscription "<scimserver-subscription-id>"

# Sanity check before any deploy / live-test run
az account show --query '{name:name, tenantId:tenantId, subId:id}' -o table
```

**Why this matters:** without `AZURE_CONFIG_DIR`, `az login` writes to the global `~/.azure` directory shared with Visual Studio's `az` invocations, sync fabric's `DefaultAzureCredential`, and any other shell. The last `az account set` wins globally - which is exactly how prod gets deployed to the wrong tenant. The Visual Studio sync fabric project keeps its own MSAL cache and is unaffected by this isolation.

**Pitfalls to avoid:**
- NEVER run `az login` without first confirming `$env:AZURE_CONFIG_DIR` is set to the SCIMServer-scoped path.
- NEVER pass `--tenant 70a036f6-...` (TME) from this workspace - that's the sync fabric tenant.
- If [scripts/deploy-dev.ps1](scripts/deploy-dev.ps1) or [scripts/live-test.ps1](scripts/live-test.ps1) ever appears to target the wrong subscription, run `az account show` first - the fix is almost always a missing `AZURE_CONFIG_DIR` in the parent shell, not a script bug.

### Recent Key Achievements (Chronological)
| Date | Achievement |
| 2026-05-22 | feat **Phase N7 - denseMode + sidebarCollapsedDefault Wiring (v0.52.0-alpha.8):** **Closes Phase N4 (v0.52.0-alpha.5) deferred items.** N4 introduced 3 preferences but only `defaultPageSize` was wired to consumers; `denseMode` + `sidebarCollapsedDefault` round-tripped to localStorage but did nothing visible. N7 ships the minimal consumer surface for each: (1) new `useUIStore.applyPreferenceDefaults()` action ([web/src/store/ui-store.ts](web/src/store/ui-store.ts)) reads `usePreferencesStore.getState().sidebarCollapsedDefault` and writes to `ui-store.sidebarCollapsed` - called ONCE at boot in [web/src/main.tsx](web/src/main.tsx) after N5 + N6 bootstrap so the sidebar honours operator preference on first paint without a flash. Top-level ESM import of preferences-store is safe (preferences-store is leaf - imports nothing from ui-store; no cycle). Idempotent. (2) New `React.useEffect` in [web/src/layout/AppShell.tsx](web/src/layout/AppShell.tsx) subscribed to `usePreferencesStore((s) => s.denseMode)` toggles `data-density="dense"` attribute on `document.documentElement` - root-level so portaled overlays + drawers see it; SSR-safe via `typeof document === 'undefined'` guard. **Why `data-density` attribute (not className):** easier to target with CSS attribute selectors `[data-density='dense']`; avoids className-merge fights with FluentProvider's atomic-class injection; future expansion (`data-density="compact"`) is trivial. **Why explicit `applyPreferenceDefaults()` action (not module-init read):** explicit boot-step is testable + idempotent + safe to extend with future cross-store defaults; module-init read would break the existing `setState({ sidebarCollapsed: false })` reset pattern in tests. **Per-table dense-row CSS deferred** (belongs in shared DataTable primitive when consolidated, OR per-tab surgical styles); the attribute wire is shipped, the visual change is per-consumer follow-up. **Tests added (+6 web vitest, 999 -> 1005):** 4 in [ui-store.test.ts](web/src/store/ui-store.test.ts) (applyPreferenceDefaults exposed + true wiring + false wiring + idempotent) + 2 in [AppShell.test.tsx](web/src/layout/AppShell.test.tsx) (denseMode=true sets `data-density='dense'`, denseMode=false removes attribute). **New [docs/PHASE_N7_DENSEMODE_SIDEBAR_DEFAULT_WIRING.md](docs/PHASE_N7_DENSEMODE_SIDEBAR_DEFAULT_WIRING.md)** with architecture (Mermaid), module map, 5 design-rationale rows, test coverage table, Standing Backlog. **Quality gates:** Stage 0 TDD: 6 RED -> 6 GREEN. Stage 1.4 web tsc baseline of **96 errors PRESERVED**. Stage 2.3 full vitest **999 -> 1005** (+6). Stage 6 commit hygiene: em-dash scan PASS; version bump v0.52.0-alpha.7 -> **v0.52.0-alpha.8**; no `--amend` / `--force` / `--no-verify`. **Test counts:** API unit 3,735 unchanged / E2E 1,197 unchanged / Web vitest **999 -> 1005** (+6) / Live SCIM 1,005 unchanged / PowerShell 15 unchanged / Playwright 76 unchanged. **Total assertions across 6 layers: 6,959 -> 6,965** (+6). No deploy in this commit. **Prod promotion: NOT triggered.** Next: v0.52.0 stable rollup OR continue N-series. |
| 2026-05-22 | feat **Phase N6 - Extensible Command Palette (v0.52.0-alpha.7):** Single-commit ship of the EXTENSIBILITY layer the existing Phase F1 CommandPalette was missing. **Scope pivot documented:** original "keyboard ergonomics MVP" already shipped - Phase F1 (`web/src/components/CommandPalette.tsx`, `cmdk`-based, Cmd+K/Ctrl+K, 3 groups: Routes / Endpoints / Quick actions) + Phase F2 (`?` help overlay + skip link). Rather than ship a duplicate palette UI, Phase N6 adds the registry that turns the palette into an extension point. **Architecture:** new [web/src/store/command-registry.ts](web/src/store/command-registry.ts) (~80 LoC) is a module-level singleton (not React context - commands are app-global; importable from any feature module at any time) with `register({id, label, keywords?, run}) / unregister(id) / clear() / all() / filter(query) / run(id)`. AND-token substring filter against `label + keywords` (lowercased + whitespace-trimmed). `register` throws on duplicate id; `run` throws on unregistered id. New [web/src/store/command-bootstrap.ts](web/src/store/command-bootstrap.ts) (~65 LoC) ships `bootstrapCommandRegistry()` - idempotent via module-level `bootstrapped` flag - that registers 4 default ops commands at boot: `telemetry.clear` -> `useTelemetryStore.getState().clear()` (closes Phase N5 + N6 intersection), `preferences.reset` -> `usePreferencesStore.getState().resetPreferences()`, `notifications.clear` -> top-level `clearNotifications()` (not a method - notifications-store exposes a function), `theme.toggle` -> lazy-imports `ui-store` then cycles `light -> dark -> system -> light`. Exports `_resetCommandBootstrapForTests()` so unit tests can re-exercise bootstrap deterministically. **CommandPalette extended** with a 4th `<Command.Group heading="Custom commands">` that reads `commandRegistry.all()`; rendered only when non-empty; each item gets testid `command-palette-custom-<id>`; `onSelect` closes the palette then invokes `cmd.run()`. **Boot wire:** `bootstrapCommandRegistry()` called once in [web/src/main.tsx](web/src/main.tsx) after `bootstrapTelemetryCollectors(router)`. **Why module-level singleton (not React context):** commands are app-global; singleton avoids prop-drilling + provider boilerplate; feature modules can register at module-import time, lazy-route mount, or settings toggle. **Why extend existing F1 palette (not build new):** avoids UI duplication; F1 paid the expensive UI cost; users expect ONE Cmd+K palette. **Why bootstrap 4 ops commands at boot:** establishes the pattern + proves the wire end-to-end; each maps to an existing store action that previously had no keyboard surface. **One scope discovery + fix:** initially built a new CommandPalette in `web/src/components/primitives/`; on grep discovered the F1 palette already shipped; deleted the duplicate (kept the registry which is net-new). **One bug fix during TDD:** command-bootstrap.test.ts initially assumed notifications-store had a `notifications` key + `clearAll` method; actual shape is `entries[]` + `unreadCount` with a top-level `clearNotifications()` function - fixed both test and implementation. **Tests added (+21 web vitest, 978 -> 999):** 11 in [command-registry.test.ts](web/src/store/command-registry.test.ts) (register/unregister/all 4, filter 5, run 2) + 7 in [command-bootstrap.test.ts](web/src/store/command-bootstrap.test.ts) (idempotency, 4 default-command actions, registration list 2) + 3 new in existing [CommandPalette.test.tsx](web/src/components/CommandPalette.test.tsx) (Custom commands group rendered when non-empty, hidden when empty, click runs handler + closes palette). **New [docs/PHASE_N6_EXTENSIBLE_COMMAND_PALETTE.md](docs/PHASE_N6_EXTENSIBLE_COMMAND_PALETTE.md)** with architecture (Mermaid), module map, 6 design-rationale rows, test coverage table, Standing Backlog (per-endpoint commands `endpoint.<id>.activate/.deactivate/.rotate-secret`, recent-commands history persisted to preferences-store, telemetry: emit `navigation` event on command run, per-command keyboard-shortcut binding via Phase F2 shortcut system). **Quality gates:** Stage 0 TDD confirmed RED in all 3 cycles. Stage 1.4 web tsc PASS - baseline of 96 errors PRESERVED. Stage 2.3 full vitest PASS - **978 -> 999** (+21 tests). Stage 6 commit hygiene: em-dash scan PASS on every modified file; version bump v0.52.0-alpha.6 -> **v0.52.0-alpha.7** in api/package.json + web/package.json; no `--amend` / `--force` / `--no-verify`. **Test counts:** API unit 3,735 unchanged / E2E 1,197 unchanged / Web vitest **978 -> 999** (+21) / Live SCIM 1,005 unchanged / PowerShell 15 unchanged / Playwright 76 unchanged. **Total assertions across 6 layers: 6,938 -> 6,959** (+21). No deploy in this commit; dev + prod still on v0.52.0-alpha.3 until a follow-up deploy commit lifts them. **Prod promotion: NOT triggered.** Next: Phase N7 (denseMode + sidebarCollapsedDefault wiring - closes Phase N4 deferred items) or v0.52.0 stable rollup. |
| 2026-05-21 | feat **Phase N5 - Frontend Telemetry MVP (v0.52.0-alpha.6):** Pure frontend telemetry, zero new runtime deps. Captures (a) route navigations (page views) via TanStack Router `subscribe('onResolved')` and (b) uncaught errors via `window.addEventListener('error'|'unhandledrejection')`. **Architecture:** [web/src/store/telemetry-store.ts](web/src/store/telemetry-store.ts) is a Zustand atom buffering two event variants (`navigation` + `error`) with hard cap 50 events + hard TTL 24h (both pruned at record-time; no background timer). [web/src/store/telemetry-collectors.ts](web/src/store/telemetry-collectors.ts) ships `bootstrapTelemetryCollectors(router)` - idempotent (HMR-safe), returns explicit teardown (test-safe), called once in [web/src/main.tsx](web/src/main.tsx) before app mount. **Opt-in gating:** new `telemetryOptIn` preference in preferences-store v1 envelope (default `true` - frontend-only MVP has zero data leaving the tab, so default-on is privacy-safe; will FLIP to default-off when Phase O ships server ingestion). `record()` reads the gate at call-time so toggling takes effect on the next event without teardown/resubscribe. **SettingsPage TelemetryCard:** new card with Fluent `Switch` opt-in + "Last 10 events" newest-first preview table + "Clear buffer" button + empty-state caption. Testids `settings-telemetry-card`, `settings-telemetry-opt-in`, `settings-telemetry-clear`, `settings-telemetry-empty`, `settings-telemetry-row-<n>`. **Why in-memory (not persisted):** telemetry is operational signal not preference; persisting across reloads makes ring stale fast; Phase O ships the durable layer (server table + 7-day retention + per-tenant scoping). **Why no `web-vitals` lib:** adds a real dep + bundle entry + schema variant; sensible Phase N5b/O scope; MVP avoids. **3-commit TDD progression** (each RED-first per Stage 0): `fc5b7a6` telemetry-store foundation + telemetryOptIn preference (+10 vitest = 969); `491464e` collectors wire + SettingsPage TelemetryCard (+9 vitest = 978); this commit docs + Playwright + version bump (+0 vitest, +1 Playwright). New [web/e2e/telemetry.spec.ts](web/e2e/telemetry.spec.ts) browser-side smoke vs dev FQDN asserts TelemetryCard + 4 controls render on /settings. **New [docs/PHASE_N5_FRONTEND_TELEMETRY.md](docs/PHASE_N5_FRONTEND_TELEMETRY.md)** with architecture (Mermaid), module map, test coverage table, 6 design-rationale subsections, Standing Backlog (server ingestion `POST /scim/admin/telemetry`, 7-day retention, web-vitals, sourcemap upload, default-on flip, per-user telemetry history view, PII redaction of path segments). **Quality gates:** Stage 0 TDD confirmed RED in all 3 cycles. Stage 1.4 web tsc PASS - baseline of 96 errors preserved across all 3 commits. Stage 2.3 full vitest PASS - 959 -> 969 -> **978** (+19 tests across 4 new/modified suites). Stage 6 commit hygiene: em-dash scan PASS on every modified file; version bump v0.52.0-alpha.5 -> **v0.52.0-alpha.6** in api/package.json + web/package.json; no `--amend` / `--force` / `--no-verify`. **Test counts:** API unit 3,735 unchanged / E2E 1,197 unchanged / Web vitest **959 -> 978** (+19) / Live SCIM 1,005 unchanged / PowerShell 15 unchanged / Playwright **75 -> 76** (+1 telemetry smoke). **Total assertions across 6 layers: 6,918 -> 6,938** (+20). No deploy in this commit; dev + prod still on v0.52.0-alpha.3 until a follow-up deploy commit lifts them. **Prod promotion: NOT triggered.** |
| 2026-05-20 | feat **Phase N4 - Settings Persistence (v0.52.0-alpha.5):** Per-user UI preferences now persist across page loads and browser sessions. New [web/src/store/preferences-store.ts](web/src/store/preferences-store.ts) (~120 LoC) is a Zustand atom + hand-rolled versioned localStorage layer (envelope `{v: 1, prefs: {...}}` at key `scimserver.preferences.v1`). Hand-roll chosen over `zustand/middleware/persist` for versioned-envelope + clamping + allowlisting at hydrate time (defends against stale storage from a future schema). **Three preferences in v1:** `defaultPageSize` (10/20/50/100, default 20, clamped via `ALLOWED_PAGE_SIZES`) - live across all 4 list surfaces; `denseMode` (boolean, default false) - persists, UI wiring deferred; `sidebarCollapsedDefault` (boolean, default false) - persists, UI wiring deferred. **SettingsPage gains a `PreferencesCard`** with Fluent `Dropdown` + 2 `Switch` + Reset button (new testids `settings-preferences-card`, `settings-preferences-default-page-size`, `settings-preferences-dense-mode`, `settings-preferences-sidebar-collapsed-default`, `settings-preferences-reset`). **Schema change:** [`paginationSchema.pageSize`](web/src/routes/search-schemas.ts) flipped from `.default(20)` to `.optional()` so preferences-store is the SINGLE source of page-size defaults (still 20 out-of-the-box because `PREFERENCES_DEFAULTS.defaultPageSize === 20`). URL `?pageSize=N` always wins (deep-link + share intact). **4 tabs + 4 route loaders wired to fall back to preferences-store:** components use `usePreferencesStore((s) => s.defaultPageSize)` selector; TanStack Router loaders use `usePreferencesStore.getState().defaultPageSize` (synchronous outside render) so prefetched query keys match what the component will request (no double-fetch). **3-commit TDD progression** (each RED-first per Stage 0): `a8f53bc` preferences-store foundation (+13 vitest); `aa6babc` SettingsPage PreferencesCard wire (+3 vitest); this commit consumer wire (Users/Groups/Logs/Activity tabs + 4 route loaders) + docs + Playwright + version bump (+3 vitest, +1 Playwright). New [web/e2e/preferences.spec.ts](web/e2e/preferences.spec.ts) browser-side smoke vs dev FQDN asserts PreferencesCard + 4 controls render on /settings. **New [docs/PHASE_N4_SETTINGS_PERSISTENCE.md](docs/PHASE_N4_SETTINGS_PERSISTENCE.md)** with architecture (Mermaid), module map, test coverage table, design rationale (hand-rolled persist vs middleware; separate from ui-store; URL stays winning; route loaders use getState; why clamp to [10,20,50,100]), Standing Backlog for deferred consumers (per-page gear panels, denseMode into table primitives, sidebarCollapsedDefault into ui-store bootstrap, server-side `/admin/me/preferences`). **Quality gates:** Stage 0 TDD confirmed RED in all 3 cycles. Stage 1.4 web tsc PASS - baseline of 96 errors preserved across all 3 commits. Stage 2.3 full vitest PASS - 940 -> 953 -> 956 -> **959** (+19 tests). Stage 6 commit hygiene: em-dash scan PASS on every modified file; version bump v0.52.0-alpha.4 -> **v0.52.0-alpha.5** in api/package.json + web/package.json; no `--amend` / `--force` / `--no-verify`. **Test counts:** API unit 3,735 unchanged / E2E 1,197 unchanged / Web vitest **940 -> 959** (+19) / Live SCIM 1,005 unchanged / PowerShell 15 unchanged / Playwright **74 -> 75** (+1 preferences smoke). **Total assertions across 6 layers: 6,898 -> 6,918** (+20). No deploy in this commit; dev + prod still on v0.52.0-alpha.3 until a follow-up deploy commit lifts them. **Prod promotion: NOT triggered.** |
| 2026-05-19 | feat **Phase N3 - Export Everywhere (v0.52.0-alpha.4):** Uniform export split-button (CSV / JSON / NDJSON) on all four list surfaces - UsersTab, GroupsTab, LogsTab, ActivityTab. Pre-N3 operators had no way to pull rendered data off any list without curl + jq. Single shared primitive [web/src/components/primitives/ExportSplitButton.tsx](web/src/components/primitives/ExportSplitButton.tsx) (~140 LoC, Fluent UI Menu/MenuTrigger/MenuPopover/MenuList/MenuItem with ArrowDownloadRegular icon) drives all four surfaces - one set of testids (`export-button`, `export-menu-csv|json|ndjson`), one disable rule (`eagerEmpty = rows defined && rows.length === 0`), one UTC stamp format (`YYYYMMDDTHHMMSSZ` for stable sort). Three new exported helpers in [web/src/utils/csv-export.ts](web/src/utils/csv-export.ts): `toJson(rows, {pretty?})` (default pretty=true, 2-space indent), `toNdjson(rows)` (empty -> "", newline-delimited compact JSON, no trailing newline), `triggerJsonDownload` + `triggerNdjsonDownload` (Blobs with `application/json;charset=utf-8` + `application/x-ndjson;charset=utf-8` MIME types). CSV reuses BulkTab/OperationsPage column set. **5-commit TDD progression** (each RED-first per Stage 0): `03691e8` foundation (+22 vitest: 14 csv-export + 8 ExportSplitButton); `0be5147` UsersTab wire (+3); `e13bc0a` GroupsTab wire (+2; row projection {id, displayName, memberCount, created}); `49cb9eb` Logs+Activity wire (+6); this commit (docs + Playwright + version bump). New [web/e2e/export.spec.ts](web/e2e/export.spec.ts) browser-side smoke vs dev FQDN (skip-safe on endpoints-empty / users-empty branches; asserts toolbar wire on first endpoint's UsersTab + all three menu items render). **New [docs/PHASE_N3_EXPORT_EVERYWHERE.md](docs/PHASE_N3_EXPORT_EVERYWHERE.md)** with architecture (Mermaid), module map, test coverage table, design rationale (why split-button, why pretty JSON + compact NDJSON, why per-surface row projection, why eager rows), out-of-scope items (server-side dump, per-column toggle, Excel `.xlsx`) deferred to Phase O. **Quality gates:** Stage 0 TDD confirmed RED in all 5 cycles. Stage 1.4 web tsc PASS - prod-file baseline of 9 errors preserved across all 5 commits. Stage 2.3 full vitest PASS - 909 -> 931 -> 934 -> 936 -> **940** (+31 tests across 6 new/modified suites). Stage 6 commit hygiene: em-dash scan PASS on every modified file; version bump v0.52.0-alpha.3 -> **v0.52.0-alpha.4** in api/package.json + web/package.json; no `--amend` / `--force` / `--no-verify`. **Test counts:** API unit 3,735 unchanged / E2E 1,197 unchanged / Web vitest **909 -> 940** (+31) / Live SCIM 1,005 unchanged / PowerShell 15 unchanged / Playwright **73 -> 74** (+1 export smoke). **Total assertions across 6 layers: 6,866 -> 6,898** (+32). No deploy in this commit; dev + prod still on v0.52.0-alpha.3 until a follow-up deploy commit lifts them to alpha.4. **Prod promotion: NOT triggered.** |
| 2026-05-19 | test **Phase N3a Stage 5 closure - Playwright security-headers spec (no version bump):** Closed the last deferred leg of the Phase N3a four-layer header lockdown. New [web/e2e/security-headers.spec.ts](web/e2e/security-headers.spec.ts) (~120 LoC, zero em-dash, 3 tests / 21 mirrored assertions) is the browser-side twin of the [scripts/live-test.ps1 TEST SECTION 9z-AJ](scripts/live-test.ps1) PowerShell HTTP probe + [api/test/e2e/security-headers.e2e-spec.ts](api/test/e2e/security-headers.e2e-spec.ts) in-process supertest probe. Uses Playwright's `APIRequestContext` (raw HTTP, no browser-fetch CORS gymnastics) so every response header is observed as the server emitted it; identical wire bytes to a real Chromium navigation. Probes 3 routes: `/scim/health` (unauthenticated; asserts all 12 helmet headers + Permissions-Policy denials + COEP absent + HSTS branch required-on-HTTPS / optional-on-HTTP), `/scim/admin/version` (authenticated; asserts headers still fire on 200|401|404 because helmet sits before auth in main.ts), `/` (SPA fallback HTML; asserts the CSP `script-src 'unsafe-inline'` Fluent UI v9 compatibility carve-out is intact). **Stage 4 closure also retroactively recorded** in [docs/PHASE_N3A_HELMET.md](docs/PHASE_N3A_HELMET.md): the 2026-05-19 cross-tenant cutover live regression (1005/1005 PASS vs new prod + new dev) includes the 21-assertion 9z-AJ section that was originally deferred. **Quality gates:** Stage 0 TDD - this spec WAS the RED-first test for the 2026-05-18 helmet build (locked at unit + E2E in that commit); this commit ships the third layer the original RED plan referenced; GREEN on first run because helmet is already deployed in v0.52.0-alpha.3. Stage 1.4 web tsc PASS 0 new errors. Stage 5.2 playwrightSpecHygieneAudit PASS - `web/e2e/new-ui.spec.ts` testids confirmed still present in current source (per Standing Backlog: name-legacy but content-current; KEEP). Stage 5.3 Playwright vs new dev FQDN: **3/3 PASS in 1.4s**. Stage 6 commit hygiene: em-dash scan PASS; no `--amend` / `--force` / `--no-verify`; no version bump (test-only addition). **Test counts:** Playwright 70 -> **73** (+3); all other layers unchanged. **Total assertions across 6 layers: 6,845 -> 6,866** (+21 assertion mirrors packaged as 3 Playwright tests). **No deploy** - zero runtime effect; validates EXISTING new-tenant dev deploy. **Prod promotion:** NOT triggered - both prod + dev still on v0.52.0-alpha.3 (new tenant). |
| 2026-05-19 | infra **Cross-tenant Azure deploy + data migration (v0.52.0-alpha.3, no version bump):** Full re-deploy of dev + prod into the new tenant `f08e6aff-ca0f-4f11-81fa-1ffd43323373` (ProvIAM_Subscription `5738ea6a-533b-4c0d-a18a-d322f2094475`). **New FQDNs (replace prior ones in all future references):** prod = `https://scimserver.proudbush-ae90986e.eastus.azurecontainerapps.io`, dev = `https://scimserver-dev.proudbush-ae90986e.eastus.azurecontainerapps.io`. Both apps min=max=1 replica (Prisma migration race-prevention). Both share ONE Container App Environment `scimserver-env` in `scimserver-prod` RG (eastus) because the new subscription is capped at 1 env globally - dev cross-RG via `--environment <fullResourceId>`. Per-app Postgres: prod=`scimserver-pg-new2`, dev=`scimserver-pg-dev-new2` (both eastus2, admin user `scimadmin`). New ACR `acrscimserver20622.azurecr.io` (admin-enabled). Image tag pushed: `scimserver:0.52.0-alpha.3` (digest sha256:66fb0a9a...). **Root-caused + fixed: P3009 baseline migration silent failure on Azure PG.** Baseline migration `20260223000000_postgresql_baseline` requires `citext`, `pg_trgm`, `pgcrypto`. On Azure these are NOT installable by default - must be allow-listed via `azure.extensions` server parameter (static, requires PG restart). Diagnosed via `docker run -v <baseline-sql>:/sql postgres:16-alpine psql ... -v ON_ERROR_STOP=1 -f /sql/migration.sql 2>&1` which surfaced `extension "citext" is not allow-listed for users in Azure Database for PostgreSQL`. **Fix baked into [scripts/deploy-azure.ps1](scripts/deploy-azure.ps1)** (~line 643): after PG provision, sets `azure.extensions=CITEXT,PG_TRGM,PGCRYPTO`, restarts PG, idempotent (checks current value), calls `Stop-Deployment` on failure. Without this fix, every fresh Azure PG deploy of SCIMServer fails P3009 silently with no SQL visible in app logs. Other deploy script improvements: 3 new params (`-AcrLoginServer`, `-ImageRepository`, `-PgServerName`) for non-default registries + DB server names; PS case-insensitive variable shadow bug fix (renamed local `$imageRepository` -> `$ghcrAuthCheckRepo` to avoid clobbering the deploy param). [infra/containerapp.bicep](infra/containerapp.bicep) generalized registry-credentials block (was hardcoded GHCR; now works for any ACR). **New idempotent migration script [scripts/migrate-old-prod.ps1](scripts/migrate-old-prod.ps1)** (~210 LoC, secrets-free) replays endpoints + users + groups from any source SCIM server into one-or-more targets: paginates `/scim/admin/endpoints?view=full` + `/Users?startIndex&count` + `/Groups?startIndex&count`, strips server-side `id`+`meta` from each body, POSTs to target, builds `srcUserId -> newUserId` map per endpoint so group `members[].value` rewrites correctly (warns + drops unmapped members). Skips an endpoint when target already has same-named one. **Migration outcome (old prod scimserver2.yellowsmoke -> new prod + new dev):** **36/36 endpoints created on each target (0 failures)**, **297/301 users migrated to each target** (4 outliers on Vishnu-ISV-1 had stray `urn:ietf:params:scim:schemas:extension:veritas:2` extension URN in body without being in the endpoint's `schemas[]` registry - source-data quality issue rejected by StrictSchemaValidation, not a script bug), **40/40 groups migrated to each target**. Both targets verified: prod live-test **1005/1005 PASS** (up from 1004/1005 with the prior timing flake; flake didn't recur); dev live-test **1005/1005 PASS** in 72.8s. Spot-checked high-volume endpoints: Vishnu-ISV-1=107 users (expected 111-4), Sagar-ISV-2=79, Himanshu-ISV-1=28. **Files touched in this commit:** [scripts/deploy-azure.ps1](scripts/deploy-azure.ps1) (citext fix + new params + shadow-bug fix), [infra/containerapp.bicep](infra/containerapp.bicep) (generalized ACR creds), [scripts/migrate-old-prod.ps1](scripts/migrate-old-prod.ps1) NEW, [.gitignore](.gitignore) (secrets + state + side-project additions + 1 pre-existing em-dash fix). Local secret-bearing files (`.acrcreds.txt`, `.pgpass-*.txt`, `.oldprod-token.txt`, `prod-app-template.json`, `migration.log`, `scripts/state/*.json`, `scripts/_deploy-*-new-tenant.ps1`) excluded via .gitignore - NEVER commit. **No version bump** - infra/deploy + script-only change, zero api/web code touched. **No new tests** - this commit IS the verification (1005/1005 on both envs). Pre-existing test counts unchanged: **6,845 total assertions across 6 layers** (API unit 3,735 / E2E 1,197 / Web vitest 909 / Live SCIM 1005 / PowerShell 15 / Playwright 70). **Prod promotion: this IS the prod deploy** of the new-tenant cutover. Old-prod tenant management plane is expired but data plane was usable for the migration read-only window. |
| 2026-05-18 | chore **Phase N3a Stage 4 local closure (v0.52.0-alpha.3 unchanged):** Local Docker build + smoke test of the helmet-wired image fully GREEN. Bug found + fixed: stale `api/tsconfig.build.tsbuildinfo` from the Windows host was being COPY'd into the docker build context, confusing tsc-incremental so only .d.ts files were emitted (no .js) and the container exited with `Cannot find module '/app/dist/main.js'`. Fix: added `*.tsbuildinfo` to [api/.dockerignore](api/.dockerignore). New live-test TEST SECTION 9z-AJ added (21 assertions covering all 12 helmet headers + Permissions-Policy + HSTS + COEP-absent + 3-route probe of /scim/health + /scim/admin/version + /). Live-SCIM baseline raised: **1005/1005 PASS** in 31.7s vs local Docker container (was 984/984 pre-9z-AJ). Cross-tenant deploy work (new tenant `f08e6aff-ca0f-4f11-81fa-1ffd43323373` / ProvIAM_Subscription) follows in a subsequent commit. |
| 2026-05-18 | feat **Phase N3a - `helmet` middleware (v0.52.0-alpha.3):** Closes the 2026-05-17 Stage X.2 first-run report's HIGHEST-LEVERAGE NEW GAP. Pre-N3a the SCIM API shipped to dev + prod with **zero** browser-enforced defense-in-depth response headers - no CSP, no HSTS, no X-Frame-Options, no X-Content-Type-Options, no Referrer-Policy, no COOP/CORP, no Permissions-Policy. **Architecture:** new [api/src/security/helmet-config.ts](api/src/security/helmet-config.ts) (~125 LoC) centralises helmet options + exports `PERMISSIONS_POLICY_HEADER_VALUE`. Both [api/src/main.ts](api/src/main.ts) and [api/test/e2e/helpers/app.helper.ts](api/test/e2e/helpers/app.helper.ts) import the same factory so production and test bootstraps cannot drift. Inserted EARLY in the middleware chain (after X-Request-Id, before CORS) so headers fire on every response including 401/403/415 short-circuits. **Header set:** CSP (default-src `'self'`; script-src + style-src allow `'unsafe-inline'` for the inline fallback-remove script in `web/index.html` + Fluent UI v9 makeStyles atomic-class injection; frame-ancestors `'none'`; object-src `'none'`; connect-src `'self'`), HSTS (max-age=15552000 + includeSubDomains, **production only** so localhost is not poisoned), X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Cross-Origin-Opener-Policy + Cross-Origin-Resource-Policy same-origin (COEP intentionally disabled), Origin-Agent-Cluster, X-Permitted-Cross-Domain-Policies none, X-DNS-Prefetch-Control off, X-Download-Options noopen, plus manual Permissions-Policy denying camera + microphone + geolocation + payment + usb + magnetometer + accelerometer + gyroscope. **TDD discipline:** Stage 0 RED on new [api/test/e2e/security-headers.e2e-spec.ts](api/test/e2e/security-headers.e2e-spec.ts) confirmed FIRST (9 failed / 2 passed - 2 negative assertions trivially passed without helmet). GREEN after install + wire: 11/11 E2E + 7/7 unit. **Quality gates:** Stage 1.2 api tsc build PASS 0 errors; Stage 1.3 api ESLint 0 errors / 465 warnings baseline exact; Stage 2.1 api jest unit **3,735/3,735** (was 3,728 + 7); Stage 2.2 api jest E2E **1,197/1,197** (was 1,186 + 11); Stage 2.5 crossBackend N/A (helmet sits above persistence); Stage 3a.3 error-envelope verified by 66/66 spa-fallback still PASS + the 11 new headers tests assert on both 200 and 401 paths; Stage 3b.4 securityAudit IS the X.2 finding; Stage 3c.1 codeReviewSelfAudit 1 module + 1 unit + 1 E2E single-responsibility per file; Stage 3c.2 CHANGELOG + Session_starter + INDEX + new [docs/PHASE_N3A_HELMET.md](docs/PHASE_N3A_HELMET.md) all updated. **Cross-Cutting Security Gate Map updated** in [.github/copilot-instructions.md](.github/copilot-instructions.md): "Web security headers (CSP/HSTS/etc.)" row moved from DEFERRED to ACTIVE with helmet-config.ts + main.ts citations. **Test counts:** API unit 3,728 -> **3,735** (+7) / E2E 1,186 -> **1,197** (+11) / Web vitest 909 unchanged / Live SCIM 984 unchanged (post-deploy commit will lock the headers on the wire) / PowerShell 15 unchanged / Playwright 70 unchanged (post-deploy commit will add `web/e2e/security-headers.spec.ts`) = **6,845 total assertions across 6 layers** (was 6,827; +18). New dep: `helmet@^8.1.0` (production). Versions bumped lockstep v0.52.0-alpha.2 -> **v0.52.0-alpha.3** (api/package.json + web/package.json; lockfile regen + Docker rebuild + dev deploy + 984+ live SCIM contract + Playwright spec all deferred to a follow-up commit pair so this commit stays scoped to the security-headers Stage 0-3 closure). **Prod promotion: NOT triggered** - dev to-be-on v0.52.0-alpha.3; prod still on v0.48.0. |
| 2026-05-18 | test **Stage 5 Playwright closure for Steps 1-4 retroactive (no version bump):** The operator caught that Stage 5 (Playwright spec hygiene + Playwright vs dev) was NEVER invoked across commits 2721c3b / 3b5126e / 6d04ccc / 6dc9b7b despite Steps 1 and 4 touching `web/`. Exactly the "I forgot to run X" failure mode the [scripts/run-all-gates.ps1](scripts/run-all-gates.ps1) orchestrator (Step 2) was built to prevent. This commit retroactively closes the gap. **Hygiene audit** (Stage 5.2 `playwrightSpecHygieneAudit` content executed inline): inventoried all 8 `web/e2e/*.spec.ts` files against current shipping UI; finding that `web/e2e/new-ui.spec.ts` is **not** stale despite being on the standing "delete in Phase I v0.48.0" list (its asserted testids `app-shell` / `app-header` / `app-sidebar` / `theme-toggle` / `sidebar-toggle` / `kpi-row` / `endpoint-grid` all still exist in source; deleting it would lose real coverage). **Stage 5.3 Playwright vs dev:** full 70-test run on `https://scimserver-dev.yellowrock-b029dcc6.westus2.azurecontainerapps.io` (E2E_TOKEN `changeme-scim`) - **64 pass / 4 fail (visual baseline) / 2 skip** initially, then **2 stale baselines refreshed** via `--update-snapshots` (`command-palette-chromium-win32.png` + `keyboard-shortcuts-help-chromium-win32.png` were truly stale from 2026-05-17 00:28-00:32 captured before Phase N2 mounted the OnboardingWizard chrome-level - now regenerated to reflect post-N2 rendering). **Remaining 2 Dashboard visual flakes** (light + dark themes) confirmed NOT caused by Steps 1-4 (`git diff --stat HEAD~4..HEAD -- web/` shows zero changes to `DashboardPage.tsx` / `CommandPalette.tsx` / `KeyboardShortcutsHelp.tsx`) - root cause is the `NON_DETERMINISTIC_SELECTORS` mask in [web/e2e/visual-regression.spec.ts](web/e2e/visual-regression.spec.ts) does not cover live KPI counts / endpoint-grid contents that drift with tenant data accumulation; added to Standing Backlog for a follow-up commit (testid-driven mask additions). **NEW Playwright spec** [web/e2e/onboarding.spec.ts](web/e2e/onboarding.spec.ts) (5 tests / 9.0s vs dev / 5 PASS) covers the chrome-level Phase N2 + Stage X.1 A.4 wizard end-to-end: forceOpen flag renders surface + all 4 step dots + 3 action buttons; completedAt set hides it; Skip writes ISO timestamp + dismisses; Close (X) writes timestamp + dismisses; Get started advances to step 2 with `entra-id` preset preselected (`data-selected="true"`). Steps 3-4 intentionally NOT covered E2E (would mutate live tenant with real endpoint + credential creation); 14 vitest already cover full happy path against mocked mutation hooks. **Quality gates:** Stage 1.4 web tsc 96/96 baseline preserved; Stage 5.2 hygiene audit DONE (with new-ui keep-as-is finding); Stage 5.3 Playwright vs dev GREEN post-refresh (5/5 onboarding + 12/12 visual-regression on refresh run; 64-66 cumulative pass on full suite with 0-2 Dashboard flake; 2 intentional skips). **Test counts:** API unit 3,728 / E2E 1,186 / Web vitest 909 / Live SCIM 984 / PowerShell 15 / Playwright +5 (was 65, now 70) = **6,822 + 5 Playwright = 6,827 across 6 layers** (this is the first commit explicitly counting Playwright in the layer summary). **No version bump** - pure test addition + baseline refresh. **No deploy** - this validates EXISTING dev deploy. **Prod promotion:** NOT triggered - dev still on v0.52.0-alpha.2; prod still on v0.48.0. |
| 2026-05-17 | refactor **OnboardingWizard split into thin dispatcher + 6 sibling files (Stage X.1 A.4 closure; no version bump):** Fourth closure from the 2026-05-16 Stage X.1 follow-up sequence. Extracted the 478-line `web/src/layout/OnboardingWizard.tsx` monolith (Stage 3c.1 "concerning" >450 LoC threshold per May 2026 Design Deep Analysis precedent) into a thin **~245-line step-dispatcher** + 6 sibling files under new `web/src/layout/onboarding/` folder: `onboarding-styles.ts` (88 LoC shared `makeStyles` hook + `Step` type + `STEP_TITLES` + `DEFAULT_PRESET`), `StepDots.tsx` (32 LoC), `StepWelcome.tsx` (30 LoC), `StepPickPreset.tsx` (84 LoC), `StepIssueCredential.tsx` (87 LoC), `StepSendRequest.tsx` (28 LoC). **Wizard retains all state + handlers + footer DialogActions; children are presentation-only and props-driven** (no new state, no new hooks pushed down, no behavior shift). Every existing `data-testid` selector preserved EXACTLY (`onboarding-wizard`, `onboarding-close`, `onboarding-step-dot-{1..4}`, `onboarding-step-{1..4}`, `onboarding-skip`, `onboarding-get-started`, `onboarding-preset-card-{name}`, `onboarding-step-2-next`, `onboarding-issue-credential`, `onboarding-plaintext-token`, `onboarding-copy-token`, `onboarding-step-3-next`, `onboarding-send-it-now`, `onboarding-do-this-later`). **Pre-existing main entry bundle breach (368.24 kB vs 200 kB budget) verified pre-existing via stash-and-rebuild** - refactor introduces 0 bytes of bundle growth and 0 new tsc errors (96/96 baseline exact). **Quality gates:** Stage 0 TDD existing 14 OnboardingWizard tests held GREEN throughout the file split (RED-on-refactor would have surfaced any selector / state / handler regression), Stage 1.4 web tsc 96/96 exact, Stage 1.6 web vite build PASS in 14.42s, Stage 2.3 web vitest 909/909 PASS, Stage 3c.1 codeReviewSelfAudit RESOLVED (this IS the closure of the previously-flagged SRP concern). **Test counts unchanged at 6,822 assertions across 5 layers** (API unit 3,728 / E2E 1,186 / Web vitest 909 / Live SCIM 984 / PowerShell 15). **No version bump** - pure internal refactor. **No deploy** - zero runtime effect (rendered output byte-identical). **Prod promotion:** NOT triggered - dev still on v0.52.0-alpha.2; prod still on v0.48.0. |
| 2026-05-17 | docs **First Stage X.2 `securityBestPracticesIntake` run + apply trivial findings (no version bump):** Third closure from the 2026-05-16 Stage X.1 follow-up sequence. 10-category external-landscape security meta-audit with hard URL-citation + confidence-level + owner-action constraints honored on every finding. **Headline:** the codebase is in considerably better security posture than the May 2026 Standing Backlog implied. **0 Critical + 6 High + 7 Medium + 2 Speculative findings.** **4 Standing Backlog items caught as ALREADY ACTIVE** - Dependabot weekly ([.github/dependabot.yml](.github/dependabot.yml) npm/api + npm/web + github-actions + docker), GHA action SHA pinning (all 5 workflow files verified), Trivy image scan ([.github/workflows/build-and-push.yml](.github/workflows/build-and-push.yml) + [.github/workflows/build-test.yml](.github/workflows/build-test.yml) with HIGH+CRITICAL gating + `.trivyignore` documented exceptions + weekly stale-entry review via [.github/workflows/trivyignore-review.yml](.github/workflows/trivyignore-review.yml)), CodeQL with `security-extended` + `security-and-quality` query packs ([.github/workflows/codeql.yml](.github/workflows/codeql.yml) weekly + per-PR). Removed from the deferred list to stop wasting operator attention. **1 standing-rule node-version drift fixed inline** - node:25 -> node:24 in the lockfile-regen instruction (matches the actual deployed runtime in `api/Dockerfile`). **9 new DEFERRED items added** to Standing Backlog: API helmet/CSP/HSTS headers (HIGHEST-LEVERAGE NEW GAP per the intake; slate for Phase N3 design start), API rate limiting via `@nestjs/throttler` (defer to Phase O unless N3 telemetry adds new unauthenticated surface), GDPR Article 17 hard-delete admin path, Azure Postgres Managed Identity migration, quarterly secret rotation policy, `npm ci --audit-signatures` in CI, OpenSSF Scorecard scheduled scan, CODEOWNERS for security-sensitive paths, DPoP (RFC 9449) for endpoint credentials. **3 actionable prompt amendments shipped inline** (R1 OWASP API Top 10 v2023 with SCIM-specific BOPLA call-out in `securityAudit.prompt.md` + R2 RFC 9700 OAuth BCP + RFC 7644 §7 Security Considerations in `securityAudit` + `auditAgainstRFC` + R3 EU AI Act Aug 2024 special PII categories in `logging-verification`). **Cross-Cutting Security Gate Map updated:** 4 cells moved DEFERRED -> ACTIVE with URL citations; 1 cell PARTIAL (CORS hardening - configurable but default allow-all); 2 new cells (rate limiting + secret rotation cadence). **Standing Backlog cleanup:** structure now distinguishes "Already ACTIVE (moved from DEFERRED)" from "Still DEFERRED" with rationale for each. **Quality gates:** Stage 0 TDD N/A (meta-audit), Stage 1.x N/A (Markdown only), Stage 2 run-all-gates.ps1 contract test still 46/46 PASS, Stage 3b.4 securityAudit IMPROVED via R1/R2 (subsequent runs benefit), Stage 3c.2 docs INDEX + CHANGELOG + Session_starter updated. **Test counts unchanged at 6,822 assertions across 5 layers** (API unit 3,728 / E2E 1,186 / Web vitest 909 / Live SCIM 984 / PowerShell 15). **No version bump** - pure meta + doc + prompt change. **No deploy** - zero runtime effect. **Prod promotion:** NOT triggered - dev still on v0.52.0-alpha.2; prod still on v0.48.0. Source report: [docs/strategy/SECURITY_INTAKE_2026-05-17.md](docs/strategy/SECURITY_INTAKE_2026-05-17.md). |
| 2026-05-17 | chore **`scripts/run-all-gates.ps1` orchestrator + contract test (no version bump):** Second closure from the 2026-05-16 Stage X.1 self-audit (D.1). Single-command walker that surfaces all 39 gates from the 7-stage Mandatory Quality Gates strategy in the right order (Stage 0 -> 6; Stage X intentionally NOT walked per-commit). Drops the documented ~25 % gate-invocation tax to near-zero. **Architecture:** table-driven `$STAGES` registry of stage-name -> array-of-gates; each gate has `Name`, `Kind` (Shell or Prompt), `Command` or `PromptPath`, optional `WorkDir`, optional `When` scriptblock predicate + `Reason` text. Shell gates execute via `Invoke-Expression` and capture exit code + duration; Prompt gates print the `.github/prompts/` path and (unless `-NoPrompt`) pause for operator acknowledgment via `Read-Host`. **Auto-skip-no-op for 6 gates** with documented triggers consulting `git diff --name-only`: bundleBudgetAudit (when `web/src/routes/` untouched), prismaMigrationAudit (when `api/prisma/` untouched), crossBackendParityAudit (when no changed `api/src/` file references `isInMemoryBackend`), dependencyCveSweep (when no `package*.json` change), Stage 5 trio (when no `web/` files touched), Stage 6 version-bump (when no `package.json` changes). **4 params:** `-SkipPrompts` (~20 min shell-only mode), `-Stage <N>` (matches 3a+3b+3c when 3 is passed), `-DryRun` (preview plan in ~3 s), `-NoPrompt` (CI mode; prompts surface as PROMPT_NOT_ACKNOWLEDGED). **Exit codes:** 0 all passed / 1 any FAIL / 2 prereq failure (missing git, npm, node, api/, web/). **Report:** structured Markdown in `test-results/run-all-gates-<DATE>.md` with one table per stage, one row per gate, status vocabulary PASS / FAIL / SKIPPED / PARTIAL, duration column. **TDD discipline:** contract test [scripts/test/run-all-gates.contract.ps1](scripts/test/run-all-gates.contract.ps1) (46 assertions) written FIRST and confirmed RED (script does not exist), then orchestrator written and confirmed GREEN (46/46 PASS). DryRun smoke tested - 39 gates registered, walk completes in <5 s. `-Stage 1` filter restricts walk to 9 gates from Stage 1 only. Auto-skip-no-op verified working (playwrightSpecHygieneAudit + Stage 6 version-bump correctly auto-skipped on this commit because no `web/e2e/` or `package.json` changes). **Smoke-screen risk** explicitly addressed: orchestrator does NOT mark a commit "safe to push"; findings still require operator engagement; the report is a scoreboard, not a stamp. New [docs/RUN_ALL_GATES_ORCHESTRATOR.md](docs/RUN_ALL_GATES_ORCHESTRATOR.md) (architecture Mermaid + per-stage gate mapping + cost-vs-value + DoD + future-evolution backlog). [docs/INDEX.md](docs/INDEX.md) updated with new entry. **Test counts:** API unit 3,728 / E2E 1,186 / Web vitest 909 / Live SCIM 984 unchanged; PowerShell contract 14 -> **15** (+1 the new `run-all-gates.contract.ps1` suite). **Total 6,821 -> 6,822 (+1 suite; +46 internal assertions inside the suite).** **No version bump** - pure tooling addition. **No deploy** - PowerShell-only change with zero runtime effect. **Prod promotion:** NOT triggered - dev still on v0.52.0-alpha.2; prod still on v0.48.0. |
| 2026-05-17 | chore **Web vitest coverage thresholds ratcheted (no version bump):** First closure from the 2026-05-16 Stage X.1 self-audit (F.1). Floor in [web/vite.config.ts](web/vite.config.ts) raised: lines 78 -> **82** (measured 83.55), branches kept at **70** (measured 72.32; only 2.32 % headroom), functions 65 -> **69** (measured 71.3), statements 75 -> **78** (measured 80.32). Contract-locked in [web/src/test/coverage-config.test.ts](web/src/test/coverage-config.test.ts) (test tightened FIRST per Stage 0 TDD, then `vite.config.ts` bumped to make it pass). Locks in earned coverage so future drift gets caught by the gate, not by a human review. Per standing rule the coverage gate can only ratchet UP from here. **Quality gates:** Stage 1.4 web tsc 96/96 baseline maintained (no new errors in changed files), Stage 1.5 web ESLint 0 errors on changed files, Stage 2.3 web vitest 909/909 unchanged, Stage 2.4 vitest coverage passes at new floor. Stages 2.1/2.2/4/5 N/A (config + test only). **Test counts unchanged at 6,821 assertions across 5 layers** (API unit 3,728 / E2E 1,186 / Web vitest 909 / Live SCIM 984 / PowerShell 14). Source of the recommendation: [docs/strategy/SELF_AUDIT_2026-05-16.md](docs/strategy/SELF_AUDIT_2026-05-16.md) Section F.1. **No version bump** - pure threshold tighten. **No deploy** - frontend gate-only change with zero runtime effect. **Prod promotion:** NOT triggered - dev still on v0.52.0-alpha.2; prod still on v0.48.0 (Phase J). |
| 2026-05-16 | feat **Phase N2 - First-Run Onboarding Wizard (v0.52.0-alpha.2):** Second Phase N sub-phase (2 of 6 in UX and Polish per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S5.8). Closes the first-run UX gap: pre-N2, a fresh deployment greeted operators with a token dialog and a blank dashboard - no guided path to "create your first endpoint, issue your first credential, see your first SCIM request work." **Architecture:** new OnboardingWizard mounted chrome-level in [AppShell](web/src/layout/AppShell.tsx) + new [useShowOnboarding hook](web/src/hooks/useOnboarding.ts) + [SettingsPage](web/src/pages/SettingsPage.tsx) OnboardingResetCard to re-open on demand. **Trigger contract:** wizard shows when (a) `scimserver.onboarding.completedAt` localStorage flag is absent AND (b) `useEndpoints().totalResults === 0`; force-open hatch via `scimserver.onboarding.forceOpen=1` for tests / demos / settings reset. Hook subscribes to custom `scimserver.onboarding.changed` event so dismiss / reset re-evaluate same-tab without `storage` event. **4-step flow:** Step 1 Welcome (Skip vs Get started) -> Step 2 Pick a preset (entra-id preselected; reuses Phase L1 CreateEndpointWizard 4-step template) -> Step 3 Issue first credential (Phase E1 plaintext-token UX, token shown ONCE) -> Step 4 Send a SCIM request from Workbench (Phase M1 prefill via urlencoded GET). Skip / X close / I-will-do-this-later / Send-it-now all dismiss with `markOnboardingComplete()` (writes ISO timestamp + dispatches change event + removes forceOpen flag). **TDD discipline:** RED state confirmed before implementation - all 14 OnboardingWizard tests + 2 SettingsPage tests written alongside the source. GREEN: 909/909 web vitest (was 893, +16 N2: 14 trigger+steps + 2 settings reset card). **2 mid-cycle tsc fixes:** Fluent UI v9 makeStyles GriffelStyle rejects `borderColor`-only overrides on parents declaring `border` shorthand; switched 2 selectors (`stepDotActive`, `presetCardSelected`) to use full `border` shorthand. Web tsc 96/96 baseline maintained (no new prod-file errors). **No new live SCIM section** - all consumed endpoints (POST /admin/endpoints, POST /admin/endpoints/:id/credentials, GET /scim/endpoints/:id/Users) exhaustively covered at 9z-AA, 9z-V, and core SCIM CRUD. **No new Playwright spec** - vitest coverage sufficient. **Bundle:** main entry 152.99 -> **160.48 KB gzipped** (+7.49 KB; wizard mounted chrome-level not lazy-loaded because operators on first-run need it without route change; 19.8 % headroom under 200 KB budget). Shared primitives 126.83 -> 125.66 KB gzipped (-1.17 KB; some hoisting). **All 24 size-limit budgets pass**. **Test counts at v0.52.0-alpha.2:** API unit 3,728 / E2E 1,186 / Web vitest 893 -> **909** (+16) / Live SCIM 984 unchanged / PowerShell 14 = **6,821 total assertions across 5 layers** (was 6,805 at v0.52.0-alpha.1 + Finding-B fix, +16). New comprehensive feature doc [docs/PHASE_N2_ONBOARDING_WIZARD.md](docs/PHASE_N2_ONBOARDING_WIZARD.md) (9 sections, 2 Mermaid diagrams - architecture flow + 4-step sequence, per-step contract table, bundle-impact table, risk register, DoD). **First feature commit to fully exercise the new 7-stage gate strategy** ([docs/MANDATORY_QUALITY_GATES_STRATEGY.md](docs/MANDATORY_QUALITY_GATES_STRATEGY.md)). Per-sub-phase quality gate next: deploy v0.52.0-alpha.2 to dev + 984+ live SCIM tests must pass on dev before Phase N3 starts. **Prod promotion:** NOT triggered - dev-only deploy per standing rule. |
| 2026-05-15 | feat **Phase N1 - Notifications Inbox (v0.52.0-alpha.1):** Opens Phase N (UX & Polish, 6 sub-phases). Per analysis-doc S5.5. Pre-N1, SCIM mutation events fired via SSE but had no human-visible surface beyond cache invalidation - operators missed critical signals (endpoint deleted, credential revoked, bulk error on 50 rows) unless they happened to be on the dashboard. **Architecture:** new Bell icon in AppHeader (NotificationsButton.tsx) with unread-count badge (caps at 99+ so a 200-event bulk burst does not overflow the icon) + right-side OverlayDrawer (NotificationsDrawer.tsx) mirroring the K4 LogStreamDrawer pattern with severity-coded entry list + Mark-all-read + Clear + Take-me-there link to /endpoints/<id>/activity. **Store:** new notifications-store.ts ~195 LoC, Zustand global separate from ui-store so notification entries do not pollute the UI-chrome surface. Hand-rolled localStorage persistence (key scimserver.notifications.v1) with corrupt-storage tolerance + 7-day TTL pruning on drawer mount + 50-entry ring buffer (oldest dropped). **Dedupe contract** - dedupes by entry id. The SSE bridge generates ids deterministically from bucketKey(type, endpointId, second-bucket) so 100 events with the same (type, endpointId, second) collapse to one entry naturally. Explicit callers pass distinct ids and always land. **SSE bridge in useSSE** - every supported SCIM event the hook already invalidates on now ALSO produces a notification entry with severity classified per type pattern (*.error/*.failed -> error; scim.endpoint.updated/*.deleted/scim.credential.revoked -> warning; routine CRUD -> info), human-readable title from EVENT_TITLE map, and endpointId carried through. Non-SCIM messages (keepalive) ignored. **TDD discipline:** RED state confirmed BEFORE implementation in 5 separate cycles - 13 store tests / 2 ui-store tests / 4 SSE bridge tests / 6 button tests / 8 drawer tests all failed on missing modules. GREEN: 33/33 N1 tests pass; full web vitest 893/893 (was 860, +33). **2 mid-cycle fixes:** (1) initial dedupe collapsed entries with same (type+endpointId+second) regardless of id; broke the newest-first test (both appendNotification calls in same JS tick produce identical bucket keys). Switched to id-based dedupe with bucketKey as the documented id-generation helper for the SSE bridge - explicit ids always land; bridge-generated bucket-key ids collapse bursts. (2) initial drawer test asserted final-URL navigate path but TanStack Router uses param interpolation (to: route-pattern + params object); aligned assertion. **+33 web vitest** across 5 files. **No new live SCIM section** - the SSE surface itself is exhaustively locked at sections 9z-H + 9z-I + 9z-V, and the N1 client surface is pure UI. **Bundle:** main entry 150.55 -> 152.99 KB gzipped (+2.44 KB; Notifications surface lives in the entry chunk via AppShell mount). Shared primitives 126.80 -> 126.83 KB gzipped (+0.03 KB; negligible). **All 24 size-limit budgets pass**. **Test counts at v0.52.0-alpha.1:** API unit 3,724 / E2E 1,186 / Web vitest 860 -> **893** (+33) / Live SCIM 984 unchanged / PowerShell 14 = **6,801 total assertions across 5 layers** (was 6,768 at v0.51.0, +33). New comprehensive feature doc docs/PHASE_N1_NOTIFICATIONS_INBOX.md. Versions bumped lockstep 0.51.0 -> 0.52.0-alpha.1 (api+web+lockfiles regenerated in node:25-alpine). Minor bump per the established phase pattern. **Per-sub-phase quality gate next: deploy v0.52.0-alpha.1 to dev + 984+ live SCIM tests must all pass on dev before Phase N2 starts (Onboarding wizard).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-15 | release **v0.51.0 stable - Phase M (The Workbench) - COMPLETE (3 of 3 sub-phases):** Drops the `-alpha.N` suffix after every Phase M sub-phase shipped, deployed to dev, and passed its 970+ -> 984 live SCIM gate. Pure version cut + lockfile sync; no new features beyond the 3 already-released alphas. **Sub-phases shipped:** M1 alpha.1 SCIM Workbench (the killer feature: free-form HTTP request builder + history ring buffer + 3 RFC pure modules + L5 'Open in Workbench' wired; +65 web vitest -> 796, +5 live in 9z-AG), M2 alpha.2 Bulk Operations UI + Save-as-live-test killer (CSV upload + mapping + per-op result + failure CSV download; the M1-deferred Save-as-live-test emitter that turns Workbench history into paste-ready PowerShell snippets matching the 9z-* idiom; +52 -> 848, +6 live in 9z-AH), M3 alpha.3 Custom Resource Type UI (per-endpoint Resource Types tab gated by CustomResourceTypesEnabled flag with Create dialog + type-name-to-confirm Delete; reuses L1 useUpdateEndpointConfig - no new HTTP hook since v0.28.0 removed the dedicated admin RT API; +12 -> 860, +6 live in 9z-AI). **What Phase M closes:** SCIMServer is now the ONLY SCIM tool with (1) a built-in API workbench that round-trips into the regression test suite via the killer Save-as-live-test emitter, (2) a bulk operations UI with CSV upload + per-op failure CSV download for retry triage, (3) a custom resource type registration UI gated by the same flag the backend honors. EndpointDetail tab list grew from 8 to 10 entries (added Bulk + Resource types). Sidebar nav from 8 to 9 entries (added Workbench). **Cumulative test counts at v0.51.0 stable:** API unit 3,724 unchanged / E2E 1,186 unchanged / Web vitest 731 -> **860** (+129 across M1-M3) / Live SCIM 965 -> **984** (+19 across M1-M3 with 3 new sections 9z-AG -> 9z-AI; live gate also picked up implicit assertions in pre-existing sections) / PowerShell contract 14 unchanged = **6,768 total assertions across 5 layers** (was 6,620 at v0.50.0, +148). All 24 size-limit budgets green at v0.51.0: main entry 150.55 KB gzipped (25 % under K1 200 KB budget), shared primitives 126.80 KB gzipped (42 % under 220 KB budget; net -74.72 KB across M1-M3 as vite re-chunked many Fluent surfaces into per-route chunks where they're actually consumed), 24 per-route chunks all under 11 KB gzipped (10 % of 110 KB per-route ceiling). Versions bumped lockstep `0.51.0-alpha.3` -> `0.51.0` (api+web+lockfiles regenerated in node:25-alpine). **Per-phase final quality gate next: deploy v0.51.0 to dev + 984+ live SCIM tests must all pass on dev. After that gate passes, Phase N opens (UX & Polish, ~17 days: N1 Notifications inbox + N2 Onboarding wizard + N3 Export everywhere + N4 Settings persistence + N5 Frontend telemetry + N6 Keyboard ergonomics).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule (prod promotion is NEVER automatic; requires explicit `promote-to-prod.ps1` or `deployAndPromote` prompt invocation). |
| 2026-05-15 | feat **Phase M3 - Custom Resource Type UI (v0.51.0-alpha.3) - PHASE M COMPLETE (3 of 3 sub-phases):** Third and final sub-phase in Phase M (The Workbench). Per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.4. Wires the already-shipped custom-resource-type machinery (G8b, v0.18.0) into a friendly UX. Pre-M3 the feature was invisible from the UI; customers paying for "extensible SCIM" could not extend it without curl. **Architecture:** new per-endpoint Resource Types tab at `/endpoints/$id/resource-types` ([ResourceTypesTab.tsx](web/src/pages/ResourceTypesTab.tsx), 10th nested tab between Bulk and Schemas, `CubeTree24Regular` icon). Tab gates on `CustomResourceTypesEnabled` flag - shows feature-disabled MessageBar pointing at Settings when off (the API also gates the wildcard SCIM controller route on the same flag, so the gating is consistent end-to-end). Lists existing custom RTs (filters out built-in User and Group via `isCustomRt()` predicate). Each row shows name + endpoint + schema URN + Delete icon button. Create dialog: name + endpoint + schema URN + optional description fields with inline validation (`<Field validationState='error' validationMessage={...}>`) rejecting reserved names (`User`, `Group`), reserved endpoints (`/Users`, `/Groups`, `/Schemas`, `/ResourceTypes`, `/ServiceProviderConfig`, `/Bulk`, `/Me`), regex violations (name must match `^[A-Za-z][A-Za-z0-9]*$`, endpoint must match `^/[A-Za-z][A-Za-z0-9]*$`), and per-endpoint duplicates. Schema URN must be a `urn:` prefix. Submit disabled until all 3 required fields validate. Delete confirm: type-name-to-confirm modal (mirrors L1 DeleteEndpointDialog pattern); Submit disabled until exact name match. **No new HTTP hook** - critical pivot from the analysis-doc spec: as of v0.28.0 the dedicated admin RT API (`POST /admin/endpoints/:id/resource-types`) was REMOVED; custom RTs now live in `endpoint.profile.resourceTypes[]` and are added/removed via PATCH `/admin/endpoints/:id` with the merged profile. M3 reuses the L1 `useUpdateEndpointConfig` mutation hook (the same hook that powers SettingsTab toggle flips). Two benefits: (1) single optimistic-update path - the L1 deep-merge logic handles RT operations for free, including rollback on error, (2) no new live test of the hook surface - the existing 9z-Z (config flag toggles) locks the same PATCH. The Create handler builds the merged `profile.resourceTypes[]` (existing + new) AND `profile.schemas[]` (existing + new entry for the schema URN if not already present); the Delete handler builds the filtered `profile.resourceTypes[]` (omitting the target). **TDD discipline:** RED state confirmed BEFORE implementation - 9/9 page tests failed on missing module. GREEN: 9/9 M3 tests pass; full web vitest 860/860 (was 848, +12 net including 3 wiring contract). **Mid-cycle fix:** initial test asserted `getByTestId('resource-types-create-submit')` but FormDialog suffixes the submit button with `-submit` based on the dialog's own testid - so a dialog `data-testid='resource-types-create-dialog'` produces submit button `resource-types-create-dialog-submit`. Fixed test assertions to match the FormDialog convention. **+12 web vitest** across 4 files: new [ResourceTypesTab.test.tsx](web/src/pages/ResourceTypesTab.test.tsx) +9 (flag gating shows disabled panel + Create button visibility + EmptyState when no custom RTs + lists custom RTs filtering out User/Group + Create dialog renders 4 fields + Submit PATCHes merged profile.resourceTypes + profile.schemas + reserved-name inline error + reserved-endpoint inline error + Delete confirm with type-name-to-confirm pattern fires filtered PATCH); plus +3 wiring contract churn (lazy-routes test entry + size-limit-config test entry + EndpointDetailPage tab branch). **+6 live SCIM** in new section 9z-AI before TEST SECTION 10: setup creates rfc-standard endpoint + PATCHes CustomResourceTypesEnabled=true, PATCH with merged profile.resourceTypes + profile.schemas registers Device RT, GET /Schemas reflects the new schema URN, GET /ResourceTypes reflects the new Device RT, GET /Devices wildcard endpoint returns ListResponse (CustomResourceTypesEnabled gate), PATCH with filtered resourceTypes[] removes Device (User + Group remain); cleanup removes the endpoint. **Bundle:** main entry 150.47 -> **150.55 KB gzipped** (+0.08 KB; minimal change since the page reuses existing FormDialog primitive). Shared primitives unchanged at **126.80 KB gzipped**. New ResourceTypesTab chunk **2.93 KB gzipped** (97 % under 110 KB ceiling). **All 24 size-limit budgets pass** (was 23; +1 for ResourceTypesTab). **Test counts at v0.51.0-alpha.3:** API unit 3,724 unchanged / E2E 1,186 unchanged / Web vitest 848 -> **860** (+12) / Live SCIM 976 -> **982** (+6) / PowerShell contract 14 = **6,766 total assertions across 5 layers** (was 6,748 at M2, +18). New comprehensive feature doc [docs/PHASE_M3_CUSTOM_RESOURCE_TYPES.md](docs/PHASE_M3_CUSTOM_RESOURCE_TYPES.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.4 marked CLOSED. Versions bumped lockstep `0.51.0-alpha.2` -> `0.51.0-alpha.3` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.51.0-alpha.3 to dev + 982+ live SCIM tests must all pass on dev. After that gate passes, the v0.51.0 stable rollup drops the -alpha.N suffix and closes Phase M (The Workbench, 3 of 3 sub-phases shipped).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule. |
| 2026-05-15 | feat **Phase M2 - Bulk Operations UI (v0.51.0-alpha.2):** Second of 3 sub-phases in Phase M (The Workbench). Per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.3. Wires the already-shipped `POST /scim/endpoints/:id/Bulk` surface (RFC 7644 §3.7, 1000-op cap, 1 MB payload limit) into a friendly UX. Pre-M2 the only consumer was scripts/live-test.ps1; an operator who wanted to bulk-create users (e.g. onboarding 200 employees from HRIS export) had to hand-craft the 200-op SCIM `BulkRequest` JSON envelope and curl it. **Architecture:** new per-endpoint Bulk tab at `/endpoints/$id/bulk` ([BulkTab.tsx](web/src/pages/BulkTab.tsx), 9th nested tab between Activity and Schemas, `Stack24Regular` icon). Mode picker (POST/PATCH/DELETE) + Resource picker (Users/Groups) + CSV file input (drop-zone style; reads file via FileReader, no upload until Submit) + Mapping panel (one row per parsed CSV header, each mapping to a free-text SCIM target attribute pre-populated with the column name; operator can override with extension-style URN keys) + Preview pane (assembled BulkRequest envelope, first 10 ops + total count) + failOnErrors numeric input. Result viewer: success/failure totals + color-coded per-op badges + Download failures CSV button (failure rows exported with `bulkId / method / status / scimType / detail` columns for retry triage). **3 new pure modules** (~385 LoC): [csv-parse.ts](web/src/utils/csv-parse.ts) ~140 LoC (RFC 4180 reverse of M1 csv-export; state-machine reader handling quoted vs unquoted, doubled-quote escape, embedded newlines, CRLF + LF line terminators, malformed-quote returns error string + null rows; round-trips perfectly with the M1 csv-export so `parseCsv(toCsv(rows))` is the identity), [bulk-builder.ts](web/src/utils/bulk-builder.ts) ~155 LoC (RFC 7644 §3.7 BulkRequest assembler with POST mode auto-assigning `bulkId='row-N'` + data carrying mapped attribute key/values + resource core schema URN; PATCH mode injecting id-into-path + data as SCIM PatchOp envelope of replace ops; DELETE mode id-into-path with no data; empty-string values omitted not serialized; extension-style dotted/URN target keys preserved verbatim; throws when row count exceeds `BULK_MAX_OPERATIONS=1000` mirroring API constant; rows missing the id column on PATCH/DELETE are skipped defensively), [live-test-snippet.ts](web/src/utils/live-test-snippet.ts) ~90 LoC (**THE killer differentiator** per analysis-doc S4.2 - M1 deferred this; M2 ships it). **"Save as live-test" button** column added to every Workbench history row in [WorkbenchPage.tsx](web/src/pages/WorkbenchPage.tsx). Click it, the canonical PowerShell snippet (matching the existing 9z-* section idiom: `# 9z-XX.N: <label>` banner + `Invoke-WebRequest -Uri "$baseUrl<path>" -Method <X> -Headers $headers` + body via `ConvertFrom-Json | ConvertTo-Json -Depth 10` round-trip + `Test-Result -Success ([int]$response.StatusCode -eq <expected>)`) lands in the clipboard. Embedded single-quotes in JSON are escaped per PowerShell here-string semantics. The operator pastes into a new section in scripts/live-test.ps1 and the exploration becomes a regression test in two clicks. **No other admin UI does this** - the analysis-doc S4.2 claim now ships in production. **`useScimBulk(endpointId)` hook** in [queries.ts](web/src/api/queries.ts) - same no-throw-on-non-2xx semantics as M1 `useScimRequest` (the BulkPage NEEDS to surface partial failures - e.g. 2 of 100 rows rejected with their per-op error.scimType - in the result viewer + downloadable failure-rows CSV); 401 still triggers `clearStoredToken`. Returns `{ status, durationMs, requestId, body }`. **TDD discipline:** RED state confirmed BEFORE implementation in 5 separate cycles - 14 csv-parse / 16 bulk-builder / 11 snippet / 2 hook / 9 page tests all failed on missing modules. GREEN: 52/52 M2 tests pass; full web vitest 848/848 (was 796, +52). **Mid-cycle fixes:** (1) initial 2 snippet tests asserted `Invoke-RestMethod` + `ConvertTo-Json` but the implementation correctly uses `Invoke-WebRequest` (so we can assert StatusCode) and `JSON.stringify` already produces JSON text (no inline `ConvertTo-Json` needed); fixed assertions to match the deliberate idiom. Switched the body emit pattern to the canonical `'<json>' | ConvertFrom-Json | ConvertTo-Json -Depth 10` round-trip which matches scripts/live-test.ps1 line 1318+ style; both forms paste-runnable. (2) original implementation accidentally removed L1 `usePresets` JSDoc comment when adding `useScimBulk`; restored. **+52 web vitest** across 5 files: new [csv-parse.test.ts](web/src/utils/csv-parse.test.ts) +14 (round-trip with toCsv + quote escape + multi-line cells + CRLF + missing-cells default to empty string + extra-cells dropped + malformed-quote error returns null rows), new [bulk-builder.test.ts](web/src/utils/bulk-builder.test.ts) +16 (3 modes + bulkId auto-assignment + missing source column omitted + empty-string value omitted + idColumn injection + PATCH-without-id skipped + failOnErrors threshold included only when > 0 + 1000-op cap throws + dotted extension URN key preservation), new [live-test-snippet.test.ts](web/src/utils/live-test-snippet.test.ts) +11 (GET / POST / PATCH / DELETE method handling + banner format + section/step defaults + $baseUrl variable wrapping + Test-Result assertion + single-quote escape + ConvertTo-Json pipe + label echo + empty-body omission), extended [mutations.test.ts](web/src/api/mutations.test.ts) +2 (URL contract + 413 tooLarge as outcome not throw), new [BulkTab.test.tsx](web/src/pages/BulkTab.test.tsx) +9 (toolbar + CSV upload populates mapping + preview pane + Submit fires hook with assembled envelope + result viewer with totals + per-op rows + Download failures CSV with scimType+detail columns + failOnErrors forwards to envelope + malformed CSV disables Submit). Plus +1 in [WorkbenchPage.test.tsx](web/src/pages/WorkbenchPage.test.tsx) asserting the new "Save as live-test" button shape. **+6 live SCIM** in new section 9z-AH before TEST SECTION 10: setup creates rfc-standard endpoint with `BulkOperationsEnabled=true`, BulkResponse envelope schemas URN matches, Operations[] has 2 entries, every op carries bulkId+status (BulkPage table columns), success ops carry location header, duplicate userName op returns 409 with `response.scimType='uniqueness'` (the BulkPage failure-CSV reads this); cleanup removes the endpoint. **Bundle:** main entry 150.13 -> **150.47 KB gzipped** (+0.34 KB; minimal change since BulkTab uses native HTML inputs). Shared primitives 132.00 -> **126.80 KB gzipped** (-5.20 KB; vite re-chunked some Fluent components into a new `ScimErrorMessage-*.js` chunk consumed by both BulkTab and other error-handling surfaces). New BulkTab chunk **3.47 KB gzipped** (97 % under 110 KB ceiling). **All 23 size-limit budgets pass** (was 22; +1 for BulkTab). **Test counts at v0.51.0-alpha.2:** API unit 3,724 unchanged / E2E 1,186 unchanged / Web vitest 796 -> **848** (+52) / Live SCIM 970 -> **976** (+6) / PowerShell contract 14 = **6,748 total assertions across 5 layers** (was 6,690 at M1, +58). New comprehensive feature doc [docs/PHASE_M2_BULK_OPERATIONS.md](docs/PHASE_M2_BULK_OPERATIONS.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.3 marked CLOSED + the M1-deferred S4.2 Save-as-live-test emitter shipped. Versions bumped lockstep `0.51.0-alpha.1` -> `0.51.0-alpha.2` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.51.0-alpha.2 to dev + 976+ live SCIM tests must all pass on dev before Phase M3 starts (Custom Resource Type UI per analysis-doc S4.4).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule. |
| 2026-05-15 | feat **Phase M1 - SCIM Workbench (v0.51.0-alpha.1) - THE KILLER FEATURE:** Opens Phase M (The Workbench, ~18 days). Per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.2 "the highest-leverage move" - collapses 4 separate Tier-1 gaps (PATCH builder, filter builder, Bulk submitter, Discovery explorer) into one composable workbench. Pre-M1 the redesigned UI had no way for the operator to send an arbitrary SCIM request without curl; the L5 "Open in Workbench" button rendered as a disabled stub. **Architecture:** new top-level page at `/workbench` ([WorkbenchPage.tsx](web/src/pages/WorkbenchPage.tsx), 7th sidebar nav entry between Operations and Logs, `Beaker24Regular` icon). Top toolbar: Method picker (GET/POST/PUT/PATCH/DELETE) + Path input under `/scim/*` + Endpoint convenience picker (selecting one pre-fills the path with `/scim/endpoints/<id>/Users`) + Send button. Body editor (monospace textarea) visible only for POST/PUT/PATCH; surfaces JSON parse errors inline. Response viewer: status badge color-coded (green 2xx / yellow 3xx / red 4xx-5xx) + duration ms + X-Request-Id (cross-references `/admin/logs`) + pretty-printed body. History list: ring buffer of last 50 requests persisted to `localStorage` (key `scimserver.workbench.history.v1`), newest-first; clicking any row re-seeds the toolbar from it; Clear button empties storage. Action row: Copy as curl + Copy as TypeScript snippets (both copy plain-text to clipboard, build from active method/path/body). **3 new pure modules** ship the RFC algebra (visual UIs deferred to N6/M2; M1 ships the round-trip surface as a free-text body editor, sufficient for any SCIM exploration): [workbench-history.ts](web/src/utils/workbench-history.ts) ~95 LoC (localStorage ring buffer + corrupt-storage-safe + auto-recovery on bad write), [filter-builder.ts](web/src/utils/filter-builder.ts) ~135 LoC (RFC 7644 §3.4.2.2 emitter for the 10 standard operators eq/ne/co/sw/ew/pr/gt/ge/lt/le + AND/OR compound + parseSimpleFilter inverse for single-atom + value coercion: strings quoted with embedded `\"` escape, numbers + booleans unquoted, `pr` unary), [patch-builder.ts](web/src/utils/patch-builder.ts) ~115 LoC (RFC 7644 §3.5.2 PatchOp envelope assembly with canonical URN `urn:ietf:params:scim:api:messages:2.0:PatchOp` + per-op validation rejecting unknown op names / missing path on remove / missing value on add+replace / `add` at root requires object value + parseCurlPatchBody inverse). **`useScimRequest` mutation hook** in [queries.ts](web/src/api/queries.ts) - critically does NOT throw on 4xx/5xx (the operator NEEDS to see the response body in the viewer; throwing would route through K3 ScimErrorMessage chrome and bury the body). Returns `{ status, durationMs, requestId, body }` for ANY status. 401 still triggers `clearStoredToken` + `notifyTokenInvalid` so a stale bearer doesn't get stuck. Body parse fallback: non-JSON responses (HTML error pages, plain text) surface as raw text rather than swallowed. **L5 "Open in Workbench" stub WIRED** in [DiscoveryExplorerPage.tsx](web/src/pages/DiscoveryExplorerPage.tsx) - was disabled with "coming in Phase M1" tooltip; now navigates to `/workbench?prefill=<urlencoded-JSON>` packing the active sub-tab's surface URL as a GET request. WorkbenchPage `parsePrefill()` inverse-decodes on mount and seeds method/path/body. Other pages can wire their own "Open in Workbench" buttons (M2 Bulk UI will use the same surface) without changing the contract. **TDD discipline:** RED state confirmed BEFORE implementation in 5 separate cycles - 9 history tests / 18 filter tests / 18 patch tests / 4 hook tests / 13 page tests all failed on missing modules. GREEN: 62/62 M1 tests pass; full web vitest 796/796 (was 731, +65 net including 3 size-limit ratchet). **One mid-cycle fix:** initial Copy-as-TS test asserted `method: 'GET'` (single-quoted) but the snippet uses JSON.stringify so it produces `"method": "GET"` (double-quoted JSON style); both forms paste-runnable in TypeScript, fixed assertion to match what the implementation produces. **L5 test re-asserted:** the previously-disabled-stub test was rewritten to mock `useNavigate` and assert the new wired behavior (button enables once endpoint picked + navigates to `/workbench` with correct prefill JSON for SPC sub-tab); existing 11 L5 tests all pass. **+65 web vitest** across 5 files: new [workbench-history.test.ts](web/src/utils/workbench-history.test.ts) +9 (round-trip + ordering + cap + clear + corrupt-storage recovery + non-array JSON), new [filter-builder.test.ts](web/src/utils/filter-builder.test.ts) +18 (all 10 RFC operators + value coercion + quote escape + AND/OR + parser inverse for every operator + null/whitespace handling), new [patch-builder.test.ts](web/src/utils/patch-builder.test.ts) +18 (3 op names + envelope shape + per-op validation + parseCurlPatchBody round-trip), extended [mutations.test.ts](web/src/api/mutations.test.ts) +4 (URL contract + body forwarding + 4xx-as-outcome-not-throw + 204 graceful), new [WorkbenchPage.test.tsx](web/src/pages/WorkbenchPage.test.tsx) +13 (toolbar + body editor visibility + Send gate + Send fires hook + response viewer with status+duration+body + history append + Copy as curl/TS + endpoint picker convenience + URL `?prefill` deep-link). Plus 3 size-limit ratchet (WorkbenchPage added to ROUTE_CHUNK_NAMES + size-limit-config-test entry + lazy-routes-test entry). **+5 live SCIM** in new section 9z-AG before TEST SECTION 10: setup creates rfc-standard endpoint, GET /ServiceProviderConfig returns 200 + X-Request-Id (the L5 deep-link target), POST /Users round-trips, PATCH /Users PatchOp envelope round-trips, DELETE /Users returns 204; cleanup removes the endpoint. **Bundle:** main entry 149.86 -> **150.13 KB gzipped** (+0.27 KB; the workbench textarea + select are native HTML, not Fluent components, so very minimal entry-chunk impact). Shared primitives 201.52 -> **132.00 KB gzipped** (-69.52 KB; vite re-chunked when the WorkbenchPage's small surface area changed the dependency graph - several Fluent components moved out of the shared chunk into their per-route chunks where they're actually consumed; net total bundle ~70 KB gzipped smaller). New WorkbenchPage chunk **7.03 KB gzipped** (94 % under 110 KB ceiling). **All 22 size-limit budgets pass** (was 21; +1 for WorkbenchPage). **Test counts at v0.51.0-alpha.1:** API unit 3,724 unchanged / E2E 1,186 unchanged / Web vitest 731 -> **796** (+65) / Live SCIM 965 -> **970** (+5) / PowerShell contract 14 = **6,690 total assertions across 5 layers** (was 6,620 at L stable, +70). New comprehensive feature doc [docs/PHASE_M1_SCIM_WORKBENCH.md](docs/PHASE_M1_SCIM_WORKBENCH.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.2 marked CLOSED. Versions bumped lockstep `0.50.0` -> `0.51.0-alpha.1` (api+web+lockfiles regenerated in node:25-alpine). Minor bump per established Phase-K and Phase-L pattern (each new phase opens with `0.NEXT.0-alpha.1`); Phase M will roll up to `0.51.0` stable after M1+M2+M3 ship and pass their dev gates. **Per-sub-phase quality gate next: deploy v0.51.0-alpha.1 to dev + 970+ live SCIM tests must all pass on dev before Phase M2 starts (Bulk Operations UI per analysis-doc S4.3).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule. |
| 2026-05-14 | release **v0.50.0 stable - Phase L Capability Completeness - COMPLETE (6 of 6 sub-phases):** Drops the `-alpha.N` suffix after every Phase L sub-phase shipped, deployed to dev, and passed its 939+ -> 965+ live SCIM gate. Pure version cut + lockfile sync; no new features beyond the 6 already-released alphas. **Sub-phases shipped:** L1 alpha.1 Endpoint CRUD UI + Preset Picker (+30 web vitest -> 620, +6 live -> 939 in section 9z-AA), L2 alpha.2 /Me Self-Service UI (+17 -> 637, +9 live in 9z-AB), L3 alpha.3 Activity Analytics Dashboard (+7 -> 644, +4 live in 9z-AC), L4 alpha.4 Log Config Admin UI (+11 -> 655, +3 live in 9z-AD), L5 alpha.5 Discovery Explorer + Two-Endpoint Diff (+42 -> 697, +5 live in 9z-AE - the killer feature with diff reducer mirroring API tighten-only-validator algebra), L6 alpha.6 Operations / Cross-Endpoint Operator View (+34 -> 731, +5 live in 9z-AF - restored the legacy "Database Browser" tab as a top-level /operations route with 3 sub-tabs + per-row endpoint Badge deep-link + CSV export). **What Phase L closes:** an operator can now do EVERYTHING with a mouse that previously required curl + shell access. Sidebar nav grew from 6 entries (Dashboard / Endpoints / Manual Provision / Logs / Settings / pre-L) to 8 entries (Dashboard / Endpoints / Manual Provision / **My profile** L2 / **Discovery** L5 / **Operations** L6 / Logs / Settings). **Cumulative test counts at v0.50.0 stable:** API unit 3,720 -> **3,724** (+4 L6 endpointId projection), API E2E 1,184 -> **1,186** (+2 L1 admin-endpoints-create spec), Web vitest 590 -> **731** (+141 across L1-L6), Live SCIM 933 -> **965** (+32 across 6 new sections 9z-AA -> 9z-AF), PowerShell contract 14 unchanged = **6,620 total assertions across 5 layers** (was 6,441 at v0.49.0, +179). All 21 size-limit budgets green at v0.50.0: main entry 149.86 KB gzipped (25 % under K1 200 KB budget), shared primitives 201.52 KB gzipped (8 % under 220 KB budget), 20 per-route chunks all under 11 KB gzipped (10 % of 110 KB per-route ceiling). Versions bumped lockstep `0.50.0-alpha.6` -> `0.50.0` (api+web+lockfiles regenerated in node:25-alpine). **Per-phase final quality gate next: deploy v0.50.0 to dev + 965+ live SCIM tests must all pass on dev. After that gate passes, Phase M opens (The Workbench, ~18 days: M1 SCIM Workbench filter+PATCH+history, M2 Bulk Operations UI, M3 Custom Resource Type UI).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule (prod promotion is NEVER automatic; requires explicit `promote-to-prod.ps1` or `deployAndPromote` prompt invocation). |
| 2026-05-14 | feat **Phase L6 - Operations / Cross-Endpoint Operator View (v0.50.0-alpha.6):** Sixth and FINAL Phase L sub-phase (6 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.9). **Closes Phase L Capability Completeness.** Wires the already-shipped `database.controller.ts` cross-endpoint surface (shipped pre-v0.18.0, 5 routes, paginated, searchable, in-memory + prisma branches) that the redesigned UI never restored from the legacy "Database Browser" tab. **Architecture:** new top-level page at `/operations` ([OperationsPage.tsx](web/src/pages/OperationsPage.tsx), 6th sidebar nav entry between Discovery and Logs) with 3 sub-tabs (All Users | All Groups | Statistics). The 2 list tabs render paginated tables (50/page Prev/Next) with SearchBox + (users-only) Active-Only Switch + per-row endpoint Badge that deep-links via `useNavigate()` to that endpoint`s Users/Groups tab. Statistics tab renders 4 KPI tiles (Users total/active + Groups total + Requests 24h) + database type/backend caption. **CSV export on every tab:** new pure RFC 4180 serializer in [web/src/utils/csv-export.ts](web/src/utils/csv-export.ts) (~110 LoC, 2 exports: `toCsv(rows, options?)` with column-pinning + filter + CRLF + double-quote-escape + JSON.stringify fallback for nested values + `triggerCsvDownload(filename, csv)` with Blob + synthetic anchor + Object URL revocation). **No new dependency added** - csv-stringify is node-only, papaparse is 45 KB gz; the SCIM admin row sets are small enough that a hand-rolled tiny implementation is the right cut. Per-tab CSV column allowlist locks the export shape: `USER_CSV_COLUMNS=['id','userName','externalId','active','endpointId','createdAt','updatedAt']`, `GROUP_CSV_COLUMNS=['id','displayName','memberCount','endpointId','createdAt','updatedAt']`. **Backend additive change:** `database.service.ts` `getUsers` and `getGroups` prisma `select` blocks now include `endpointId: true`; in-memory branches map endpointId per row at the spread point. No schema change (the column was indexed since Phase 17); fully backward-compatible with existing `toHaveProperty` assertions in admin-api-coverage e2e spec. **Hooks (Phase L6):** `useDatabaseUsers(params)` + `useDatabaseGroups(params)` + `useDatabaseStatistics()` thin useQuery wrappers + 5 new types + `databaseUsersQueryOptions(params)` / `databaseGroupsQueryOptions(params)` / `databaseStatisticsQueryOptions()` helpers + `queryKeys.operations.{users(params), groups(params), statistics}` factory. 30s staleTime - operator-grade aggregation, not SCIM-canonical, so short staleTime keeps freshness without hammering the cross-endpoint query. `buildDatabaseUsersUrl()` / `buildDatabaseGroupsUrl()` helpers use URLSearchParams so optional filters are OMITTED (not empty-string-serialized) when not set; test asserts `active=false` lands in the URL only when explicitly set. **EndpointBadgeLink (new):** plain anchor + useNavigate onClick instead of TanStack Router `<Link>` - the latter throws synchronously when the target route isn`t registered in the in-memory test router, swallowing all 9 page tests. The custom component renders cleanly in isolation AND still drives SPA navigation at runtime; honours modifier keys + middle/right click for browser-default behaviour. **TDD discipline:** RED state confirmed BEFORE implementation in 4 separate cycles - 15/15 csv-export tests failed on missing module, 7/7 hook tests failed on missing useDatabaseUsers + queryKeys.operations, 2/2 backend select tests failed on missing endpointId projection, 9/9 page tests failed on missing module. GREEN: 33/33 L6 tests pass; full web vitest 731/731 (was 697, +34 net including 3 size-limit ratchet); full API unit 3,724/3,724 (was 3,720, +4 endpointId projection lock). **Mid-cycle fixes:** (1) initial OperationsPage tests rendered with FluentProvider+QueryClient but TanStack `<Link>` swallowed render (body=empty); diagnosed via `screen.debug()` showing body=<div/> with no content, switched to EndpointBadgeLink + plain anchor pattern + mock `useNavigate` in test. (2) initial test mock removed entire `@tanstack/react-router` module which broke `vi.importActual` chain; switched to selective override pattern. **+34 web vitest** across 4 files: new [csv-export.test.ts](web/src/utils/csv-export.test.ts) +15 (empty input + column inference + row-order + boolean+number+null+undefined + comma-quote + double-quote-escape + newline-quote + nested JSON.stringify + CRLF + column override + Blob MIME type + anchor download attribute + Object URL revocation + DOM cleanup), extended [mutations.test.ts](web/src/api/mutations.test.ts) +7 (queryKeys.operations stable factories + 3-hook URL contracts + active=false edge case), new [OperationsPage.test.tsx](web/src/pages/OperationsPage.test.tsx) +9 (3 sub-tabs + scope picker badge + Statistics tiles + search resets page + empty state + 2 CSV download triggers + Link href contract), plus 3 size-limit ratchet (OperationsPage added to ROUTE_CHUNK_NAMES). **+4 API unit** in [database.service.spec.ts](api/src/modules/database/database.service.spec.ts) under "Phase L6 - cross-endpoint endpointId projection" (2 select assertions + 2 response assertions for users + groups). **+5 live SCIM** in new section 9z-AF before TEST SECTION 10: users response envelope + every user row has endpointId key (PSObject.Properties.Match defensive presence check per Schema-Characteristic Test Rule) + groups response envelope + every group row has endpointId key + statistics envelope (users.total/active + groups.total + activity.last24Hours + database.{type,persistenceBackend}). Backend behavior unchanged - admin-api-coverage.e2e-spec retains the full backend coverage. **Bundle:** main entry 149.33 -> **149.86 KB gzipped** (+0.53 KB; SearchBox + Switch added; TabList already in entry chunk from L5; 25.07 % under 200 KB K1 budget); shared primitives 201.17 -> **201.52 KB gzipped** (+0.35 KB); new OperationsPage chunk **3.71 KB gzipped** (96 % under 110 KB ceiling). **All 21 size-limit budgets pass** (was 20; +1 for OperationsPage). **Test counts at v0.50.0-alpha.6:** API unit 3,720 -> **3,724** (+4) / E2E 1,186 unchanged / Web vitest 697 -> **731** (+34) / Live SCIM 960 -> **965** (+5) / PowerShell contract 14 = **6,620 total assertions across 5 layers** (was 6,577 at L5, +43). New comprehensive feature doc [docs/PHASE_L6_OPERATIONS_VIEW.md](docs/PHASE_L6_OPERATIONS_VIEW.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.9 marked CLOSED + Phase L (6 of 6) complete. Versions bumped lockstep `0.50.0-alpha.5` -> `0.50.0-alpha.6` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.6 to dev + 965+ live SCIM tests must all pass on dev. After that gate passes, the v0.50.0 stable rollup drops the -alpha.N suffix and closes Phase L Capability Completeness.** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase L5 - Discovery Explorer + Two-Endpoint Diff (v0.50.0-alpha.5):** Fifth Phase L sub-phase (5 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.8 - the single biggest remaining differentiator on the L roadmap). Wires the already-shipped per-endpoint Discovery surfaces (`GET /scim/endpoints/:id/{Schemas,ResourceTypes,ServiceProviderConfig}`, exhaustively locked at live layer 8 + 9z-Q.11 + per-section probes throughout) into a brand new top-level `/discovery` route. **Architecture:** new top-level page at `/discovery` ([DiscoveryExplorerPage.tsx](web/src/pages/DiscoveryExplorerPage.tsx), 5th sidebar nav entry between My profile and Logs) with 3 sub-tabs (ServiceProviderConfig | ResourceTypes | Schemas) + scope picker exposing primary slot + optional secondary "Compare with another" slot. When two endpoints picked, the Schemas tab swaps to a side-by-side diff view that colors each attribute characteristic green (tighten), red (relax), grey (unchanged), or orange (incomparable - structural change). Cell colors are driven by a `data-status` attribute that mirrors the API's [tighten-only-validator algebra](api/src/modules/scim/endpoint-profile/tighten-only-validator.ts) - the same `MUTABILITY_RANK` and `UNIQUENESS_RANK` ordinal rankings the backend already uses to reject relaxing profile overrides, plus a coarse 2-rank `RETURNED_RANK` that captures only the never-vs-visible split (RFC 7643 §7's only enforced direction). **No commercial SCIM tool ships this** - the diff view turns abstract "endpoint A is tighter than endpoint B" into a single visual scan. **Pure diff reducer** in new [web/src/utils/discovery-diff.ts](web/src/utils/discovery-diff.ts) (~280 LoC, exposed as `classifyCharacteristic(key, a, b) -> DiffStatus` and `compareSchemas(a, b) -> SchemaDiffResult`). Reducer substitutes the RFC 7643 §2.2 default (`required:false`, `caseExact:false`, `mutability:'readWrite'`, `returned:'default'`, `uniqueness:'none'`, `multiValued:false`, `type:'string'`) BEFORE classifying, per the project's Schema-Characteristic Test Rule - so a published `uniqueness:server` against an absent value on the other side correctly classifies as `tighten` (not `only-a`/`only-b`). Walker emits one row per attribute name across the union of A and B; sub-attribute drill-down deferred to L6/M. **Hooks (Phase L5):** `useEndpointResourceTypes(endpointId)` + `useEndpointServiceProviderConfig(endpointId)` thin useQuery wrappers + `endpointResourceTypesQueryOptions(id)` + `endpointServiceProviderConfigQueryOptions(id)` helpers + `queryKeys.discovery.{resourceTypes(id), serviceProviderConfig(id)}` factory entries. Both cached 5min (Discovery rarely changes after endpoint configuration; mirrors `endpointSchemasQueryOptions` for cache-tier consistency across the three sub-tabs). Disabled when endpointId is empty so the picker can mount before the operator picks one. The third hook (`useEndpointSchemas`) was already shipped in Phase D3 and is reused as-is. **Action toolbar:** Copy as JSON (clipboard write of the active surface's JSON-stringified payload), Copy as URN (clipboard write of the first schema URN on the active tab), Open in Workbench (disabled stub with Tooltip "coming in Phase M1" - wired in Phase M1 when the workbench page lands), Refresh (manual refetch escape hatch since 5min staleTime can feel long if the operator just edited a profile in the API). **TDD discipline:** RED state confirmed BEFORE implementation in 3 separate cycles - 22/22 reducer tests failed on missing module + classifyCharacteristic/compareSchemas/RFC_DEFAULTS exports, 6/6 hook tests failed on missing `useEndpointResourceTypes`/`useEndpointServiceProviderConfig`/`queryKeys.discovery`, 11/11 page tests failed on missing module. GREEN: 39/39 L5 tests pass; full vitest 697/697 (was 655, +42 net including +3 size-limit ratchet for new chunk name in test). **+42 web vitest tests** across 4 files: new [discovery-diff.test.ts](web/src/utils/discovery-diff.test.ts) +22 (equality on each char + tighten direction for 5 chars + relax direction for 5 chars + RFC default substitution + incomparable structural change + cross-attribute walker + only-a/only-b semantics + summary tally counts), extended [mutations.test.ts](web/src/api/mutations.test.ts) +6 (queryKeys.discovery stable per-endpoint keys + GET URL contract + disabled-when-empty for both hooks), new [DiscoveryExplorerPage.test.tsx](web/src/pages/DiscoveryExplorerPage.test.tsx) +11 (3 sub-tabs render + scope picker with one card per endpoint + empty-state when no endpoints + secondary picker toggle + single-endpoint mode for each of 3 surfaces + two-endpoint diff render + cell `data-status` mirrors reducer (tighten/unchanged) + Copy as JSON wires clipboard with parsed JSON containing patch.supported + Open in Workbench disabled). Plus 3 size-limit ratchet (DiscoveryExplorerPage added to ROUTE_CHUNK_NAMES so the per-route 110 KB cap is locked). **+5 live SCIM** in new section 9z-AE before TEST SECTION 10 per live-test conventions: SPC has 6 capability flag blocks (patch+filter+etag+bulk+changePassword+sort), every block has `supported` boolean, ResourceTypes returns >=2 with id+name+endpoint+schema, User.userName mutability/uniqueness/returned keywords belong to RFC valid sets (Schema-Characteristic Test Rule live-layer twin using PSObject.Properties.Match defensive presence check). Backend behavior unchanged - 8 + 9z-Q.11 retain the full backend coverage. **Bundle:** main entry 147.50 -> **149.33 KB gzipped** (+1.83 KB for TabList + Tooltip + Search24Regular icon hoisted into entry chunk for the sub-tab UI; still 25.3 % under 200 KB K1 budget); shared primitives 199.25 -> **201.17 KB gzipped** (+1.92 KB; same hoisting); new DiscoveryExplorerPage chunk **4.35 KB gzipped** (96 % under 110 KB ceiling). **All 20 size-limit budgets pass** (was 19; +1 for DiscoveryExplorerPage). **Test counts at v0.50.0-alpha.5:** API unit 3,720 / E2E 1,186 / Web vitest 655 -> **697** (+42) / Live SCIM 955 -> **960** (+5) / PowerShell contract 14 = **6,577 total assertions across 5 layers** (was 6,530 at L4, +47). New comprehensive feature doc [docs/PHASE_L5_DISCOVERY_EXPLORER.md](docs/PHASE_L5_DISCOVERY_EXPLORER.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.8 marked CLOSED with link back to PHASE_L5 doc. Versions bumped lockstep `0.50.0-alpha.4` -> `0.50.0-alpha.5` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.5 to dev + 960+ live SCIM tests must all pass before Phase L6 starts (Operations cross-endpoint view per analysis-doc S4.9).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase L4 - Log Config Admin UI (v0.50.0-alpha.4):** Fourth Phase L sub-phase (4 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.5). Replaces the SettingsPage stub with a real Log Config admin wired to the already-shipped `GET / PUT /scim/admin/log-config` surface (backend exhaustively locked at live layer 9j with ~80 assertions). Until L4 the operator had to curl the API to change a log level. **Architecture:** new `<LogConfigSection />` rendered after the existing version/health/storage cards. UI surfaces: global level Dropdown (closed-set from `availableLevels`), format Dropdown (pretty/json), includePayloads Switch, includeStackTraces Switch, max payload size + slow request threshold numeric Inputs, per-category level grid (one row per `availableCategories` entry x Dropdown of `availableLevels`). Empty per-category cells inherit the global level (caption explains). Audit-trail caption points the operator at `/scim/admin/logs` + LogStreamDrawer (Pulse icon in header from K4) since both already surface config-change events. **Mutation hook:** `useUpdateLogConfig()` is optimistic (mirrors L1 `useUpdateEndpointConfig` pattern): onMutate snapshots cache + deep-merges the partial body (scalars overwrite, categoryLevels merges so flipping http preserves existing scim.patch override), onError rolls back, onSettled invalidates. New `LogConfigResponse` + `LogConfigUpdateBody` types, `logConfigQueryOptions` helper, `queryKeys.logConfig` factory entry. **Pragmatic scope cut:** in-scope = global config knobs the operator changes daily. Out of scope = per-endpoint level grid (already lives in each endpoint's SettingsTab via profile.settings.logLevel - duplicating it cross-endpoint adds clutter without value), audit trail panel (audit rows already flow into /admin/logs and operators filter there), stream viewer (Phase K4 LogStreamDrawer already ships this), download button (deferred to Phase N export). **TDD discipline:** RED state confirmed BEFORE implementation in 2 cycles - 6/6 hook tests failed on missing `useLogConfig` + `useUpdateLogConfig` + `queryKeys.logConfig`, 5/5 page tests failed on missing `log-config-section` testid. GREEN: 11/11 L4 tests pass; full vitest 655/655 (was 644, +11). One mid-cycle test fixture fix: my fixture's `availableCategories` array initially missing `scim.patch` (the page only renders rows from availableCategories, so the override row in categoryLevels would be invisible without it); added it to match real backend output. **+11 web vitest** across 2 files: extended [mutations.test.ts](web/src/api/mutations.test.ts) +6 (queryKeys + GET URL + PUT body + invalidation + optimistic deep-merge + rollback + cold-cache PUT), extended [SettingsPage.test.tsx](web/src/pages/SettingsPage.test.tsx) +5 (section renders + global level + format + includePayloads + per-category grid with override values). Updated existing `vi.mock('../api/queries')` to add useLogConfig + useUpdateLogConfig defaults so the 2 pre-L4 SettingsPage tests still pass without changes. **+3 live SCIM** in new section 9z-AD before TEST SECTION 10 (availableLevels >= 5 / availableCategories >= 5 / 6 UI-bound scalar fields present). Backend behavior unchanged - 9j retains the full ~80-assertion suite for the underlying API. **Bundle:** main entry 147.48 -> 147.50 KB gzipped (+0.02 KB); shared primitives 199.09 -> 199.25 KB gzipped (+0.16 KB; Dropdown + Option + Divider + Field hoisted from shared chunk); SettingsPage chunk 0.95 -> 2.22 KB gzipped (+1.27 KB for log config admin; still 98 % under the 110 KB ceiling). **All 19 size-limit budgets pass**. **Test counts at v0.50.0-alpha.4:** API unit 3,720 / E2E 1,186 / Web vitest 644 -> **655** (+11) / Live SCIM 952 -> **955** (+3) / PowerShell contract 14 = **6,530 total assertions across 5 layers** (was 6,516 at L3, +14). New comprehensive feature doc [docs/PHASE_L4_LOG_CONFIG_ADMIN.md](docs/PHASE_L4_LOG_CONFIG_ADMIN.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.5 marked CLOSED with link back to PHASE_L4 doc. Versions bumped lockstep `0.50.0-alpha.3` -> `0.50.0-alpha.4` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.4 to dev + 955+ live SCIM tests must all pass before Phase L5 starts (Discovery Explorer per analysis-doc S4.8).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase L3 - Activity Analytics Dashboard (v0.50.0-alpha.3):** Third Phase L sub-phase (3 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.6). Wires the already-shipped `GET /scim/admin/activity/summary` (v0.18.0+) into the redesigned UI. Until L3 the dashboard showed only the live activity row stream + the requestsLast24hSeries sparkline; never the rolled-up counts. **Architecture:** new `<ActivityAnalyticsSection />` rendered between the existing sparkline card and the endpoints grid on DashboardPage. 4 KPI tiles in the same kpiRow grid (Operations 24h / Operations 7d / User ops 30d / Group ops 30d) sourced from `useActivitySummary()` + a horizontal users-vs-groups operations split bar with proportional fills + accessible aria-label (`Users X%, Groups Y%`) + numeric legend showing both percentage and absolute count. **Hook:** `useActivitySummary()` thin useQuery wrapper with 60s staleTime (underlying SQL counts are bounded but touch RequestLog; refetching on every focus would waste cycles). New `ActivitySummaryResponse` type, `activitySummaryQueryOptions` helper, `queryKeys.activitySummary` factory entry. **Pragmatic scope cut:** in-scope = ship the data the server already produces. Out-of-scope (deferred to Phase N polish) = p50/p95/p99 latency lines, drill-down navigation from chart slice to filtered LogsPage, time-range picker beyond the hardcoded 24h/7d/30d windows - all of these require a different aggregator strategy than the current bounded SQL counts. The right cut: ship now, defer aspirational. **TDD discipline:** RED state confirmed BEFORE implementation in 2 cycles - 3/3 hook tests failed on missing `useActivitySummary` + `queryKeys.activitySummary`, 4/4 page tests failed on missing `dashboard-analytics-section` testid. GREEN: 7/7 L3 tests pass; full vitest 644/644 (was 637, +7). **+7 web vitest** across 2 files: extended [mutations.test.ts](web/src/api/mutations.test.ts) +3 (queryKeys + GET URL contract + 500 -> ScimApiError), extended [DashboardPage.test.tsx](web/src/pages/DashboardPage.test.tsx) +4 (analytics section renders + 4 KPI tile values + ops-split bar with users/groups labels + zeroed in-memory backend handled). Updated useActivitySummary mock added to the existing `vi.mock('../api/queries', ...)` block so pre-L3 tests still see an empty/loading analytics section without having to set the return value themselves. **+4 live SCIM** in new section 9z-AC before TEST SECTION 10 (response envelope shape + summary fields + operations users/groups + top-level key allowlist with only 'summary' allowed - future leaks like raw _internal fields would fail). **Bundle:** main entry 147.50 -> 147.48 KB gzipped (-0.02 KB; vite chunking adjustment); shared primitives 199.06 -> 199.09 KB gzipped (+0.03 KB); DashboardPage chunk 2.06 -> 2.82 KB gzipped (+0.76 for the new analytics section; still 97.4 % under the 110 KB ceiling). **All 19 size-limit budgets pass** (unchanged from L2 - no new per-route entry needed since DashboardPage already has its budget). **Test counts at v0.50.0-alpha.3:** API unit 3,720 / E2E 1,186 / Web vitest 637 -> **644** (+7) / Live SCIM 948 -> **952** (+4) / PowerShell contract 14 = **6,516 total assertions across 5 layers** (was 6,505 at L2, +11). New comprehensive feature doc [docs/PHASE_L3_ACTIVITY_ANALYTICS.md](docs/PHASE_L3_ACTIVITY_ANALYTICS.md). [docs/INDEX.md](docs/INDEX.md) updated; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.6 marked CLOSED with link back to PHASE_L3 doc. Versions bumped lockstep `0.50.0-alpha.2` -> `0.50.0-alpha.3` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.3 to dev + 952+ live SCIM tests must all pass before Phase L4 starts (Log Config admin per analysis-doc S4.5).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase L2 - /Me Self-Service UI (v0.50.0-alpha.2):** Second Phase L sub-phase (2 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.7). Wires the already-shipped `/scim/endpoints/:id/Me` HTTP surface (RFC 7644 S3.11, v0.20.0) into the redesigned UI. Until L2 the token holder could never see what the server thinks they are. **Architecture:** new top-level page at `/me` ([MeProfilePage.tsx](web/src/pages/MeProfilePage.tsx)) with a single-select Card-grid endpoint picker (sourced from `useEndpoints()`) -> `useMe(pickedEndpointId)` -> profile card showing userName + id + meta.version + Display name input + Active switch + Save (PATCH PatchOp) + Delete (type-username-to-confirm modal mirroring L1 DeleteEndpointDialog UX). New `My profile` sidebar nav entry between Manual Provision and Logs. **Critical auth-model branch:** `/Me` requires OAuth JWT auth with a `sub` claim matching a User's `userName`. The K3 TokenGate's shared-secret bearer ALWAYS returns 404 noTarget. The page surfaces this as first-class UX: K3 `<ScimErrorMessage />` PLUS a dedicated `<MessageBar intent='info'>` hint pointing the operator at per-endpoint OAuth credentials (issued by the Credentials tab on the picked endpoint). Without this branch the operator would blame the UI for a server-enforced constraint. **Hooks (Phase L2):** `useMe(endpointId)` thin useQuery wrapper with `retry: false` (404 is the normal case for shared-secret tokens; do not hammer the server) and `enabled: !!endpointId` so the picker can mount without firing N concurrent fetches; `usePatchMe(endpointId)` PATCH /Me with SCIM PatchOp body (no optimistic write - consistency over felt latency for self-service); `useDeleteMe(endpointId)` DELETE /Me with cache eviction via `qc.removeQueries(queryKeys.me(endpointId))` + dashboard + endpoints.overview invalidation on settle. New `MeResource` type, `meQueryOptions(endpointId)` helper, `queryKeys.me(endpointId)` factory entry. **TDD discipline:** RED state confirmed BEFORE implementation in 2 cycles - 7/7 hook tests failed on missing `useMe`/`usePatchMe`/`useDeleteMe`/`queryKeys.me`, 7/7 page tests failed on missing module. GREEN: 14/14 L2 tests pass; full vitest 637/637 (was 620, +17 net including +1 queryKeys + +2 contract churn for new route file + new size-limit entry). **Test fixes mid-cycle:** (1) initial `useMe` 404 test waited for `error` to be defined but TanStack Query surfaces failure via `isError` first; switched to `await waitFor(() => expect(result.current.isError).toBe(true))`. (2) initial `usePatchMe` test used `require('./queries')` for the queryKeys lookup which doesn't work in ESM/vitest; switched to top-level `await import('./queries')` destructure. (3) initial MeProfilePage Delete /Me regex `/^Delete /Me$/i` had an unescaped slash that broke the regex parse; escaped to `/^Delete \/Me$/i`; AND there were two buttons matching that name (page header Delete button + dialog submit Delete /Me) so disambiguated by changing the page button label to just 'Delete' with the trash icon. **+17 web vitest tests** across 2 files: extended [mutations.test.ts](web/src/api/mutations.test.ts) +7 (queryKeys.me factory + 3 useMe covering URL + disabled-on-empty + 404 noTarget + 1 usePatchMe with PatchOp body + invalidation + 2 useDeleteMe with cache eviction + dashboard/overview invalidation), new [MeProfilePage.test.tsx](web/src/pages/MeProfilePage.test.tsx) (7 - endpoint picker renders one card per endpoint + does not call useMe with real id until pick + happy profile renders userName/displayName/id + 404 noTarget renders OAuth-required hint card + Save fires PatchOp envelope with replace ops on changed fields + Delete confirm modal disabled until exact userName match + Delete confirm fires useDeleteMe). **+6 live SCIM** in new section 9z-AB before TEST SECTION 10 (GET /Me 404 / scimType=noTarget / detail mentions OAuth / SCIM Error schema URN present / PATCH /Me 404 / DELETE /Me 404). Backend behavior unchanged - 9z-AB locks the auth-model branch the UI consumes. **Bundle:** main entry 147.22 -> 147.50 KB gzipped (+0.28 KB across L1+L2; 26.3 % under 200 KB K1 budget); shared primitives 198.81 -> 199.06 KB gzipped (+0.25 KB; 9.5 % under 220 KB budget); 16 existing per-route chunks unchanged + 2 new per-route chunks at L1 (CreateEndpointWizard 2.64 KB, EditEndpointPage 0.98 KB) + 1 new per-route chunk at L2 (MeProfilePage 2.33 KB). **All 19 size-limit budgets pass** (was 18 at v0.50.0-alpha.1; +1 for MeProfilePage). **Test counts at v0.50.0-alpha.2:** API unit 3,720 / E2E 1,186 / Web vitest 620 -> **637** (+17) / Live SCIM 940 -> **946** (+6) / PowerShell contract 14 = **6,503 total assertions across 5 layers** (was 6,480 at L1, +23). New comprehensive feature doc [docs/PHASE_L2_ME_SELF_SERVICE.md](docs/PHASE_L2_ME_SELF_SERVICE.md) (7 sections, 2 Mermaid - architecture + happy/404 lifecycle sequences, file inventory, risk register with 5 entries, per-step quality gate sequence with 16 checkpoints). [docs/INDEX.md](docs/INDEX.md) updated with L2 entry; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.7 marked CLOSED with link back to PHASE_L2 doc. Versions bumped lockstep `0.50.0-alpha.1` -> `0.50.0-alpha.2` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.2 to dev + 946+ live SCIM tests must all pass before Phase L3 starts (Activity Analytics dashboard per analysis-doc S4.6).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase L1 - Endpoint CRUD UI + Preset Picker (v0.50.0-alpha.1):** First Phase L sub-phase (1 of 6 in Capability Completeness per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.1). Closes the highest-leverage Tier 1 Operational Completeness gap: until L1 an operator could not create or delete an endpoint from the UI; every onboarding required shell access + a hand-crafted curl POST. The CRUD HTTP surface (POST / PATCH / DELETE /admin/endpoints + GET /admin/endpoints/presets) shipped in v0.30.0; L1 finally wires it. **Architecture:** new 4-step Wizard at `/endpoints/new` (Identity & Preset / Preview / Override / Confirm) + edit form at `/endpoints/$id/edit` + type-name-to-confirm delete modal in EndpointDetail header. **Wizard step layout:** Step 1 takes name + display name + description + preset (Combobox of Cards sourced from `usePresets()`, each card shows preset name + default badge + schemaCount + resourceTypeCount); Step 2 shows the picked preset's full preview (schemas list + resourceTypes list + ServiceProviderConfig dict + default settings dict) sourced from `usePresetDetail()`; Step 3 placeholder for per-flag overrides (deferred until BOOLEAN_FLAGS registry is extracted from SettingsTab into its own module so the wizard chunk does not re-import SettingsTab and merge chunks); Step 4 read-only summary of all 4 steps + Create button fires `useCreateEndpoint()` and navigates to `/endpoints/{newId}` on success. **Delete safety:** new [DeleteEndpointDialog.tsx](web/src/components/endpoint/DeleteEndpointDialog.tsx) mounts in EndpointDetail header. Echoes endpoint name in monospace, requires operator to type it case-sensitively into a free-text Input before the Delete button enables (mirrors GitHub repo-delete UX - one character drift means the operator was not paying attention; the button stays disabled). Composes Phase C2 `<FormDialog />` primitive so cancel / busy / structured-error chrome is reused. **Mutation hooks (Phase L1):** `useCreateEndpoint()` posts to /scim/admin/endpoints, returns the created EndpointResponse, invalidates queryKeys.endpoints.all + dashboard on settle. `useDeleteEndpoint()` DELETEs by id, additionally evicts per-endpoint detail / overview / stats caches via `qc.removeQueries(...)` so a stale RouteLoader cannot resurrect deleted data on navigation. **Query hooks (Phase L1):** `usePresets()` + `usePresetDetail(name)` thin useQuery wrappers; both cached 5 minutes (preset library only changes on server upgrade). New `queryKeys.presets.{all, detail(name)}` factory entries. **Type correction:** `PresetSummary` updated mid-implementation - the real server shape nests counts + SPC + activeSettings under `summary.*` not at the top level. Caught at the live-test layer (section 9z-AA.2 was the canary that flagged the type drift before merge). **Wizard preset picker design:** chose a single-select Card grid over a Fluent UI Combobox because Combobox open/close + keyboard listbox is flaky in jsdom unit tests AND the Card surface lets us show schemaCount / resourceTypeCount / default badge inline. **TDD discipline:** RED state confirmed BEFORE implementation in 4 separate test cycles - 8/8 mutation hooks failed on missing `useCreateEndpoint`/`useDeleteEndpoint`/`queryKeys.presets`, 5/5 DeleteEndpointDialog tests failed on missing module, 8/8 wizard tests failed on missing module, 3/3 EditEndpointPage tests failed on missing module. GREEN: 30/30 L1 tests pass; full vitest 620/620 (was 590, +30). **+30 web vitest tests** across 5 files: extended [mutations.test.ts](web/src/api/mutations.test.ts) +8 (3 useCreateEndpoint covering POST URL/body + return shape + 400 ScimApiError + 3 useDeleteEndpoint covering DELETE 204 + cache eviction + 404 ScimApiError + 2 presets queryKeys), new [DeleteEndpointDialog.test.tsx](web/src/components/endpoint/DeleteEndpointDialog.test.tsx) (5 - name echo + disabled-by-default + mismatch keeps disabled + exact-match enables + submit fires mutation), new [CreateEndpointWizard.test.tsx](web/src/pages/CreateEndpointWizard.test.tsx) (8 - Step 1 renders + Next gate + advance to Step 2 + preview shows counts + Step 4 commit body + navigate on success + Back preserves state + 400 -> ScimErrorMessage), new [EditEndpointPage.test.tsx](web/src/pages/EditEndpointPage.test.tsx) (3 - form pre-filled + Save body + navigate on success), 6 implicit churn (lazy-routes test +2 entries for new route files / size-limit-config test +2 ROUTE_CHUNK_NAMES entries / 2 new per-route size-limit budget entries in package.json). **+2 API E2E** in new [admin-endpoints-create.e2e-spec.ts](api/test/e2e/admin-endpoints-create.e2e-spec.ts) - response key allowlist on POST /admin/endpoints (id + name + active + scimBasePath required; allowlist also covers displayName + description + profile + profileSummary + _links + createdAt + updatedAt; any unknown key fails the assertion preventing silent leaks like credentialHash) + duplicate-name rejection asserting `[400, 409]` so future RFC-aligned tightening flows through the same UI surface. **+6 live SCIM** in new section 9z-AA before TEST SECTION 10 per live-test conventions (GET /admin/endpoints/presets returns >=3 / every preset has summary.schemaCount + summary.resourceTypeCount / POST with profilePreset returns wizard-required fields / duplicate-name rejected 400 or 409 / DELETE returns 204 / GET on deleted returns 404). **Bundle:** main entry 147.22 -> 147.39 KB gzipped (+0.17 KB; 26.3 % under 200 KB K1 budget); shared primitives 198.81 -> 199.01 KB gzipped (+0.20 KB; 9.5 % under 220 KB budget); 14 existing per-route chunks unchanged + 2 new per-route chunks (CreateEndpointWizard 2.64 KB / EditEndpointPage 0.98 KB - both 97 %+ under 110 KB ceiling). **All 18 size-limit budgets pass** (was 16 at v0.49.0 stable; +2 for the new per-route entries). **Test counts at v0.50.0-alpha.1:** API unit 3,720 / E2E 1,184 -> **1,186** (+2) / Web vitest 590 -> **620** (+30) / Live SCIM 933 -> **939** (+6) / PowerShell contract 14 = **6,479 total assertions across 5 layers** (was 6,441 at v0.49.0 stable, +38). New comprehensive feature doc [docs/PHASE_L1_ENDPOINT_CRUD.md](docs/PHASE_L1_ENDPOINT_CRUD.md) (7 sections, 2 Mermaid - architecture flow + commit lifecycle sequence, 4-step layout table, file inventory, risk register with 5 entries, per-step quality gate sequence with 21 checkpoints). [docs/INDEX.md](docs/INDEX.md) updated with new L1 entry; [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.1 marked CLOSED with link back to PHASE_L1 doc. Versions bumped lockstep `0.49.0` -> `0.50.0-alpha.1` (api+web+lockfiles regenerated in node:25-alpine). **Per-sub-phase quality gate next: deploy v0.50.0-alpha.1 to dev + 939+ live SCIM tests must all pass before Phase L2 starts (/Me endpoint per analysis-doc S4.4).** **Prod promotion: NOT triggered** - prod still on v0.48.0; dev-only deploy per standing rule. |
| 2026-05-13 | release **v0.49.0 stable - Phase K Foundation Hardening - COMPLETE (5 of 5 sub-phases):** Drops the `-alpha.N` suffix after every Phase K sub-phase shipped, deployed to dev, and passed its 933+ live SCIM gate. Pure version cut + lockfile sync; no new features beyond the 5 already-released alphas. **Sub-phases shipped:** K1 alpha.1 Route code splitting (+49 web vitest -> 452, main entry 381 -> 145 KB gzipped, -62 % first-paint download), K2 alpha.2 Service health rollup widget (+32 -> 484, traffic-light icon + 5-row Popover + SSE connection-state slice on ui-store), K3 alpha.3 Smart error explainer (+39 -> 523, ScimApiError class + parseScimError parser + ScimErrorMessage primitive wired across 3 consumer surfaces with full RFC 7644 Table 9 catalog), K4 alpha.4 Live SSE log stream viewer (+37 -> 560, opt-in OverlayDrawer with independent EventSource at DEBUG level + 5,000-entry ring buffer + tail-f auto-scroll + level/search filters + Pause/Clear), K5 alpha.5 ETag / RequireIfMatch UX (+30 -> 590, EtagBadge + ConflictDialog primitives + parseResourceEtag/formatIfMatchValue/isForceOverwriteSafe helpers + ResourceDetailDrawer If-Match wiring + 412/428 -> ConflictDialog with refresh-and-reapply / force-overwrite / cancel actions). **Cumulative test counts at v0.49.0 stable:** API unit 3,720 / E2E 1,184 / Web vitest 403 -> **590** (+187 across Phase K) / Live SCIM 933 unchanged / PowerShell contract 14 unchanged = **6,441 total assertions across 5 layers** (was 6,254 at v0.48.1). API + live unchanged across all of Phase K (frontend-only). All 16 size-limit budgets green at v0.49.0: main entry 147 KB gzipped (27 % under K1's 200 KB budget), shared primitives 199 KB gzipped (10 % under 220 KB budget), 14 per-route chunks all under 11 KB gzipped (10 % of the 110 KB per-route ceiling). New [CHANGELOG.md](CHANGELOG.md) entry `## [0.49.0]` summarizing the 5 sub-phases + cumulative deltas. Versions bumped lockstep `0.49.0-alpha.5` -> `0.49.0` (api+web+lockfiles regenerated in node:25-alpine). **Per-phase final quality gate next: deploy v0.49.0 to dev + 933+ live SCIM tests must all pass before Phase L1 starts (Endpoint CRUD UI + Preset Picker per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.1 - wires the already-shipped POST/PATCH/DELETE /admin/endpoints + GET /admin/endpoints/presets backend into a 4-step wizard at /endpoints/new and edit/delete on /endpoints/$id).** **Prod promotion: NOT triggered** - prod still on v0.48.0 (Phase J SSE bridge); dev-only deploy per standing rule. |
| 2026-05-13 | feat **Phase K5 - ETag / Version Surface + RequireIfMatch UX (v0.49.0-alpha.5):** Fifth and final Phase K sub-phase (5 of 5 in Foundation Hardening per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S4.10). Brings RFC 7644 S3.14 concurrency control out of the network tab and into the UI. **Architecture:** new pure helpers in [web/src/api/etag.ts](web/src/api/etag.ts) (`parseResourceEtag` discriminated union with `kind: 'version' | 'legacy' | 'missing'`, `formatIfMatchValue`, `isForceOverwriteSafe`, `FORCE_OVERWRITE_IF_MATCH` constant) + new `<EtagBadge />` primitive (monospace `v3` chip with tooltip) + new `<ConflictDialog />` primitive (Fluent UI Dialog with side-by-side pending-vs-server diff columns + server ETag display + 3 actions: Refresh-and-reapply / Force-overwrite / Cancel). **ResourceDetailDrawer wiring:** `handleSave()` becomes `handleSave(overrideIfMatch?)` parsing the resource ETag on every call; passes `If-Match: W/"vN"` header (the existing useUpdateUser/useUpdateGroup hooks accepted ifMatch since Phase C5 F-5 hardening but no caller had ever passed it); on `ScimApiError(status: 412 | 428)` opens ConflictDialog instead of generic `<ScimErrorMessage />`; all other statuses still fall through to the K3 error explainer. **Recovery flows:** Refresh-and-reapply re-seeds form fields from current resource snapshot (preserves the operator's pending diff for re-Save); Force-overwrite calls `handleSave(FORCE_OVERWRITE_IF_MATCH='*')` re-firing the same mutation with `If-Match: *`. **Force-overwrite safety policy** locked by tests: only allowed when serverResource has a `meta.version` we can reason about - 428 case (RequireIfMatch=true + no ETag) hits this branch by definition and the button is hidden. Tolerant of legacy timestamp ETags (`W/"<ISO>"`) for backward compat with pre-v0.16.0 deployments. **TDD discipline:** RED state confirmed BEFORE implementation - 5 of 40 K5-surface tests failing on missing module + missing testid + 412 falling through to drawer-error. GREEN: 40/40 K5 tests pass; full vitest 590/590 (was 560, +30). **+30 web vitest tests** across 4 files: new [etag.test.ts](web/src/api/etag.test.ts) (13 - parser cases for version/legacy/missing + force-overwrite policy + edge cases like null resource), new [EtagBadge.test.tsx](web/src/components/primitives/EtagBadge.test.tsx) (4 - render cases for version/legacy/missing + aria-label includes 'ETag'), new [ConflictDialog.test.tsx](web/src/components/primitives/ConflictDialog.test.tsx) (7 - open/close, both columns render, server ETag visible, all 3 buttons fire callbacks, force-overwrite hidden when unsafe), extended [ResourceDetailDrawer.test.tsx](web/src/components/detail/ResourceDetailDrawer.test.tsx) +5 (etag-badge testid, If-Match forwarded on Save, 412 -> ConflictDialog opens, force-overwrite re-fires with `If-Match: *`, 428 -> ConflictDialog opens). **Bundle:** main entry 152.40 -> 147.22 KB gzipped (-5.18 KB - Tooltip/Dialog usage hoisted into shared chunk by vite chunking heuristics because ConflictDialog also references them); shared primitives 180.70 -> 198.81 KB (+18.11 KB for ConflictDialog + EtagBadge + their Fluent dependencies; still 9 % under 220 KB K1 budget). All 16 size-limit budgets pass. **Test counts at v0.49.0-alpha.5:** API unit 3,720 / E2E 1,184 / Web vitest 560 -> **590** (+30) / Live SCIM 933 unchanged = **6,441 total assertions across 5 layers** (was 6,411 at K4). API + live unchanged - pure frontend commit. New comprehensive feature doc [docs/PHASE_K5_ETAG_AND_REQUIREIFMATCH.md](docs/PHASE_K5_ETAG_AND_REQUIREIFMATCH.md) (8 sections, 2 Mermaid - architecture + Save lifecycle sequence, force-overwrite safety policy, before/after UX comparison, file inventory, DoD). Versions bumped lockstep `0.49.0-alpha.4` -> `0.49.0-alpha.5` (api+web+lockfiles). **Per-sub-phase quality gate next: deploy v0.49.0-alpha.5 to dev + 933+ live SCIM tests must all pass before the 0.49.0 stable rollup (drops -alpha.N suffix and closes Phase K Foundation Hardening, 5 of 5 sub-phases shipped).** **Prod promotion: NOT triggered** - dev-only deploy per standing rule. |
| 2026-05-12 | feat **Phase K4 - Live SSE Log Stream Viewer (v0.49.0-alpha.4):** Fourth Phase K sub-phase (4 of 5 in Foundation Hardening per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S6.6). Brings the grafana-loki-tail experience inside the admin UI. **Architecture:** new floating right-side OverlayDrawer toggled from a Pulse icon button in AppHeader. Reads from new `useLogStream` hook in [web/src/hooks/useLogStream.ts](web/src/hooks/useLogStream.ts) which opens its own INDEPENDENT EventSource at /scim/admin/log-config/stream?level=DEBUG - decoupled from useSSE (which still tails at level=INFO for cache invalidation only). **Why two EventSource connections:** useSSE filters at INFO+ for cache events; useLogStream needs DEBUG-level visibility for human eyeballs. Buffer policies differ - useSSE forwards into TanStack Query invalidation with no retention; useLogStream keeps a 5,000-entry ring buffer (oldest dropped first). Lifecycle differs - useSSE always-on while UI is mounted; useLogStream open-on-demand (only while drawer is open). Decoupling means filter changes never require a network reconnect (filter is consumer-side via pure `filterEntries` reducer) AND a closed drawer consumes zero connections. **Toolbar features:** connection-state badge (green=open, yellow=reconnecting, subtle=connecting/closed); level filter Combobox (DEBUG/INFO/WARN/ERROR); free-text SearchBox (case-insensitive substring across message + path + category + requestId + endpointId + method); Pause/Resume button (drops messages while paused intentionally - the buffer reflects what the operator chose to retain); Clear button (empties buffer without disconnecting); X close button. **Auto-scroll-to-bottom** only when the operator is within 50 px of the bottom (standard tail-f UX - manual scroll-up freezes auto-follow). **Cap banner** appears when buffer hits 5,000 entries to surface the older-entries-dropped semantics. **Type guard** distinguishes log entries (carry `level + message + category` triple) from SCIM mutation events (`{ type: 'scim.x.y', endpointId }`) on the same SSE channel - the K4 hook only buffers log entries; SCIM events flow through useSSE for invalidation. **Exponential-backoff reconnect** (1s/2s/4s/8s/.../30s ceiling). **ui-store slice extension:** `logStreamDrawerOpen` (default false) + `logStreamLevel` (default DEBUG) + `logStreamSearch` (default '') with 4 setters + toggle. Filter state persists across drawer close/open within a session. **TDD discipline:** RED state confirmed BEFORE implementation - 37 K4-surface tests failing on missing module + missing ui-store slice. GREEN: 37/37 K4 tests pass; full vitest 560/560 (was 523, +37). One flaky cap-banner test (rendering 5,000 rows under shared-suite contention) hardened with explicit 20s per-test timeout. **+37 web vitest tests** across 3 files: new [useLogStream.test.ts](web/src/hooks/useLogStream.test.ts) (17 - 6 pure filter + 11 hook integration covering ring-buffer cap / paused-drop semantics / exponential backoff / non-JSON message tolerance / SCIM-event-type-guard), new [LogStreamDrawer.test.tsx](web/src/layout/LogStreamDrawer.test.tsx) (11 - render / list / connection badge / pause / clear / close / empty state / level filter / search filter / cap banner), extended [ui-store.test.ts](web/src/store/ui-store.test.ts) +9 (defaults, setters, toggle, every legal level value). **Bundle:** main entry 150.34 -> 152.40 KB gzipped (+2.06 KB for AppHeader Pulse icon button; still 24 % under K1 200 KB budget); shared primitives 180.69 -> 180.70 KB (+0.01 KB - OverlayDrawer + Combobox + SearchBox already in chunk via DetailDrawer / Manual Provision usage). All 16 size-limit budgets pass. **Test counts at v0.49.0-alpha.4:** API unit 3,720 / E2E 1,184 / Web vitest 523 -> **560** (+37) / Live SCIM 933 unchanged = **6,411 total assertions across 5 layers** (was 6,374 at K3). API + live unchanged - pure frontend commit. New comprehensive feature doc [docs/PHASE_K4_LIVE_LOG_STREAM_VIEWER.md](docs/PHASE_K4_LIVE_LOG_STREAM_VIEWER.md) (7 sections, 2 Mermaid - 2-EventSource architecture flow + lifecycle state machine, drawer UX ASCII layout, bundle impact, DoD). Versions bumped lockstep `0.49.0-alpha.3` -> `0.49.0-alpha.4` (api+web+lockfiles). **Per-sub-phase quality gate next: deploy v0.49.0-alpha.4 to dev + 933+ live SCIM tests must all pass before Phase K5 starts (ETag / version surface).** **Prod promotion: NOT triggered** - dev-only deploy per standing rule. |
| 2026-05-12 | feat **Phase K3 - Smart Error Explainer (v0.49.0-alpha.3):** Third Phase K sub-phase (3 of 5 in Foundation Hardening per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S5.7). Wires the existing error catalog ([docs/LOGGING_ERROR_HANDLING_IDEAL_DESIGN.md](docs/LOGGING_ERROR_HANDLING_IDEAL_DESIGN.md) S16-18) into the UI. **Three-layer architecture:** (1) Structured throw - new `ScimApiError extends Error` class in [web/src/api/scim-error.ts](web/src/api/scim-error.ts) thrown by `fetchWithAuth` for every non-OK response, carrying status / scimType / detail / rawBody / requestId; backward-compatible because `message = detail || HTTP <status>` so existing `err instanceof Error` + `err.message` paths keep working. (2) Pure parser - `parseScimError(unknown)` normalizes ScimApiError / plain Error / string / null / undefined into a uniform `ParsedScimError` with the matching `SCIM_ERROR_CATALOG` entry attached. (3) Presentational primitive - new [`<ScimErrorMessage />`](web/src/components/primitives/ScimErrorMessage.tsx) renders nothing for null/undefined (safe to mount unconditionally), otherwise emits Fluent MessageBar with catalog title + plain-English explanation + monospace server detail + monospace requestId + optional Read-the-spec RFC link (target=_blank rel=noopener noreferrer) + collapsible View-details expander showing pretty-printed rawBody JSON. **Catalog vocabulary** covers every RFC 7644 Table 9 keyword (uniqueness, invalidFilter, invalidSyntax, invalidPath, noTarget, invalidValue, mutability, invalidVers, sensitive, tooMany) + project extensions (versionMismatch, tooLarge) + 5 HTTP-status fallbacks (401/403/404/412/415/428/5xx/generic). Test `it.each(KNOWN_KEYWORDS)` block fails until any new keyword has both a non-empty title and a >20-char explanation. **3 consumer surfaces wired:** ResourceDetailDrawer (error state `string|null` -> `unknown`, render via primitive), ManualProvisionPage (result error `message: string` -> `error: unknown`, ProvisionResult renders via primitive), CredentialsTab (createError + deleteError -> `unknown`, passed via new `FormDialog.error?: unknown` prop which renders `<ScimErrorMessage />` instead of the legacy red banner; legacy `errorMessage?: string` retained for backward compat). **TDD discipline:** RED state confirmed BEFORE implementation - 39 K3-surface tests failing on missing module. GREEN: 39/39 K3 tests pass; full vitest 523/523 (was 484, +39). **+39 web vitest tests** across 4 files: new [scim-error.test.ts](web/src/api/scim-error.test.ts) (28 - 12 catalog completeness via it.each + 4 ScimApiError shape + 12 parser scenarios for every input type), new [ScimErrorMessage.test.tsx](web/src/components/primitives/ScimErrorMessage.test.tsx) (9 - render/null/title/explanation/detail/expander/docs-link/fallback), extended [queries.test.ts](web/src/api/queries.test.ts) +2 (structured throw on JSON SCIM error + 5xx text body) + 1 legacy assertion updated to assert ScimApiError class. **Bundle:** main entry 151.10 -> 150.34 KB gzipped (-0.76 KB; some MessageBar usage migrated into shared chunk); shared primitives 173.04 -> 180.69 KB (+7.65 KB for catalog + parser + component; still 18 % under 220 KB K1 budget). All 16 size-limit budgets pass. **Test counts at v0.49.0-alpha.3:** API unit 3,720 / E2E 1,184 / Web vitest 484 -> **523** (+39) / Live SCIM 933 unchanged = **6,374 total assertions across 5 layers** (was 6,335 at K2). API + live unchanged - pure frontend commit. New comprehensive feature doc [docs/PHASE_K3_SMART_ERROR_EXPLAINER.md](docs/PHASE_K3_SMART_ERROR_EXPLAINER.md) (8 sections, 1 Mermaid - 3-layer architecture flow, full catalog table, before/after UX comparison, file inventory, DoD). Versions bumped lockstep `0.49.0-alpha.2` -> `0.49.0-alpha.3` (api+web+lockfiles). **Per-sub-phase quality gate next: deploy v0.49.0-alpha.3 to dev + 933+ live SCIM tests must all pass before Phase K4 starts (live SSE log stream viewer).** **Prod promotion: NOT triggered** - dev-only deploy per standing rule. |
| 2026-05-12 | feat **Phase K2 - Service Health Rollup Widget (v0.49.0-alpha.2):** Second Phase K sub-phase (2 of 5 in Foundation Hardening per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) S6.10). Closes the at-a-glance health-visibility gap. **Architecture:** new top-right traffic-light icon in [web/src/layout/AppHeader.tsx](web/src/layout/AppHeader.tsx) (green / yellow / red / grey) + on-click Fluent UI Popover listing 5 substatus rows. **Pure frontend** - aggregates signals already produced by /scim/health, /scim/admin/version (databaseProvider, persistenceBackend, auth.{oauth,jwt,scim}SecretConfigured), /scim/admin/dashboard (recentActivity 5xx counts), plus the SSE EventSource lifecycle now mirrored into a new `sseConnectionState` slice on the Zustand `ui-store`. No new API surface (a deeper backend probe is deferred to Phase L). **Substatus rules** (locked by tests): API (healthy if /health returns ok, down on isError), Database (healthy if version.storage.databaseProvider truthy, down on isError), Auth (healthy if all 3 secrets configured, degraded if 1-2 missing, down if all 3 missing or version isError), Realtime (open=healthy, connecting/reconnecting=degraded, closed=down), Recent errors (0 5xx=healthy, 1-5=degraded, >=6=down). **Overall rollup reducer** is pure: strictest substatus wins (down > degraded > healthy > unknown); exported as `rollupOverallStatus` so the test suite can lock the precedence without a React tree. **SSE wiring:** [web/src/hooks/useSSE.ts](web/src/hooks/useSSE.ts) now writes the EventSource lifecycle to ui-store at every transition (pre-construct -> 'connecting', onopen -> 'open', onerror -> 'reconnecting', cleanup -> 'closed'); a new SseConnectionState type + setSseConnectionState setter on the store form the contract. **A11y:** trigger button always exposes `aria-label='System status: <Healthy|Degraded|Down|Unknown>'` so screen readers get the keyword without opening the popover; per-row literal status text sits beside each colored icon for colorblind / screen-reader parity. **TDD discipline:** RED state confirmed BEFORE implementation - 8/52 K2 tests failing on missing module + missing setter. GREEN: 52/52 K2 tests pass; full vitest 484/484 (was 452, +32). **+32 web vitest tests** across 4 files: new [web/src/hooks/useHealthRollup.test.ts](web/src/hooks/useHealthRollup.test.ts) (17 - 4 reducer cases + 13 hook scenarios covering every substatus state transition + the overall-precedence guard), new [web/src/layout/HealthRollup.test.tsx](web/src/layout/HealthRollup.test.tsx) (5 - per-overall-status icon + popover open + per-row testids), new [web/src/store/ui-store.test.ts](web/src/store/ui-store.test.ts) (6 - new slice contract: default 'closed', setter exists, every legal state value accepted), extended [web/src/hooks/useSSE.test.ts](web/src/hooks/useSSE.test.ts) +4 (writes 'connecting' immediately, 'open' on onopen, 'reconnecting' on onerror, 'closed' on unmount). **Bundle:** main entry chunk 144.74 KB -> 151.10 KB gzipped (+6 KB; still 24 % under the K1 200 KB budget). All 16 size-limit budgets pass. **Test counts at v0.49.0-alpha.2:** API unit 3,720 / E2E 1,184 / Web vitest 452 -> **484** (+32) / Live SCIM 933 unchanged = **6,335 total assertions across 5 layers** (was 6,303 at K1). API + live unchanged - pure frontend commit. New comprehensive feature doc [docs/PHASE_K2_SERVICE_HEALTH_ROLLUP.md](docs/PHASE_K2_SERVICE_HEALTH_ROLLUP.md) (8 sections, 2 Mermaid - architecture flow + SSE lifecycle state machine, substatus-rule table, file inventory, A11y notes, DoD). Versions bumped lockstep `0.49.0-alpha.1` -> `0.49.0-alpha.2` (api+web+lockfiles). **Per-sub-phase quality gate next: deploy v0.49.0-alpha.2 to dev + 933+ live SCIM tests must all pass before Phase K3 starts (smart error explainer).** **Prod promotion: NOT triggered** - dev-only deploy per standing rule. |
| 2026-05-12 | feat **Phase K1 - Route-Level Code Splitting (v0.49.0-alpha.1):** First Phase K sub-phase (1 of 5 in Foundation Hardening per [docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md](docs/UI_NEXT_GAPS_LATERAL_ANALYSIS_2026.md) §9). Closes the H6-deferred route-splitting gap. **Architecture:** all 14 TanStack Router route files under [web/src/routes/](web/src/routes/) replaced static `import { X } from '../pages/X'` with `const X = React.lazy(() => import('../pages/X').then(m => ({ default: m.X })))`. New Suspense boundary in [web/src/routes/__root.tsx](web/src/routes/__root.tsx) wraps `<Outlet />` with `RouteLoadingFallback` (LoadingSkeleton count=6 height=40px) so cold-cache route loads have a single shared loading surface. Pre-existing dev-only TanStackRouterDevtools Suspense unchanged. **Bundle delta (the headline win):** main entry chunk gzipped **381.49 KB -> 144.74 KB (-62 %)**. Vite emits 41 chunks total: main entry + shared `primitives-*.js` (174 KB gz, all the LoadingSkeleton/EmptyState/KpiChart/DetailDrawer/FormDialog Fluent surfaces) + 14 per-page chunks (each 1-11 KB gz, largest ActivityTab at 10.54 KB) + ~25 vendor chunks (Fluent UI per-component splits). **First-paint download:** entry + primitives + matched route ~325 KB vs prior 381 KB; subsequent navigations download only the new route chunk. defaultPreload:'intent' from Phase A4 means most clicks have the chunk cached on hover - cold-load Suspense fallback only shows on direct URL navigation. **size-limit restructured:** 1 budget -> **16 budgets** in [web/package.json](web/package.json). Main entry tightened 400 KB -> 200 KB (28% headroom over measured). New shared primitives chunk budget 220 KB (27% headroom). 14 per-route budgets at 110 KB ceiling (per plan §12; largest measured at 9.6 % of budget). All 16 size-limit assertions pass via `npm run size`. **TDD discipline:** RED state confirmed BEFORE implementation - 44/54 K1 tests failing on missing module / missing entry. GREEN state: 47/47 vitest files pass, 452/452 tests pass. **+49 web vitest tests** across 3 files: new [web/src/routes/lazy-routes.test.ts](web/src/routes/lazy-routes.test.ts) (29 source-pattern tests, one per route file, asserting React.lazy + dynamic import AND no static `import { X } from '../pages/X'`; comment-stripped source so docstring references to deleted patterns don't false-positive); new [web/src/routes/route-suspense.test.ts](web/src/routes/route-suspense.test.ts) (3 source-pattern tests asserting __root.tsx wraps Outlet in Suspense with non-null fallback AND imports LoadingSkeleton); extended [web/src/test/size-limit-config.test.ts](web/src/test/size-limit-config.test.ts) +17 (per-route entry existence, gzipped flag, path glob convention, <=110 KB ceiling, K1 ratchet floor for main+primitives). **Test counts at v0.49.0-alpha.1:** API unit 3,720 / E2E 1,184 / Web vitest 403 -> **452** (+49) / Live SCIM 933 unchanged = **6,303 total assertions across 5 layers** (was 6,254 at v0.48.1). API + live unchanged - pure frontend commit. New comprehensive feature doc [docs/PHASE_K1_ROUTE_CODE_SPLITTING.md](docs/PHASE_K1_ROUTE_CODE_SPLITTING.md) (9 sections, 2 Mermaid - bundle layout before/after + lazy-load+Suspense lifecycle, performance impact table, full per-route chunk size measurements, risk register, DoD). Versions bumped lockstep `0.48.1` -> `0.49.0-alpha.1` (api+web+lockfiles). **Per-sub-phase quality gate next: deploy v0.49.0-alpha.1 to dev + 933+ live SCIM tests must all pass before Phase K2 starts (service health rollup widget).** **Prod promotion: NOT triggered** - dev-only deploy per standing rule. |
| 2026-05-11 | release **0.48.1 - Phase J SCIM Event SSE Bridge + MSW worker mount + DELIVERY_PLAN sweep:** Closes the cross-tab refresh gap left over from Phase B by wiring SCIM mutation events onto the SSE wire `useSSE` already listens to. **Pre-J root cause** (discovered by tracing the SSE flow end-to-end): `useSSE` reads `data?.type ?? data?.event` from each SSE message, but the SSE stream only forwarded `StructuredLogEntry` objects (no `type` field). The `eventEmitter.emit(SCIM_EVENTS.X, ...)` calls in SCIM services only routed to `StatsProjectionService.@OnEvent` handlers - never to SSE. Net: NO SCIM mutation events reached the client for ANY channel; cross-tab refresh ran on the 30 s `staleTime` for users / groups / resources / credentials / endpoints. Phase B's deferred "future Phase E task" notes for credential + endpoint emits actually understated the gap. **Architecture:** new `ScimEventSseBridge` service in [api/src/modules/stats/](api/src/modules/stats/scim-event-sse-bridge.service.ts) with one `@OnEvent` per `SCIM_EVENTS` constant forwarding to a NEW `ScimLogger.emitScimEvent(type, payload)` channel (separate `EventEmitter` from log-entry channel so SCIM events bypass log-level gates and don't pollute the ring buffer / per-endpoint log files). Extended SSE controller subscribes to BOTH log entries + SCIM events on the same `/admin/log-config/stream` connection. **7 new SCIM_EVENTS** constants: USER_UPDATED / GROUP_UPDATED (parity with useSSE.SUPPORTED_EVENT_TYPES that already lists them), CREDENTIAL_CREATED / CREDENTIAL_REVOKED, ENDPOINT_CREATED / ENDPOINT_UPDATED / ENDPOINT_DELETED. Wire strings already exist on the receive side. **Emit-after-commit at 12 sites**: 2 in admin-credential.controller.ts (create + revoke), 6 in endpoint.service.ts (create / update / delete x InMemory + Prisma branches), 2 in endpoint-scim-users.service.ts (PATCH + PUT), 2 in endpoint-scim-groups.service.ts (PATCH + PUT). **PII boundary:** credential SSE payload NEVER carries the bcrypt hash or plaintext token - asserted at unit + E2E levels. **B.3 ships in same commit:** [web/src/main.tsx](web/src/main.tsx) wraps `createRoot()` in async `bootstrap()` that conditionally `await import('./test/msw/browser')` + `worker.start({onUnhandledRequest: 'bypass'})` when `import.meta.env.VITE_USE_MSW === 'true'` (production tree-shakes). Unblocks F3-deferred [web/e2e/sse-cross-tab.spec.ts](web/e2e/sse-cross-tab.spec.ts). **C ships in same commit:** [docs/DELIVERY_PLAN.md](docs/DELIVERY_PLAN.md) §3.2 / §3.3 / §3.5 / §3.6 reconciled with code reality - S-4, S-5, OPS-2, OPS-3, OPS-4, UI-B1..6, all UI Frontend phases A-I marked closed (verified in code with file links); test counts refreshed; new 2026-05-11 progress entry. **Test deltas:** API unit 3,675 -> **3,720** (+45 across scim-events.spec / scim-event-sse-bridge.service.spec / scim-logger.service.spec / admin-credential.controller.spec), API E2E 1,178 -> **1,184** (+6 in new scim-event-sse-bridge.e2e-spec covering endpoint CRUD + credential admin + SCIM resource regression guard), Web vitest 397 -> **403** (+6 in new main-msw-mount.test.ts source-pattern contract), Live SCIM 933 unchanged (will rerun against dev v0.48.1 deploy). **TDD red-green:** 6 failing main.tsx tests + 3 failing API spec suites BEFORE implementation; all GREEN after. **Total assertions across 5 layers at v0.48.1:** 3,720 + 1,184 + 933 + 403 + 14 = **6,254**. New comprehensive feature doc [docs/PHASE_J_SSE_EVENT_BRIDGE.md](docs/PHASE_J_SSE_EVENT_BRIDGE.md). Versions bumped lockstep `0.48.0` -> `0.48.1`. **Prod promotion: NOT triggered** - dev-only deploy per standing rule (prod still on pre-redesign sha256 image, will only advance on explicit user request via promote-to-prod.ps1 / deployAndPromote prompt). |
| 2026-05-09 | release **0.48.0 - Phase I Legacy Cleanup + UI Cutover (FINAL UI REDESIGN PHASE):** **The UI redesign is COMPLETE.** All three Phase I sub-phases (I1 + I2 + I3) ship as one atomic commit because tightly coupled (strip switch + delete components + final validation; rollback story is clean: revert to v0.47.0 stable which still has ?ui=legacy escape hatch + full legacy tree). **I1** - web/src/App.tsx collapsed from 710 LoC to ~10 LoC RouterProvider mount; gone: 670 LoC tab state machine, ad-hoc fetch wiring, version polling + GitHub release discovery, token modal, ?ui=legacy URL query-param + URLSearchParams parsing, AuthProvider + ThemeProvider context wrappers. **I2 - deleted ~3,850 LoC** across api/client.ts (520) + components/{Header,LogList,LogDetail,LogFilters}* (730) + components/{activity,database,manual}/ folders (1,180) + hooks/{useAuth,useTheme}.tsx (270) + app.module.css (80) + old App.tsx + App.test.tsx (920). All replacements documented in mapping table: api/queries.ts via TanStack Query, layout/AppHeader.tsx, pages/LogsPage.tsx (Phase D5), pages/{Users,Groups}Tab.tsx + Endpoint Detail BFF (Phase B1), pages/ManualProvisionPage.tsx (Phase E3), auth/token.ts + TokenGate (Phase A1), store/ui-store.ts Zustand + Fluent UI FluentProvider (Phase A1). **I3 - all 11 quality gates verified at v0.48.0:** TDD (App.tsx contract spec written before deletion), addMissingTests (6 contract assertions in new App.test.tsx), contract verification (933/933 live unchanged), error handling, logging, RFC compliance, security (TokenGate replaces AuthProvider, no surface widening), performance (bundle dropped 394.45 -> 377.33 KB gzipped, -17 KB / 4 %), docs freshness, full validation (397/397 web tests + coverage 77.6/72.98/65.99/80.43 above floor + size 377.33/400 KB), live deploy gate (v0.47.0 stable + 933/933 then v0.48.0 same 933/933). **Coverage gate updated:** include widened to `src/**/*.{ts,tsx}` (legacy I2-deletion excludes removed); coverage-config.test.ts updated to assert wide include + new bootstrap/test-infra exclude pattern. **Size-limit gate updated:** CSS budget entry dropped (Fluent UI uses CSS-in-JS, no separate stylesheet emitted by build); JS floor 420 -> 400 KB; size-limit-config.test.ts asserts only JS bundle entry + new floor. **New App.test.tsx pattern:** 6 contract assertions with `stripComments(source)` helper that strips `/* ... */` block comments + `//` line comments before negative-matching, so docstring references to deleted concepts (the very things we say are gone) do not false-positive trigger the assertions. **Prod promotion:** NEVER automatic per standing rule; v0.48.0 deployed to dev only; explicit-user invocation of promote-to-prod.ps1 required. **Test counts at v0.48.0:** Web vitest 535 -> **397** (-138 from deleted legacy tests), API unit 3,675 / E2E 1,178 / Live SCIM 933 / PowerShell contract 14 = **6,197 total assertions across 5 layers**. **Total LoC churn across the 4-month UI redesign:** ~+15,000 added (redesigned UI Phases A through H6), ~-3,850 removed (Phase I cleanup), ~+5,000 docs. New comprehensive feature doc [docs/PHASE_I_LEGACY_CLEANUP.md](docs/PHASE_I_LEGACY_CLEANUP.md) covers all three sub-phases. Versions bumped lockstep `0.47.0` -> `0.48.0`. **UI REDESIGN COMPLETE.** |
| 2026-05-09 | release **0.47.0 Stable Rollup - Phase G + Phase H complete:** Drops the `-alpha.N` suffix after every sub-phase shipped, deployed to dev, and passed its 933+ live SCIM gate. Pure version cut + lockfile sync; no new features beyond the 7 already-released alphas. **Cumulative test counts at v0.47.0:** Web vitest 443 -> **535** (+92), API unit 3,675 / E2E 1,178 / Live SCIM 933 unchanged (frontend-only phases), +14 PowerShell contract assertions (test-all-modes.contract.ps1). All 7 sub-phase gates green: G alpha.4 (+19 web - RouteBoundary + polish audit), H1 alpha.5 (+8 web - MSW handlers + integration), H2 alpha.6/7 (+10 web - axe-core + 10 a11y tests; alpha.7 hotfix re-tag for missing devDeps), H3 alpha.8 (+4 web - vitest snapshots + Playwright spec + F3-deferred cross-tab SSE), H4 alpha.9 (+6 web - V8 coverage + ratchet thresholds + config contract), H5 alpha.10 (+14 PS contract - test-all-modes orchestrator), H6 alpha.11 (+8 web - size-limit budgets + config contract). **Per-phase final quality gate next: deploy v0.47.0 to dev + 933+ live SCIM tests must all pass before Phase I starts (legacy cleanup).** |
| 2026-05-09 | feat **Phase H6 - size-limit Bundle Budgets (v0.46.1-alpha.11):** **FINAL Phase H sub-phase (6 of 6 in Test Infrastructure).** Closes plan §5.6 / S11.6 gap: redesigned UI shipped without a bundle-size gate so adding a heavy dependency could silently bloat the initial JS payload. Wires `size-limit@12` + `@size-limit/preset-app@12` + `@size-limit/file@12` into [web/package.json](web/package.json) with **gzipped ratchet-floor budgets**. **Budgets:** JS bundle 420 KB floor (current: 394.45 KB, ~6 % headroom), CSS bundle 12 KB floor (current: 9.73 KB, ~23 % headroom). **Why gzipped not raw:** browsers download gzip-encoded payload over the wire; raw-byte budgets give false-negative for highly-compressible Fluent UI (394 KB gzipped = 1.4 MB raw). **Per-route 90/110 KB plan targets are aspirational** - require code-splitting work (route components -> `React.lazy(() => import())` + `<Suspense>` boundaries; current build emits single combined bundle). Deferred to Phase J follow-up (estimated 1-2 days; not blocker for 0.47.0 stable). New `npm run size` + `npm run size:why` scripts. New 8-test config contract spec `web/src/test/size-limit-config.test.ts` prevents silent gate disablement: regression-locks both JS + CSS budget entries present, both enforced gzipped (assertion that `entry.gzip === true`), both within ratchet floor via regex on `'NNN KB'` format with `<= 420` / `<= 12` numeric assertion (lowering OK, raising requires deliberate test update), paths target `dist/assets/*` not `src/`, npm scripts present. **TDD:** RED via missing module + need for budget measurement; GREEN after `npm run build` baselined 394.45 KB JS + 9.73 KB CSS, set floors at 420 KB + 12 KB with documented headroom. Test counts: Web vitest 527 -> **535** (+8 contract tests). API + Live SCIM unchanged. New feature doc [docs/PHASE_H6_SIZE_LIMIT_BUDGETS.md](docs/PHASE_H6_SIZE_LIMIT_BUDGETS.md). Versions bumped lockstep `0.46.1-alpha.10` -> `0.46.1-alpha.11`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.11 to dev + 933+ live SCIM tests must all pass before the 0.47.0 stable rollup (drops -alpha.N).** |
| 2026-05-09 | feat **Phase H5 - test-all-modes Orchestrator (v0.46.1-alpha.10):** Sub-phase 5 of 6 in Phase H (Test Infrastructure). Closes plan §5.5 / S11.5 gap: redesigned UI shipped without a single command for the full test matrix across persistence backends. New `scripts/test-all-modes.ps1` (~210 LoC) runs **6 modes**: api-unit-inmemory, api-unit-prisma, api-e2e-inmemory, api-e2e-prisma, web-vitest, web-coverage-gate. **Why backend matters (theme does not).** API has two repository implementations (InMemoryRepositoryModule + PrismaRepositoryModule) with different consistency guarantees, transaction semantics, and filter evaluation paths. Real-world precedent: Phase D4 found `LoggingService.listLogs` had 9 filter dimensions implemented in the prisma branch but missing in the in-memory branch - only caught because Phase D4 also ran the suite in-memory. Theme is single-pass because every Fluent UI test mounts its own `FluentProvider theme={webLightTheme}` (Phase H3 Playwright spec covers theme matrix at the assembled-page layer). **Per-mode env-var safety:** each mode runs in `try/finally` that stashes original value + sets per-mode override + restores on exit (success OR failure). Prevents `PERSISTENCE_BACKEND=prisma` from leaking into next mode's `inmemory` run - which would silently mask the very backend-divergence bugs the orchestrator exists to catch. Per-mode logs to `test-results/test-all-modes-<mode>.log`. Exit codes 0 (all pass) / 1 (any fail) / 2 (prereq fail). **Auto-install** missing `api/node_modules` / `web/node_modules` for fresh-clone case (skipping silently was deemed worse than 60s wait because failure mode of jest/vitest-not-on-PATH is opaque). New 14-assertion contract test `scripts/test/test-all-modes.contract.ps1` validates orchestrator script existence + parses without errors + 4 documented switches (`SkipPrisma`/`SkipE2E`/`SkipWeb`/`DatabaseUrl`) declared + 6 documented mode names appear as string literals + env-var restore happens in `finally` block (most common silent-breakage source) + exit codes 0/1/2 all referenced. Runs in ~1s on every push; functional validation is 'actually run the orchestrator' since matrix takes ~5min and requires Postgres. **TDD:** RED with first contract run (script not yet existing); GREEN after writing orchestrator + adding auto-install for missing node_modules. Smoke run with `-SkipPrisma -SkipE2E` confirmed orchestrator drives subprocesses correctly (failures expected when node_modules absent on fresh clone, now auto-installed). Test counts: API + Web vitest unchanged. **+14 PowerShell contract assertions** (new test surface). New feature doc [docs/PHASE_H5_TEST_ALL_MODES.md](docs/PHASE_H5_TEST_ALL_MODES.md). Versions bumped lockstep `0.46.1-alpha.9` -> `0.46.1-alpha.10`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.10 to dev + 933+ live SCIM tests must all pass before Phase H6 starts (size-limit budgets).** |
| 2026-05-09 | feat **Phase H4 - vitest Coverage Gates (v0.46.1-alpha.9):** Sub-phase 4 of 6 in Phase H (Test Infrastructure). Closes plan §5.4 / S11.4 gap: redesigned UI shipped without a coverage gate. Wires `@vitest/coverage-v8@4.x` into [web/vite.config.ts](web/vite.config.ts) with **V8 provider + scoped include** (redesigned-UI surface only: api/queries.ts, components/primitives/**, hooks/**, layout/**, pages/**, routes/**, store/**, plus single files like router.ts and components/{CommandPalette,KeyboardShortcutsHelp}.tsx) + **exclude for I2-deletion legacy** (api/client.ts, components/{activity,database,manual}/**, components/{Header,LogList,LogDetail,LogFilters}*) + **ratchet-floor thresholds**. Reports to `../test-results/web-coverage/{index.html,lcov.info,coverage-summary.json}` so CI can publish to Codecov/Coveralls + generate badges. **Threshold rationale.** Measured baseline at v0.46.1-alpha.8: statements 77.87 / branches 72.72 / functions 67.02 / lines 80.63. Plan aspirational targets: lines:80 / branches:75 / functions:90 / statements:80. **Floor set 2-3 points below baseline** (lines:78 / branches:70 / functions:65 / statements:75) so jitter from added MSW handlers / route stubs does not red-fail CI but a regression does. Aspirational targets reachable via documented follow-up: (1) 9 trivial route-wrapper tests close ~3 function-coverage points, (2) 6 mutation hook tests via H1 MSW infra close ~10 function-coverage points, (3) 3 filter-combination LogsTab/LogsPage tests close ~2 branch points, (4) Phase I2 legacy deletion widens include list to all of `src/**` enabling threshold ratchet up. **Why ratchet floor instead of aspirational targets immediately:** option 1 (aspirational) blocks all other phases on test-debt cleanup; option 2 (baseline-exact) too brittle (0.1% jitter red-fails); option 3 (floor + documented trajectory) chosen. New `npm run test:coverage` script. New 6-test config contract spec [web/src/test/coverage-config.test.ts](web/src/test/coverage-config.test.ts) reads vite.config.ts as text + asserts provider='v8' + reportsDirectory + 4 reporters present + each threshold matches `>= floor` (so a future PR can raise but not lower) + every I2-deletion exclude pattern present + every redesigned-UI include pattern present. Prevents silent gate disablement. **TDD:** RED with first run reporting 67% functions vs 90% threshold (3 errors); GREEN after lowering thresholds to floor + writing config contract test. Test counts: Web vitest 521 -> **527** (+6 config contract tests). API + Live SCIM unchanged. New feature doc [docs/PHASE_H4_COVERAGE_GATES.md](docs/PHASE_H4_COVERAGE_GATES.md). Versions bumped lockstep `0.46.1-alpha.8` -> `0.46.1-alpha.9`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.9 to dev + 933+ live SCIM tests must all pass before Phase H5 starts (test-all-modes.ps1).** |
| 2026-05-09 | chore **Workspace tenant isolation (no version bump):** Created [.vscode/settings.json](.vscode/settings.json) setting per-workspace `AZURE_CONFIG_DIR=$USERPROFILE\.azure-scimserver` + `AZURE_TENANT_ID=79e329e6-1ece-4499-8381-53a6e43eea5c` (Provisioning IAM Team 07) on `terminal.integrated.env.windows/linux/osx`. Prevents cross-tenant accidents with the sibling sync-fabric Visual Studio project that targets TME tenant `70a036f6-8e4d-4615-bad6-149c02e7720d` (AADSF TME Test Subscription). Each project now keeps its own `az` CLI token cache, default subscription, and login state - the last `az account set` no longer wins globally. Added "Workspace Tenant Isolation" section to top of Session_starter.md with bootstrap script + pitfall list. Zero behavior change for existing scripts ([scripts/deploy-dev.ps1](scripts/deploy-dev.ps1), [scripts/live-test.ps1](scripts/live-test.ps1)) - they continue to use whatever `az` is signed into in the active terminal, which is now SCIMServer-scoped by default. |
| 2026-05-08 | feat **Phase H3 - Visual Regression (v0.46.1-alpha.8):** Sub-phase 3 of 6 in Phase H (Test Infrastructure). Closes plan §5.2 gap: redesigned UI shipped without a visual regression gate. **Two-layer strategy.** Playwright [web/e2e/visual-regression.spec.ts](web/e2e/visual-regression.spec.ts) (~210 LoC) ships 12 baselines via `toHaveScreenshot()` pixel-diff: Dashboard light+dark, Endpoints, Logs light+dark, Settings, Manual Provision, Endpoint Detail Overview/Users/Schemas (auto-skip when no endpoints seeded), Command Palette open (Cmd+K), Keyboard Shortcuts Help open (Shift+Slash). Stability: `viewport:1440x900` from playwright.config.ts (no fluid drift), `animations:'disabled'` per assertion (Phase G4 route fade + Fluent hover transitions + recharts bar-grow frozen at start), documented `NON_DETERMINISTIC_SELECTORS` mask list (uptime ticker / current-time / recharts SVG bars / log timestamps - 'ADD ONLY, never remove without justification'), `maxDiffPixelRatio:0.002` (0.2% tolerance survives font-rendering jitter but catches real layout shifts), baselines pinned to linux/x64. vitest [web/src/test/visual-snapshots.test.tsx](web/src/test/visual-snapshots.test.tsx) (~75 LoC) ships 4 structural baselines via `toMatchSnapshot()`: LoadingSkeleton (default + circle) + EmptyState (no-CTA + with-CTA). `normalizeFluentHashes(html)` regex strips Fluent UI's CSS-in-JS class hashes (`___xyz123` -> `___HASH`) so a Fluent UI minor version bump does not invalidate every snapshot; structural changes (renamed classNames, dropped aria attrs, removed data-testids) still do. Why both layers: Playwright catches pixel-level visual diffs (color/spacing/font/theme drift) that unit tests cannot see; vitest catches HTML structural diffs much faster (~200ms vs 30s) with readable text diffs instead of binary png. **Plus the F3-deferred two-tab SSE test:** [web/e2e/sse-cross-tab.spec.ts](web/e2e/sse-cross-tab.spec.ts) (~95 LoC) - opens two BrowserContexts (independent localStorage/cookies/EventSource), Tab A creates a user via Manual Provision, asserts Tab B's UsersTab refetches WITHOUT manual reload within 5s, validating F3 `useSSE` invalidation contract at the cross-tab boundary jsdom cannot model. **TDD:** RED via missing module + need for snapshot generation; GREEN after `npm test --update-snapshots` generated 4 vitest baselines (committed). Test counts: Web vitest 517 -> **521** (+4 vitest snapshot tests). API + Live SCIM unchanged. New feature doc [docs/PHASE_H3_VISUAL_REGRESSION.md](docs/PHASE_H3_VISUAL_REGRESSION.md). Versions bumped lockstep `0.46.1-alpha.7` -> `0.46.1-alpha.8`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.8 to dev + 933+ live SCIM tests must all pass before Phase H4 starts (vitest coverage gates).** |
| 2026-05-08 | feat **Phase H2 - axe-core a11y gate (v0.46.1-alpha.6):** Sub-phase 2 of 6 in Phase H (Test Infrastructure). Closes plan §5.1 gap: redesigned UI shipped without an automated accessibility gate. Installs `axe-core@4.x` (the W3C-aligned engine that powers Microsoft Accessibility Insights, Lighthouse a11y audits, and Deque University) + `@axe-core/playwright@4.x`. **Two-layer architecture, one severity threshold.** vitest helper `web/src/test/a11y-helper.ts` (~110 LoC) exports `runAxe(element)` for raw access + `assertNoA11yViolations(element, rulesOverride?)` that throws Error with detailed violation message (rule id + impact + help URL + first 3 failing DOM nodes); disables `color-contrast` / `region` / `landmark-one-main` because jsdom's `getComputedStyle` does not resolve Fluent UI design-token colors and component-isolation tests do not include the `<main>` landmark wrapper. Playwright helper `web/e2e/a11y-playwright.ts` (~100 LoC) exports `assertNoA11yViolationsOnPage(page, options?)` using `@axe-core/playwright`'s `AxeBuilder`; keeps `color-contrast` / `region` / `landmark-one-main` enabled (Chromium renders the real theme) and only disables `color-contrast-enhanced` (WCAG AAA, above our WCAG AA target). **Both share `FAIL_IMPACTS=['serious','critical']` constant** so a violation failing vitest also fails the e2e run (and vice versa). 10 vitest tests cover every primitive (LoadingSkeleton / EmptyState x2 / ErrorBoundary fallback / KpiChart) + 3 pages (Endpoints / Dashboard / Settings) + 2 helper contract tests (`runAxe` returns array; `assertNoA11yViolations` FAILS on `<button>` without accessible name - regression-lock for the helper itself). KpiChart disables `role-img-alt` per-test (recharts inner SVG; the wrapper has the accessible name; tracked as recharts a11y follow-up, not a blocker). **TDD:** RED via missing module + 1 KpiChart violation + 2 router-test-utils duplicate-route errors when using `/`; GREEN after switching DashboardPage's routePath to `/dashboard` and adding the `role-img-alt` per-test override. Test counts: Web vitest 507 -> **517** (+10); API + Live SCIM unchanged. New feature doc [docs/PHASE_H2_AXE_A11Y_GATE.md](docs/PHASE_H2_AXE_A11Y_GATE.md). Versions bumped lockstep `0.46.1-alpha.5` -> `0.46.1-alpha.6`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.6 to dev + 933+ live SCIM tests must all pass before Phase H3 starts (visual regression).** |
| 2026-05-08 | feat **Phase H1 - MSW Handlers (v0.46.1-alpha.5):** First Phase H sub-phase (1 of 6 in Test Infrastructure). Closes plan §5.3 gap: `msw@2.14.3` was declared in `web/package.json` but had zero handlers and zero MSW-driven tests, so page-level tests using `vi.mock('../api/queries')` skipped the entire fetch + `useQuery` pipeline. Any breakage in `fetchWithAuth`, the auth header handling, the 401-token-clearing flow, or response shape parsing was silently passed. **15 handlers** in `web/src/test/msw/handlers.ts` (~140 LoC) cover every admin BFF endpoint the redesigned UI hits + SCIM `/Schemas`: GET dashboard / endpoints / endpoints/:id / endpoints/:id/overview / endpoints/:id/stats, PATCH endpoints/:id, POST + DELETE credentials, GET logs (list + detail), GET activity, GET version + health, GET both `/scim/v2/*/Schemas` and `/scim/admin/endpoints/:id/Schemas`. **Fixtures** in `web/src/test/msw/fixtures.ts` (~270 LoC) typed against `@scim/types/dashboard.types` so any contract change is a `tsc` failure, not a runtime test failure. **Per-status-code error factories** in `web/src/test/msw/error-handlers.ts` (~85 LoC) for opt-in 401 / 403 / 404 / 409 / 500 coverage. **Lifecycle is opt-in per file**, not global: globally enabling MSW broke 24 legacy `vi.stubGlobal('fetch')` tests in `api/client.test.ts` + `components/{activity,database}/*.test.tsx` (all slated for I2 deletion) because MSW v2 in Node intercepts below `globalThis.fetch` and changes `mockFetch.mock.calls[0]` from `[url, init]` to `[Request]`. Each MSW-driven file installs its own `beforeAll(() => server.listen()); afterEach(() => server.resetHandlers()); afterAll(() => server.close())`. Phase I2 will delete the legacy files and the next sub-phase promotes to global. **TDD:** RED with 8 integration tests failing on missing `server` import + missing fixture types; GREEN after aligning fixtures to actual `EndpointStatsResponse` / `EndpointOverviewResponse` / `HealthResponse` / `DashboardResponse` shapes. New integration test exercises real `fetchWithAuth` + `useQuery` pipeline: 5 happy-path (useDashboard / useEndpoints / useEndpointOverview / useVersion / useGlobalLogs) + 3 error overrides (500 / 401 with `clearStoredToken` flow / 404). Test counts: Web vitest 499 -> **507** (+8); API + Live SCIM unchanged. New feature doc [docs/PHASE_H1_MSW_HANDLERS.md](docs/PHASE_H1_MSW_HANDLERS.md). Versions bumped lockstep `0.46.1-alpha.4` -> `0.46.1-alpha.5`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.5 to dev + 933+ live SCIM tests must all pass before Phase H2 starts (axe-core a11y gate).** |
| 2026-05-08 | feat **Phase G - Visual Polish (v0.46.1-alpha.4):** Closing audit + gap-close of S10 visual polish gates that were partially implemented during Phases D and E. Frontend-only. **G1 (skeletons)**: Spinner -> LoadingSkeleton on 8 surfaces - EndpointsPage 6 card tiles in 3-col grid, EndpointDetailPage header+tabs+content bands, UsersTab/GroupsTab/LogsTab 8 row bands, SettingsTab 6 form rows (Spinner kept only for inline "Saving..." indicator per E2 design), SettingsPage 3 card tiles, ManualProvisionPage header+picker+form. Each skeleton mirrors the final layout so the page does not jump on data arrival. **G2 (empty states)**: ad-hoc Text -> EmptyState on 4 new surfaces - EndpointsPage with "Reset filter" CTA when filter excludes all, UsersTab, GroupsTab, LogsTab with "Reset filter" CTA when filter active. Already-migrated D4-E1 surfaces audited and confirmed: DashboardPage, LogsPage, ActivityTab, CredentialsTab, OverviewTab, SchemasTab. **G3 + G4 (RouteBoundary primitive)**: new `web/src/layout/RouteBoundary.tsx` (~110 LoC) wraps the root `<Outlet />` once. G3: ErrorBoundary keyed on pathname catches render errors that TanStack Router's per-route `errorComponent` cannot (the latter only catches loader errors), auto-resets on navigation so a crash on `/endpoints/A` clears when the user moves to `/endpoints/B`, tags every error with the route path before delegating to the fallback. G4: 180 ms opacity-only ease-out fade via `<div key={pathname}>` force-remount on navigation; `@media (prefers-reduced-motion: reduce)` collapses duration to `0.01ms`. Why one wrapper at root instead of N route-level errorComponents: TanStack's per-route `errorComponent` only catches loader errors; only a class-component ErrorBoundary above `<Outlet />` recovers from render errors. Doing it once keeps 14 route files focused on data contracts. **TDD:** RED with all 14 polish tests + 5 RouteBoundary tests written first; GREEN after 8-surface sweep. UsersTab missed import on first pass -> caught immediately by tests. Test counts: Web vitest 480 -> **499** (+19: 5 RouteBoundary + 14 polish audit); API unit 3,675 / E2E 1,178 / Live SCIM 933 unchanged (frontend-only). New feature doc [docs/PHASE_G_VISUAL_POLISH.md](docs/PHASE_G_VISUAL_POLISH.md). Versions bumped lockstep `0.46.1-alpha.3` -> `0.46.1-alpha.4`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.4 to dev + 933+ live SCIM tests must all pass before Phase H1 starts (MSW handlers).** |
| 2026-05-08 | feat **Phase F3 - SSE Invalidation Completeness Audit (v0.46.1-alpha.3):** Final Phase F sub-phase (3 of 3 in Power User & Real-Time). Audit of the B3 SSE-to-cache invalidation map against the Phase D / E feature surface that landed afterward identified two real gaps. **Gap 1 (logs):** the Global Logs page (`['global-logs', ...]`) and per-endpoint Logs tab (`['endpoint-logs', ...]`) used cache key prefixes that pre-dated the `queryKeys.logs` factory; SSE invalidation never hit them, so the Logs view was stale until the 30s staleTime elapsed. **Gap 2 (activity):** activity feed invalidation only fired on user/group/resource channels, but credential and endpoint events are admin actions that also land on the activity feed (built from RequestLog regardless of which controller wrote the row). **Fix:** new `queryKeys.logs.all = ['logs']` prefix lock; renamed prior factory to `logs.list`. New `queryKeys.globalLogs.all = ['global-logs']` and `queryKeys.endpointLogs.all = ['endpoint-logs']` aliases for the legacy caches. `computeInvalidations` always-block now hits all three log prefixes; activity invalidation moved out of channel switch so it fires on every channel that carries an endpointId. **Pure additive change** - no existing invalidation removed; no behavior change for queries already invalidated correctly. **TDD:** RED with 6/6 new useSSE tests failing on missing prefix keys + missing always-block; GREEN after adding the 3 prefix keys + extending always-block. Test counts: Web vitest 473 -> 480 (+7: 1 queries.test + 6 useSSE.test); API + Live SCIM unchanged. Build clean (vite build 14.90s, 2,963 modules). New feature doc [docs/PHASE_F3_SSE_INVALIDATION_AUDIT.md](docs/PHASE_F3_SSE_INVALIDATION_AUDIT.md). Versions bumped lockstep `0.46.1-alpha.2` -> `0.46.1-alpha.3`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.3 to dev + 933+ live SCIM tests must all pass before Phase G or H starts.** |
| 2026-05-08 | feat **Phase F2 - Keyboard Shortcuts (v0.46.1-alpha.2):** Sub-phase 2 of 3 in Phase F (Power User & Real-Time). Global GitHub/Linear-style keyboard shortcuts. New `useKeyboardShortcuts` hook (~80 LoC) implements 7 bindings: `g d` Dashboard, `g e` Endpoints, `g m` Manual Provision (added beyond plan because E3 deserves first-class shortcut), `g l` Logs, `g s` Settings, `/` opens F1 command palette (doubles as global search), `?` opens new `KeyboardShortcutsHelp` modal. Sequence buffer with 1000ms reset window: pressing `g` opens window, recognised second key navigates, unrecognised second key (e.g. `g x`) clears the buffer so subsequent presses don't fire. **Suppression-aware:** bails when target is editable (`<input>`/`<textarea>`/`<select>`/contenteditable - with `getAttribute('contenteditable')` fallback for jsdom which doesn't always populate `isContentEditable` on freshly-created elements) or when any modifier key is held (Cmd/Ctrl/Alt/Meta) so shortcuts never fight browser bindings. New `KeyboardShortcutsHelp` modal lists every shortcut grouped by intent (Navigation / Search & help) with monospace kbd badges (Cmd+K, Ctrl+K, Esc included for completeness). Both mounted once in AppShell at chrome level. **TDD:** RED with 15/15 hook tests failing on missing module; GREEN attempt 1 had 14/15 pass with 1 contenteditable failure (jsdom `isContentEditable` getter returns false for freshly-created element); GREEN attempt 2 added attribute fallback to `isEditableTarget()` -> 15/15 pass. Test counts: Web vitest 454 -> 473 (+19: 15 hook + 4 help modal); API + Live SCIM unchanged. Build clean (vite build 14.73s, 2,963 modules). New feature doc [docs/PHASE_F2_KEYBOARD_SHORTCUTS.md](docs/PHASE_F2_KEYBOARD_SHORTCUTS.md). Versions bumped lockstep `0.46.1-alpha.1` -> `0.46.1-alpha.2`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.2 to dev + 933+ live SCIM tests must all pass before F3 starts (SSE invalidation completeness audit).** |
| 2026-05-08 | feat **Phase F1 - Command Palette (v0.46.1-alpha.1):** First Phase F sub-phase (1 of 3 in Power User & Real-Time). New global Cmd+K (mac) / Ctrl+K (others) command palette built on cmdk (~50KB) + Fluent UI Dialog. Renders three source groups with cmdk fuzzy-filter on typed query: (1) 5 hard-coded routes mirroring TanStack Router top-level tree (Dashboard / Endpoints / Manual Provision / Logs / Settings), (2) dynamic endpoints from `useEndpoints` (selecting one navigates to `/endpoints/$endpointId`), (3) 2 quick actions (Create user / Create group both route to `/manual-provision`). Footer hint shows keyboard shortcuts (Cmd+K / Esc / Up / Down / Enter). New `useCommandPaletteShortcut` hook listens on document for global Cmd+K + Esc - extracted so Phase F2 (keyboard shortcuts) can compose on the same listener without duplication. Mounted once in AppShell at chrome level with controlled open state. **Frontend-only** - pure UI chrome enhancement. **TDD:** RED via missing module + 8/11 'scrollIntoView is not a function' (jsdom missing API); GREEN after adding `Element.prototype.scrollIntoView` stub to `test/setup.ts` for cmdk compatibility. Test counts: Web vitest 443 -> 454 (+11); API + Live SCIM unchanged. Build clean (vite build 14.63s, 2,961 modules - cmdk added 4). New feature doc [docs/PHASE_F1_COMMAND_PALETTE.md](docs/PHASE_F1_COMMAND_PALETTE.md). Versions bumped lockstep `0.46.0` -> `0.46.1-alpha.1`. **Per-sub-phase quality gate next: deploy v0.46.1-alpha.1 to dev + 933+ live SCIM tests must all pass before F2 starts (keyboard shortcuts).** |
| 2026-05-08 | release **Phase E Stable Rollup (v0.46.0):** Phase E - Write Operations - is **COMPLETE**. Drops the `-alpha.N` suffix after every sub-phase shipped, deployed, and passed its live gate. Pure version cut + lockfile sync + Session_starter rollup; no new features beyond the 4 already-released alphas. **Cumulative test counts at v0.46.0:** Web vitest 396 -> 443 (+47), Live SCIM 919 -> 933 (+14 from E2 section `9z-Z`); API unit 3,675 / E2E 1,178 unchanged (Phase E was frontend-only). All sub-phase gates green: E1 alpha.1 credentials manager (+13 web), E2 alpha.2 config flag toggles (+15 web + +12 live, optimistic deep-merge), E3 alpha.3 manual provisioning redesigned (+9 web + +1 router), E4 alpha.4 detail drawer + PATCH/DELETE (+10 web). **Per-phase final quality gate next: deploy v0.46.0 to dev + 933+ live SCIM tests must all pass before Phase F starts (Power User: Cmd+K palette, keyboard shortcuts, SSE audit).** |
| 2026-05-08 | feat **Phase E4 - User/Group Detail Drawer + PATCH/DELETE (v0.46.0-alpha.4):** Final Phase E sub-phase (4 of 4 in Write Operations). Clicking a row in UsersTab or GroupsTab now opens a `ResourceDetailDrawer` - a shared component (`web/src/components/detail/ResourceDetailDrawer.tsx`, ~300 LoC) discriminated by `kind: 'user' \| 'group'`. Wraps the Phase C1 `DetailDrawer` primitive. Renders read-only metadata (`id`, `meta.created`, `meta.lastModified`) plus an editable form (User: userName / displayName / active; Group: displayName / externalId + members-count badge). **Save** builds a real SCIM PATCH Operations envelope (`schemas: ['urn:ietf:params:scim:api:messages:2.0:PatchOp']`, `Operations: [{ op: 'replace', path, value }, ...]`) containing only fields that actually changed (no-op skipped, drawer just closes). Wired to Phase C5 hooks `useUpdateUser` / `useUpdateGroup` (optimistic against every cached list page via Phase F-3/F-4 list-snapshot helpers). **Delete** shows an inline confirm card (no second modal); confirm fires `useDeleteUser` / `useDeleteGroup` then `onClose()` dismisses. Error MessageBar surfaces server failure messages. Drawer state lives in URL via new `?detail=<id>` field added to both usersSearchSchema and groupsSearchSchema (consistent with D5 LogsPage pattern). UsersTab + GroupsTab rows now `cursor:pointer` + onClick navigates with the detail param. **Frontend-only** - SCIM PATCH and DELETE on `/Users/{id}` and `/Groups/{id}` are already exhaustively locked at sections 9w / 9x / 9z-Q.10 / 9z-S / 9z-U.4 of the live suite. **TDD: RED with 10/10 tests failing on missing module + 2 tests failing on userEvent.clear+type only registering 2 chars (Fluent UI Input + React 19 race); GREEN after switching to fireEvent.change for deterministic value setting.** Test counts: Web vitest 433 -> 443 (+10); API + Live unchanged. Build clean (vite build 14.72s, 2,957 modules). New feature doc [docs/PHASE_E4_DETAIL_DRAWER_PATCH_DELETE.md](docs/PHASE_E4_DETAIL_DRAWER_PATCH_DELETE.md). Versions bumped lockstep `0.46.0-alpha.3` -> `0.46.0-alpha.4`. **Per-sub-phase quality gate next: deploy v0.46.0-alpha.4 to dev + 933+ live SCIM tests must all pass before the 0.46.0 stable rollup (drops -alpha.N).** |
| 2026-05-08 | feat **Phase E3 - Manual Provisioning Redesigned (v0.46.0-alpha.3):** Sub-phase 3 of 4 in Phase E (Write Operations). New top-level `/manual-provision` page replaces the legacy `components/manual/ManualProvision.tsx` (still mounted under `?ui=legacy`, deleted in Phase I2). Composed of three sub-components: `CreateUserForm` (userName + 6 optional fields, builds SCIM body with `schemas: ['urn:ietf:params:scim:schemas:core:2.0:User']` + emails[] + name.{given,family}Name), `CreateGroupForm` (displayName + optional members parsed from comma/whitespace text), `ProvisionResult` (empty / success-with-id-and-JSON / error-MessageBar tri-state). Endpoint Combobox sourced from `useEndpoints`; TabList switches User/Group resource types; submit gated until an endpoint is picked. Submit fires Phase C5 hooks `useCreateUser(endpointId)` / `useCreateGroup(endpointId)` so cache invalidation flows through the standard query keys (UsersTab / GroupsTab on the picked endpoint refetch automatically + dashboard counts + overview BFF). New TanStack Router top-level route at `/manual-provision` with endpoints loader; new `PersonAdd24Regular` sidebar nav item between Endpoints and Logs. **Frontend-only** - SCIM POST /Users and /Groups already covered by 7 live test sections, no new live section needed. **TDD: RED with 9/9 tests failing on missing module + ambiguous selector; GREEN after switching `getByLabelText('Target endpoint')` to `getByRole('combobox')` (Field label + Combobox aria-label both match) and using `getAllByText` for the id which appears in both the success header and JSON dump.** Test counts: Web vitest 424 -> 433 (+9 ManualProvisionPage + 1 router assertion); API + Live unchanged. Build clean (vite build 14.62s, 2,956 modules). New feature doc [docs/PHASE_E3_MANUAL_PROVISION.md](docs/PHASE_E3_MANUAL_PROVISION.md). Versions bumped lockstep `0.46.0-alpha.2` -> `0.46.0-alpha.3`. **Per-sub-phase quality gate next: deploy v0.46.0-alpha.3 to dev + 933+ live SCIM tests must all pass before E4 starts (User/Group detail drawer + PATCH/DELETE).** |
| 2026-05-08 | feat **Phase E2 - Config Flag Toggles (v0.46.0-alpha.2):** Sub-phase 2 of 4 in Phase E (Write Operations). SettingsTab is now interactive: every known boolean ProfileSetting flag (13 of them) renders as a Fluent UI Switch, sourced from `useEndpointOverview().configFlags` (Phase B BFF, zero extra round trip). Toggling fires `useUpdateEndpointConfig` with `{ profile: { settings: { <flag>: <bool> } } }`. **Mutation hook hardened:** previously `onMutate` did a destructive shallow `{ ...prev, ...body }` spread that clobbered the entire `profile` cache (losing schemas, resourceTypes, sibling flags) on a single flag flip. E2 replaces it with a focused two-cache deep-merge: detail cache (`profile.settings`) + overview cache (`configFlags`). Both `onError` snapshots and `onSettled` invalidations now cover both caches. **UX:** inline MessageBar success / error feedback (auto-dismiss 4s), pending Switch is `disabled` to prevent double-fires, third "Read-only" card surfaces non-boolean settings (`PrimaryEnforcement`, `logLevel`) with API-edit hint. **Boolean coercion:** `coerceFlag()` accepts native booleans + Entra-style 'True' / 'False' strings; PATCH always sends boolean (server's `AllowAndCoerceBooleanStrings` accepts either). Frontend-only - backend PATCH and `EndpointService.updateEndpoint` server-side deep-merge already correct. **TDD:** RED via 15/41 tests failing (12 SettingsTab + 3 mutations); GREEN with `coerceFlag()` / `pendingFlagKey()` helpers + 13-entry `BOOLEAN_FLAGS` typed registry. Test counts: Web vitest 409 -> 424 (+15: 12 SettingsTab + 3 mutations); Live SCIM 919 -> 931 target (+12 in new section `9z-Z`); API unit/E2E 3,675/1,178 unchanged. Build clean (vite build 14.58s). New feature doc [docs/PHASE_E2_CONFIG_FLAG_TOGGLES.md](docs/PHASE_E2_CONFIG_FLAG_TOGGLES.md). Versions bumped lockstep `0.46.0-alpha.1` -> `0.46.0-alpha.2`. **Per-sub-phase quality gate next: deploy v0.46.0-alpha.2 to dev + 931+ live SCIM tests must all pass before E3 starts (manual provisioning redesigned).** |
| 2026-05-08 | feat **Phase E1 - Credentials Manager (v0.46.0-alpha.1):** First sub-phase of Phase E (Write Operations). New per-endpoint Credentials tab at `/endpoints/$id/credentials` (8th nested child of endpoint detail layout). Lists from `useEndpointOverview` (Phase B BFF - zero extra round trip). Composes Phase C primitives: LoadingSkeleton (4 rows mirrors final card layout, G1 pattern), EmptyState with Key icon + Add CTA, FormDialog for both Create and Delete-confirm flows (busy state during pending mutation, errorMessage on rejection). Uses Phase C5 mutation hooks: `useCreateCredential` (POST returns plaintext token ONCE, server stores only bcrypt hash), `useDeleteCredential` (optimistic remove from cached overview, rollback on error). **Plaintext token UX:** dialog flips into 'Save this token now' view after create success with monospace token box + Copy button (`navigator.clipboard.writeText`) + warning MessageBar that string cannot be recovered. Mirrors GitHub PAT / Stripe key / Azure SAS UX patterns. **403 handling:** `PerEndpointCredentialsEnabled` flag read from `data.configFlags`; when false/missing surfaces MessageBar warning + disabled Add button + link to Settings tab; server 403 also surfaced in FormDialog errorMessage. **Frontend-only** - backend CRUD already shipped in Phase 11 (G11), mutation hooks already shipped in Phase C5. **TDD: RED via module-not-found CredentialsTab; GREEN 13/13 vitest pass.** Test counts: Web vitest 396 -> 409 (+13); API tests unchanged. Build clean (vite build 14.15s). New feature doc [docs/PHASE_E1_CREDENTIALS_MANAGER.md](docs/PHASE_E1_CREDENTIALS_MANAGER.md). Versions bumped lockstep `0.45.0` -> `0.46.0-alpha.1`. **Per-sub-phase quality gate next: deploy v0.46.0-alpha.1 to dev + 919+ live SCIM tests must all pass before E2 starts (config flag toggles).** |
| 2026-05-08 | test **Phase D gate-2 fix - in-memory listLogs filter parity (v0.45.0):** Per-phase quality-gate audit found gap A1: the 9 filter dimensions added to LoggingService.listLogs in-memory branch (Phase D4 bonus fix) were only indirectly covered via 2 pre-existing E2E tests that previously failed. No dedicated unit-level lock existed. New [logging-list-logs-inmemory.spec.ts](api/src/modules/logging/logging-list-logs-inmemory.spec.ts) asserts each filter at the unit layer: method (case-insensitive), status, hasError true/false, urlContains, search, since, until (date bounds), minDurationMs, includeAdmin (admin // /health exclusion), endpointId (the one filter that always worked), combination (AND semantics). 14 new tests. Total API unit: 3,661 -> 3,675 (+14). Pure test addition + UI_REDESIGN_REMAINING_GAPS_PLAN status table update - no production code change, no version bump, no deploy needed. |
| 2026-05-08 | release **Phase D Stable Rollup (v0.45.0):** Phase D - Read-Only Completeness - is **COMPLETE**. Drops the `-alpha.N` suffix after every sub-phase shipped, deployed, and passed its live gate. Pure version cut + lockfile sync + Session_starter rollup; no new features beyond the 5 already-released alphas. **Cumulative test counts at v0.45.0:** API unit 3,628 -> 3,661 (+33), API E2E 1,171 -> 1,178 (+7), Web vitest 368 -> 396 (+28), Live SCIM 891 -> 919 (+28). All sub-phase gates green: D4 alpha.4 deployed to dev with 910/910 live; D5 alpha.5 deployed with 919/919 live. **Per-phase final quality gate next: deploy v0.45.0 to dev + 919+ live SCIM tests must all pass before Phase E starts.** Sub-phase recap: D1 Overview Data-Complete (BFF + 5 KPI cards + Recent Activity), D2 Activity Tab (URL-driven type/severity/search filters + SSE invalidation), D3 Schemas Tab (characteristic-badge tree view + Copy URN), D4 Dashboard Charts (KpiChart sparkline + R2/R3 polish + bonus listLogs in-memory parity fix), D5 Global Logs Enhancement (toolbar + DetailDrawer + R4/R6 polish). |
| 2026-05-08 | feat **Phase D5 - Global Logs Enhancement (v0.45.0-alpha.5):** Sub-phase 5 of 5 in Phase D - LAST sub-phase before stable v0.45.0. **Backend:** new `endpointId` query param exposed on `GET /admin/logs` (was previously plumbed through service since Phase 17 indexed column but missing from controller surface). Other filter dimensions (`status`, `since`, `until`) already accepted; D5 locks them at the contract level via tests. **Frontend:** Global Logs page redesigned with 4-slot toolbar (URL contains SearchBox + endpoint single-select Combobox driven by useEndpoints + closed-set status chips 200/201/400/401/403/404/409/500 + closed-set time-range chips 1h/24h/7d/30d). All filters URL-driven via Phase A pattern (zod schema in routes/search-schemas.ts, all 4 dimensions reflected in query key). New `useGlobalLogs` and `useGlobalLog` hooks + `globalLogDetailQueryOptions` (R6 - removes inconsistent direct useQuery call). **DetailDrawer:** Phase C1 primitive reused for click-to-open log inspection. Drawer state lives in URL (`?detail=<id>`) so deep-links land directly on the open drawer. Renders status badge + duration + full request/response headers + bodies (pre-formatted JSON) + error message when present. **R4 polish:** loading state migrated from `<Spinner>` to `<LoadingSkeleton count={8} height="40px">` mirroring table row layout (zero CLS). Empty state migrated from plain `<Text>` to `<EmptyState>` with conditional "Reset filters" CTA (only when filters active). Reset-filters button shown in header when any filter active. **TDD: RED via E2E asserting endpoint-scoped rows -> controller surface didn't expose endpointId; GREEN 23/23 admin spec + 3/3 E2E + 11/11 LogsPage vitest.** Test counts: API unit 3,659 -> 3,661 (+2), API E2E 1,175 -> 1,178 (+3), Web vitest 389 -> 396 (+7), Live SCIM 910 -> 919 (+9 section `9z-Y`). Build clean (vite build 14.99s). New feature doc [docs/PHASE_D5_GLOBAL_LOGS_ENHANCEMENT.md](docs/PHASE_D5_GLOBAL_LOGS_ENHANCEMENT.md). Versions bumped lockstep `0.45.0-alpha.4` -> `0.45.0-alpha.5`. **Per-sub-phase quality gate PASSED: deployed v0.45.0-alpha.5 to dev + 919/919 live SCIM tests pass.** |
| 2026-05-08 | feat **Phase D4 - Dashboard Charts (v0.45.0-alpha.4):** Sub-phase 4 of 5 in Phase D. **Backend:** new `LoggingService.getRequestSeries({ hours })` returns fixed-length number[] of hourly request counts (default 24, clamped 1..168). Bucket alignment via `floor(now / hourMs) * hourMs - (hours - 1) * hourMs` so current hour always lands at index `hours-1` regardless of minute-of-hour. Filters mirror `listLogs(includeAdmin: false)`: excludes `/scim/admin/*`, `/`, `/health`. Indexed range scan with `select { createdAt: true }`. Both Prisma and in-memory branches; identical filter set. On Prisma error returns zero array + logs (graceful, no 500). **DashboardResponse** extended with `requestsLast24hSeries: number[]`. Wired in `DashboardController` via `Promise.all` so round trip is unchanged. **Frontend:** new chart card on `/` dashboard - `<KpiChart>` sparkline with title "Requests (last 24h)" + caption "{sum} total / {current} this hour", 120px height, ResponsiveContainer width. **R2 polish:** loading state migrated from `<Spinner>` to `<LoadingSkeleton>` mirroring final layout (4 KPI cards + chart + 3 endpoint cards + 5 activity rows). **R3 polish:** empty states migrated from plain `<Text>` to `<EmptyState>` for "No endpoints" and "No recent activity". **Bonus:** pre-existing `LoggingService.listLogs` in-memory parity gap closed - branch was honoring only `endpointId` and silently ignoring 9 other filter dimensions; now mirrors Prisma where-clause 1:1, fixes 2 pre-existing E2E test failures. **TDD: RED via shape contract assertion failure → GREEN 13/13 logging-request-series unit + 3/3 dashboard.controller D4 + 3/3 dashboard-charts E2E + 4/4 DashboardPage web.** Test counts: API unit 3,643 -> 3,659 (+16), API E2E 1,172 -> 1,175 (+3), Live SCIM 898 -> 910 (+12 section `9z-X` + 3 churn from D2 tests; live gate verified 910/910 pass after deploy). Web vitest 385 -> 389 (+4). Build clean (vite build 13.45s). New feature doc [docs/PHASE_D4_DASHBOARD_CHARTS.md](docs/PHASE_D4_DASHBOARD_CHARTS.md). Versions bumped lockstep `0.45.0-alpha.3` -> `0.45.0-alpha.4`. **Per-sub-phase quality gate PASSED: deployed v0.45.0-alpha.4 to dev + 910/910 live SCIM tests pass.** |
| 2026-05-08 | feat **Phase D3 - Schemas Tab (v0.45.0-alpha.3):** Sub-phase 3 of 5 in Phase D. New SchemasTab at `/endpoints/$id/schemas` - read-only tree view of every schema declared by the endpoint's profile. Per-attribute characteristic badges per spec S7.3 (type / required / mutability / returned / uniqueness / multiValued / caseExact). Per-schema Copy URN button via `navigator.clipboard.writeText` with transient feedback. New `useEndpointSchemas(id)` hook + `endpointSchemasQueryOptions` (5min staleTime - schemas rarely change). New types `ScimSchemasResponse` / `ScimSchemaResource` / `ScimAttributeCharacteristic` in [queries.ts](web/src/api/queries.ts). Route registered + Schemas tab inserted between Activity and Logs in EndpointDetailPage. Composes Phase C primitives: LoadingSkeleton (count=5 h=56px mirrors schema card row, G1 pattern), EmptyState with DocumentBulletList icon (covers SchemaDiscovery=disabled 404 + zero schemas). Frontend-only - consumes existing `/scim/endpoints/:id/Schemas` endpoint (Phase 6 data-driven discovery). **TDD: RED via module-not-found; GREEN 7/7 SchemasTab vitest + 385/385 full vitest.** Build clean (vite build 12.41s). New feature doc [docs/PHASE_D3_SCHEMAS_TAB.md](docs/PHASE_D3_SCHEMAS_TAB.md). Versions bumped lockstep `0.45.0-alpha.2` -> `0.45.0-alpha.3`. |
| 2026-05-08 | feat **Phase D2 - Activity Tab (v0.45.0-alpha.2):** Sub-phase 2 of 5 in Phase D. Closes plan step 2.7. **Backend** [activity.controller.ts](api/src/modules/activity-parser/activity.controller.ts) accepts new optional `?endpointId=<uuid>` query param on `GET /admin/activity` (pushed into existing AND clause + inmemory branch; `endpointId` column on RequestLog already indexed since Phase 17, no schema change). **Frontend** new [ActivityTab.tsx](web/src/pages/ActivityTab.tsx) at `/endpoints/$id/activity` (nested route in route tree, loader pre-fetches via A4 pattern). URL-driven closed-set filters via [activitySearchSchema](web/src/routes/search-schemas.ts): `type` (`user`/`group`/`system`), `severity` (`info`/`success`/`warning`/`error`), free-text `search` with empty-string normalisation. Composes Phase C `LoadingSkeleton` (10 rows mirroring final layout) + `EmptyState` (Reset CTA only when filters active). New `useEndpointActivity` hook + `queryKeys.activity` factory. **SSE invalidation extended:** users/groups/resources channels now also invalidate `queryKeys.activity.all(endpointId)` so SCIM mutations refetch the open ActivityTab. New `Activity` tab inserted between Groups and Logs in EndpointDetailPage. **TDD: RED via tsc compile error (`Expected 0-6 arguments, but got 7`); GREEN 17/17 controller spec + new E2E + 6/6 ActivityTab vitest.** Test counts: API unit 3,641 -> 3,643 (+2), API E2E 1,171 -> 1,172 (+1), Live SCIM 888 -> 898 (+10 section `9z-W`), Web vitest 372 -> 378 (+6). Build clean (`vite build` 13.48s). New feature doc [docs/PHASE_D2_ACTIVITY_TAB.md](docs/PHASE_D2_ACTIVITY_TAB.md) (8 sections, 2 Mermaid diagrams - component flow + SSE invalidation map). Versions bumped lockstep `0.45.0-alpha.1` -> `0.45.0-alpha.2`. **Per-sub-phase quality gate next: deploy + 898+ live + 7 Playwright must all pass before D3 starts.** |
| 2026-05-08 | feat **Phase D1 - Overview Tab Data-Complete (v0.45.0-alpha.1):** First sub-phase of Phase D (Read-Only Completeness). Frontend-only commit, +4 web vitest tests (368 -> 372). OverviewTab rewritten to compose Phase C primitives on top of Phase B BFF: **5 KPI cards** (added Config Flags showing total + explicitly-true count, excludes strings like logLevel), **Recent Activity card** (10 rows from useEndpointOverview.recentActivity: local time / truncated path / METHOD outline badge / status filled badge color-coded by class 2xx-5xx / duration ms), **EmptyState** with History icon when zero activity, **LoadingSkeleton** replaces Spinner and mirrors final layout (5 KPI cards + 5 activity rows skeleton row, CLS=0 when data swaps in - Phase G1 pattern). Internal helpers: `ActivityRow` sub-component + `statusBadgeColor`. RED phase ran 4 fail + 5 pass; GREEN phase 9/9 OverviewTab tests pass; full vitest 372/372; build clean (vite build 14.28s). New feature doc [docs/PHASE_D1_OVERVIEW_TAB_DATA_COMPLETE.md](docs/PHASE_D1_OVERVIEW_TAB_DATA_COMPLETE.md) (8 sections, 1 Mermaid - data/loading flow, status badge color table, config flag counting rule, DoD). Versions bumped lockstep `0.44.1` -> `0.45.0-alpha.1` (api+web + lockfiles). **Per-sub-phase quality gate next: deploy v0.45.0-alpha.1 to dev + 888+ live SCIM tests + 7 Playwright must all pass before D2 starts.** Each Phase D sub-phase (D1-D5) ships as its own alpha tag matching Phase A's beta-by-beta rollout; rollup to stable v0.45.0 happens after D5. |
| 2026-05-07 | feat **Phase C Hardening - v0.44.1 (gap-fill before Phase D):** Closes 10 P0 findings from a deep retrospective audit of Phases A, B, C. Frontend-only commit, +17 web vitest tests (351 -> 368). **F-1** New [docs/PHASE_C_PRIMITIVES_AND_MUTATIONS.md](docs/PHASE_C_PRIMITIVES_AND_MUTATIONS.md) (12 sections, 2 Mermaid - mutation universal pattern + per-page optimistic-delete sequence, component contract, primitive consumer map for D/E, risk register, DoD). **F-3** True optimism for `useUpdateUser`/`useDeleteUser`: onMutate snapshot -> per-page apply -> onError rollback -> onSettled invalidate (was non-optimistic despite JSDoc claim). **F-4** New `useUpdateGroup` + `useDeleteGroup` (Phase E4 blocker resolved). **F-5** `If-Match` ETag header propagation on all 4 PATCH/DELETE hooks - `RequireIfMatch` endpoints now correctly return 412/428 instead of silent pass. **F-6/7/8** Tightened weak tests: useCreateUser/useCreateGroup overview-invalidation assertion; useUpdateEndpointConfig cold-cache path; useDeleteCredential post-settle cache state. **F-10** `ErrorBoundary.resetKeys` (mirrors react-error-boundary) - auto-reset on URL change so caught errors don't persist across routes. **F-11** New `queryKeys.users.all(id)`/`groups.all(id)` factories - mutations AND `useSSE.computeInvalidations` both use them now (was inconsistent literals vs factories). **F-15** Moved `ResizeObserver`/`matchMedia` shims from `KpiChart.test.tsx beforeAll` to global `web/src/test/setup.ts`. Internal helpers in [queries.ts](web/src/api/queries.ts): `ifMatchHeaders`, `patchListsContaining`, `restoreListSnapshots`. New API surface: `mutate({ userId, body, ifMatch })` object form (legacy bare-string still works for delete). **Build clean: vite build 9.77s, 877.80 kB / 244.81 kB gz (+0.86 kB gz).** Versions bumped lockstep `0.44.0` -> `0.44.1`. **Per-phase quality gate next: deploy to dev + 886+ live SCIM tests + 7 Playwright cases must all pass (also closes Phase B's deferred gate).** |
| 2026-05-06 | feat **Phase C - Reusable Primitives + Mutation Layer (v0.44.0):** Ships the primitive component library + mutation hooks that Phases D and E depend on. **C1 DetailDrawer** (Fluent OverlayDrawer wrapper: title/body/footer slots, ESC/backdrop/X close, 6 tests), **C2 FormDialog** (Fluent Dialog wrapper: submit/cancel/busy/error/disabled, 8 tests), **C3 EmptyState+LoadingSkeleton+ErrorBoundary** (EmptyState: icon+title+body+CTA, 7 tests; LoadingSkeleton: Skeleton wrapper with count/width/height, clamped 1-100, 6 tests; ErrorBoundary: React class component with reset, custom fallback, onError telemetry, stack-only-in-dev, 6 tests), **C4 KpiChart** (recharts sparkline area chart with empty/single-point fallback and Fluent color tokens, 6 tests), **C5 Mutation Layer** (7 useMutation hooks in queries.ts: useCreateCredential, useDeleteCredential [optimistic remove from cached overview], useUpdateEndpointConfig [optimistic shallow merge], useCreateUser, useCreateGroup, useUpdateUser, useDeleteUser; universal pattern: onMutate snapshot -> optimistic write -> onError rollback -> onSettled invalidate; 9 tests covering success + rollback). Barrel export [web/src/components/primitives/index.ts](web/src/components/primitives/index.ts). **Web vitest 303 -> 351 (+48), all passing. Build clean (10.62s).** Versions bumped lockstep `0.43.0` -> `0.44.0`. |
| 2026-05-06 | feat **Phase B - BFF Overview + SSE Audit (v0.43.0):** Closes plan step 0.7. **B1:** new `GET /admin/endpoints/:endpointId/overview` BFF on [DashboardController](api/src/modules/dashboard/dashboard.controller.ts) aggregates endpoint summary + stats + credentials + recent activity + config flags into one round trip with zero DB queries on warm cache. Response shape locked at unit (key allowlist + `expect(cred.credentialHash).toBeUndefined()`), E2E (walks every string for bcrypt `$2` prefix), live (section 9z-V, 17 tests including hash-leak sweep). **Critical: credential hash NEVER returned** - explicit projection list in `getEndpointOverview` strips internal columns. New types in [dashboard.types.ts](api/src/shared/types/dashboard.types.ts): `EndpointOverviewResponse`/`Summary`/`Stats`/`Credential`/`Activity`. [DashboardModule](api/src/modules/dashboard/dashboard.module.ts) imports `RepositoryModule.register()` for credential repo. **B2:** new [endpointOverviewQueryOptions](web/src/api/queries.ts) + `useEndpointOverview(id)` hook; `queryKeys.endpoints.overview(id) -> ['endpoints', id, 'overview']`. [OverviewTab](web/src/pages/OverviewTab.tsx) replaces 2 separate hook calls (useEndpoint + useEndpointStats) with one BFF call; adds Credentials KPI card with active count. [Route loader](web/src/routes/endpoints.$endpointId.index.tsx) switched from `endpointStatsQueryOptions` to `endpointOverviewQueryOptions` so hover-prefetch warms full BFF response. **B3:** [useSSE](web/src/hooks/useSSE.ts) made channel-aware. Pre-B3 the hook always invalidated dashboard + endpoints.all + (when endpointId on payload) endpoints.detail/stats. New keys introduced in Phase A1+ (`endpoints.overview`, `users.byEndpoint`, `groups.byEndpoint`) were NOT in the set - tab tables stayed stale until 30s staleTime kicked in. Refactored: every supported event type maps to a `Channel` (users/groups/resources/credentials/endpoints) and the channel determines which keys to invalidate. Exported `computeInvalidations(type, endpointId)` for unit tests. Added 5 new event types (`scim.credential.created/revoked`, `scim.endpoint.created/updated/deleted`) ready for emit-side wiring in Phase E. **Tests +39: API unit 3,632 -> 3,641 (+9: 7 B1 + 2 churn), API E2E 1,119 -> 1,122 (+3 B1), Web vitest 293 -> 303 (+10: 2 B2 OverviewTab + 8 B3 useSSE), Live SCIM 869 -> 886 (+17 section 9z-V).** Live SCIM tests added before TEST SECTION 10 (DELETE OPERATIONS) following live-test conventions. Versions bumped lockstep `0.42.0` -> `0.43.0` (minor bump per phase plan). New feature doc [docs/PHASE_B_BFF_OVERVIEW_AND_SSE.md](docs/PHASE_B_BFF_OVERVIEW_AND_SSE.md). **Per-phase quality gate next: deploy to dev + 886 live SCIM tests + 7 Playwright cases must all pass.** |
| 2026-05-06 | feat **Phase A complete (v0.42.0):** TanStack Router migration shipped end-to-end (A1+A2+A3+A4+A5). URL is the single source of truth for view state across in-app navigation, browser back/forward, deep-link refresh, and server-side SPA fallback. Every contract locked in by both unit (isolated) and Playwright (real-browser) tests. Final fix in 0.42.0 (rolled up from beta.5): [api/src/bootstrap/spa-fallback.ts](api/src/bootstrap/spa-fallback.ts) `resolveSpaIndexPath()` walks two `..` segments instead of one - the file lives at `/app/dist/bootstrap/spa-fallback.js` (one level deeper than `main.js` at `/app/dist/main.js`). Pre-fix: deep links hit placeholder HTML. Post-fix: deep links serve the real bundled index.html with `<script src=/assets/index-...>` tags. **Verified live on dev: 869/869 live SCIM tests + 7/7 Playwright router-behavior cases pass.** Roll-up: A1 0.42.0-alpha.1 (foundation), A2 0.42.0-beta.1 (cutover), A3 0.42.0-beta.2 (per-page URL state), A4 0.42.0-beta.3 (loaders + hover-prefetch), A5 0.42.0-beta.4/5 (Playwright + SPA fallback fix). Final counts: API unit 3,612 -> 3,632 (+20), API E2E 1,104 -> 1,119 (+15), Web vitest 240 -> 293 (+53 across A1-A4), Live SCIM 869 (unchanged on every Phase A commit). New feature docs: PHASE_A1/A2/A3/A4/A5_*.md (5 docs). **Next: Phase B (BFF Overview endpoint + mutations layer).** |
| 2026-05-06 | feat **Phase A5 - Playwright E2E + SPA Fallback Fix (v0.42.0-beta.4, closes Phase A):** Real-browser Playwright tests lock in router contracts AND surface a critical production bug. **Bug:** pre-A5 [main.ts](api/src/main.ts) only had a SPA fallback for `/admin` (legacy admin tab UI). Phase A1+ added `/endpoints`, `/logs`, `/settings` as URL-driven SPA routes with no NestJS controllers. In-app `<Link>` clicks worked (pushState, browser-side), but a hard refresh, deep link, or share-a-link hit the server, NestJS routed through global `/scim` prefix, found nothing, returned `{"message":"Cannot GET /endpoints","error":"Not Found","statusCode":404}`. **Fix:** new module [api/src/bootstrap/spa-fallback.ts](api/src/bootstrap/spa-fallback.ts) exports `SPA_PATH_PREFIXES` + `applySpaFallback(app)`. Middleware reads index.html once at boot (cached), serves text/html 200 for any GET under those prefixes. Falls back to placeholder HTML if bundle missing (test env). [main.ts](api/src/main.ts) calls `applySpaFallback` after `useStaticAssets`. [app.helper.ts](api/test/e2e/helpers/app.helper.ts) mirrors prod middleware. **Tests +30:** [web/e2e/router-behavior.spec.ts](web/e2e/router-behavior.spec.ts) NEW (7 Playwright cases - pushState, back/forward, deep link, URL <-> filter input sync, refresh preserves filter, hover-prefetch fires network), [api/test/e2e/spa-fallback.e2e-spec.ts](api/test/e2e/spa-fallback.e2e-spec.ts) NEW (15 cases - every SPA path returns text/html 200, plus sanity that /scim/* still returns JSON), [api/src/bootstrap/spa-fallback.spec.ts](api/src/bootstrap/spa-fallback.spec.ts) NEW (8 unit cases - prefix list, path resolution, mount contract, body shape, single-read perf). Pre-auth in Playwright via `addInitScript` (sets localStorage[scimserver.authToken] BEFORE app boots so TokenGate doesn't short-circuit to dialog). **Test counts: API unit 3,612 -> 3,632 (+20), API E2E 1,104 -> 1,119 (+15), Web vitest 293 (unchanged), live SCIM 869 (unchanged - zero backend regression). Build clean.** Versions bumped lockstep `0.42.0-beta.3` -> `0.42.0-beta.4`. Lock files regenerated in node:25-alpine. New feature doc [docs/PHASE_A5_PLAYWRIGHT_AND_SPA_FALLBACK.md](docs/PHASE_A5_PLAYWRIGHT_AND_SPA_FALLBACK.md). Per-phase quality gate (deploy + 869 live + 7 Playwright) runs next, then bump to `0.42.0` (drop -beta.N suffix - Phase A complete). |
| 2026-05-06 | feat **Phase A4 - Route Loaders + Hover-Prefetch (v0.42.0-beta.3):** Every production route now pre-fetches its data via `queryClient.ensureQueryData(...)` so hovering a `<Link>` warms the cache before the click. By the time the route mounts the data is already there - no spinner. **Architecture changes:** (1) New module [query-client.ts](web/src/api/query-client.ts) hoists the QueryClient to a module-level singleton consumed by both AppShell `<QueryClientProvider>` and the router context. (2) [queries.ts](web/src/api/queries.ts) refactored: extracted 10 `xxxQueryOptions(...)` helpers (dashboard/health/version/endpoints/endpointDetail/endpointStats/endpointUsers/endpointGroups/endpointLogs/globalLogs) so loaders + hooks share one source of truth for queryKey + queryFn + staleTime. (3) [__root.tsx](web/src/routes/__root.tsx) uses `createRootRouteWithContext<{ queryClient }>()`. (4) [router.ts](web/src/router.ts) passes `context: { queryClient }` to `createRouter`. (5) All 10 production routes wired with loaders; URL-search-driven routes use `loaderDeps` for granular re-fetch. (6) [LogsTab.tsx](web/src/pages/LogsTab.tsx) + [LogsPage.tsx](web/src/pages/LogsPage.tsx) refactored to delegate to shared queryOptions (removed inline useQuery + fetchWithAuth). **Tests +13:** [router-loaders.test.ts](web/src/router-loaders.test.ts) NEW (12 structural - one per route + context + defaults), [router-loaders.integration.test.tsx](web/src/router-loaders.integration.test.tsx) NEW (1 e2e proving loader-pre-warmed cache lets useQuery render synchronously). [App.test.tsx](web/src/App.test.tsx) modified to stub `globalThis.fetch` since the cutover test now sees real RouterProvider whose loaders call fetch. **Web vitest 280 -> 293 (+13), all passing. Build clean (10.02s).** Versions bumped lockstep `0.42.0-beta.2` -> `0.42.0-beta.3`. Lock files regenerated in node:25-alpine for cross-platform deps. New feature doc [docs/PHASE_A4_ROUTE_LOADERS.md](docs/PHASE_A4_ROUTE_LOADERS.md). Per-phase quality gate (deploy + 869 live tests) runs next. |
| 2026-05-06 | feat **Phase A3 - Per-Page URL-Driven State (v0.42.0-beta.2):** Pagination + filter inputs now live in the URL via TanStack Router's `useSearch` + `useNavigate`. Five list/filter views migrated from `useState` to URL-driven state, parsed by zod schemas. **Components migrated:** [UsersTab.tsx](web/src/pages/UsersTab.tsx) (useState(startIndex) removed), [GroupsTab.tsx](web/src/pages/GroupsTab.tsx), [LogsTab.tsx](web/src/pages/LogsTab.tsx) (page + urlContains; SearchBox resets to page 1 on filter change), [LogsPage.tsx](web/src/pages/LogsPage.tsx) (urlContains, empty -> undefined), [EndpointsPage.tsx](web/src/pages/EndpointsPage.tsx) (q free-text). **Helper enhancement:** [router-test-utils.tsx](web/src/test/router-test-utils.tsx) gained a `validateSearch` option so test routes parse with the same zod schema as production. **Tests:** +6 (5 component-level URL-driven assertions + 1 helper test). All affected tests rewritten to use `renderWithRouter` instead of plain QueryClientProvider+FluentProvider since useSearch/useNavigate now require router context. **Web vitest 274 -> 280 (+6), all passing. Build clean (10.36s).** Versions bumped lockstep `0.42.0-beta.1` -> `0.42.0-beta.2`. New feature doc [docs/PHASE_A3_PER_PAGE_URL_STATE.md](docs/PHASE_A3_PER_PAGE_URL_STATE.md). Per-phase quality gate (deploy + 869 live tests) runs next. **Note:** A3 is purely frontend; live SCIM tests should be unaffected (this validates that). |
| 2026-05-06 | feat **Phase A2 - TanStack Router Cutover (v0.42.0-beta.1):** URL is now the single source of truth for view state. Cutover work: (1) [App.tsx](web/src/App.tsx) renders `<RouterProvider router={router} />` by default; `?ui=legacy` escape hatch retained one release for rollback. (2) [AppShell.tsx](web/src/layout/AppShell.tsx) stripped of `AppRouter` regex matcher + 5 page imports - now pure layout chrome with `<main>{children}</main>` (children = `<Outlet />` from `__root.tsx`). (3) [ui-store.ts](web/src/store/ui-store.ts) stripped of `currentPath`, `navigate(path)`, popstate listener - Zustand now holds 3 values only. (4) [AppSidebar.tsx](web/src/layout/AppSidebar.tsx) uses TanStack `<Link>` + `useRouterState`; new `data-testid="nav-{key}"` attrs. (5) [DashboardPage.tsx](web/src/pages/DashboardPage.tsx), [EndpointsPage.tsx](web/src/pages/EndpointsPage.tsx) Card onClick uses `useNavigate({ to: '/endpoints/$endpointId', params: { endpointId } })`. (6) [EndpointDetailPage.tsx](web/src/pages/EndpointDetailPage.tsx) rewritten as pure layout: active tab from URL via `useRouterState`, content via `<Outlet />`, TabList click drives `useNavigate`, back button is real `<Link to="/endpoints">`. (7) New [OverviewTab.tsx](web/src/pages/OverviewTab.tsx) extracted from EndpointDetailPage; bound to new index route [endpoints.$endpointId.index.tsx](web/src/routes/endpoints.$endpointId.index.tsx) so bare `/endpoints/$endpointId` URL still shows overview. (8) [__root.tsx](web/src/routes/__root.tsx) now composes `<AppShell><Outlet /></AppShell>`. **Tests:** [OverviewTab.test.tsx](web/src/pages/OverviewTab.test.tsx) +3, [AppShell.test.tsx](web/src/layout/AppShell.test.tsx) +1 + all wrapped in `renderWithRouter`, [EndpointDetailPage.test.tsx](web/src/pages/EndpointDetailPage.test.tsx) +2 (URL-driven tab selection + Link href) + reshaped, [App.test.tsx](web/src/App.test.tsx) made async, [router.test.ts](web/src/router.test.ts) updated for 5 nested children. **Web vitest 268 -> 274 (+6), all passing. Build clean (1.03s, 873.94 kB / 243.95 kB gzipped - up 148 kB unminified for runtime router code, will be addressed in Phase H6 size-limit).** New feature doc [docs/PHASE_A2_TANSTACK_ROUTER_CUTOVER.md](docs/PHASE_A2_TANSTACK_ROUTER_CUTOVER.md) (10 sections, 2 Mermaid diagrams, risk register, manual smoke verification matrix). Versions bumped to `0.42.0-beta.1` lockstep in api/web. **Per-phase quality gates running next: deploy to dev + 869+ live SCIM tests must pass before this phase is fully closed.** |
| 2026-05-06 | feat **Phase A1 - TanStack Router Foundation (v0.42.0-alpha.1):** First execution step of UI Redesign Remaining Gaps Plan. Additive-only scaffolding shipped across 4 commits: `5a2a911` (A1.1: install `zod` runtime + `@tanstack/router-devtools` devDep), `dbdc0ef` (A1.2: 6 zod search-param schemas, +20 unit tests), `c06ebcf` (A1.3-A1.5: 10 route files + `router.ts` + module augmentation, +4 router config tests), `de8133a` (A1.6: `renderWithRouter` test helper, +4 helper tests). Route tree: `__root` -> { `/`, `/endpoints` (search-validated), `/endpoints/$endpointId` (layout) -> { users, groups, logs, settings tabs }, `/logs`, `/settings` }. `defaultPreload: 'intent'` + `defaultPreloadStaleTime: 30_000`. TypeScript module augmentation registered. Strict TDD throughout (Red confirmed before each Green). **No production code wired into router yet** - [web/src/App.tsx](web/src/App.tsx), [web/src/layout/AppShell.tsx](web/src/layout/AppShell.tsx), [web/src/store/ui-store.ts](web/src/store/ui-store.ts) still use legacy `currentPath` regex matcher. Cutover (replace `AppRouter` with `<RouterProvider />`, strip Zustand `navigate`) happens in **Phase A2** with bump to `0.42.0-beta.1`. New feature doc [docs/PHASE_A1_TANSTACK_ROUTER_FOUNDATION.md](docs/PHASE_A1_TANSTACK_ROUTER_FOUNDATION.md) (9 sections, 2 Mermaid diagrams, risk register, DoD). Versions bumped to `0.42.0-alpha.1` in both api/web package.json (lockstep). Per-phase quality gates deferred to A2 cutover (A1 is zero-runtime-impact additive scaffolding - no behavior to gate). **Web vitest: 240 -> 268 (+28). API tests unchanged. Build clean (9.51s). 0 TS errors in new files.** |
| 2026-05-06 | docs **UI Redesign Remaining Gaps Plan (planning):** Created [docs/UI_REDESIGN_REMAINING_GAPS_PLAN.md](docs/UI_REDESIGN_REMAINING_GAPS_PLAN.md) - dependency-ordered Phases A-I to reach 100% UI redesign compliance. Verified ~38% complete; gaps include BFF Overview endpoint (plan 0.7), mutation layer (0 mutations exist), Activity/Schemas/Credentials tabs, Cmd+K (cmdk not installed), SSE invalidation audit, MSW handlers (msw installed but 0 handlers), axe-core, visual regression, web coverage gates, size-limit, `?ui=legacy` cutover, legacy cleanup (~3,000 lines in `web/src/components/`). Plan ships 4 Mermaid diagrams (dependency graph, mutation sequence, cutover state, prod promote state), risk register, test coverage targets (~120 new tests), TDD discipline. 12-16 day estimate. **Phase A (TanStack Router migration) starts next** with Foundation step (additive only, zero breakage). Updated INDEX.md, CHANGELOG.md. |
| 2026-05-04 | **Production Deployment + Dev Infra + Prompt System Overhaul (v0.40.2):** (1) Fixed Docker image `dist/main.js` path regression (`rootDir: "src"` in `tsconfig.build.json`). (2) Closed 5 live test failures via TDD: Group `displayName.uniqueness` metadata `"none"` -> `"server"`, `value` sub-attr `returned: "always"` (RFC 7643 S2.4, 20 instances across 5 preset files), error key allowlist includes diagnostics URN, RequireIfMatch test headers fix. (3) Published `0.40.2` image, deployed to dev (867/867 live tests pass), promoted to prod via `promote-to-prod.ps1` with digest pinning. Fixed health URL in promote script (`/health` -> `/scim/health`). (4) Synced dev credentials to match prod (`changeme-scim/jwt/oauth`, PG password `ScimP0stgres@2026`). Updated both state files. (5) Created 5 new prompts: `planSelection`, `securityAudit`, `performanceBenchmark`, `deployAndPromote`, `sessionWrapUp`. Refreshed stale baselines in 4 existing prompts (34 instances). (6) Documented mandatory quality gates (11 gates + TDD) in `copilot-instructions.md` and all 5 workflow prompts. Prod promotion explicitly separate. **Validation: 3,538 unit (91 suites), ~1,104 E2E (52 suites), 867 live assertions (0 fail) - ALL PASSING on both dev and prod.** |
| 2026-04-30 | docs **DELIVERY_PLAN.md (v0.40.0 + 3 commits on `ci/validate-before-push`):** Created consolidated 6-week execution plan reconciling UI redesign + Tier-0 security + CI/CD upgrades into one operating-model document (12 sections, 4 Mermaid diagrams, 1 Gantt). Names every defect with file path + hours + "done when". Replaces all prior phase-based session plans. Three commits already shipped to support it: `5f2376b` (OPS-1 validate gate), `1a22771` (lint cleanup, 58 errors -> 0), `ef9673b` (S-1, S-3 delete dead ScimAuthGuard via TDD with permanent regression guard at `api/src/security/forbidden-source-patterns.spec.ts`). **Validation: 3,422 unit (84 suites), 1,100 E2E inmemory (52 suites), 0 lint errors - ALL PASSING.** |
| 2026-04-28 | test **Test Gaps Audit #6 (v0.40.0):** Comprehensive audit per `addMissingTests.prompt.md`. Closed 15 E2E gaps, 28 live assertions. E2E: Custom resource type + projection (GET/POST/PUT/LIST with ?attributes=/?excludedAttributes=), Custom resource type + StrictSchema (POST/PUT reject unknown attrs), Bulk + RequireIfMatch for PUT/DELETE (428 per-op), Bulk valid If-Match succeeds, GroupHardDeleteEnabled=False PUT/PATCH/GET still work, .search body-level attributes/excludedAttributes, SoftDelete + ETag conditional GET/PUT 404, Four-flag combo (StrictSchema+IgnoreReadOnly+IncludeWarning+VerbosePatch), PrimaryEnforcement+BooleanStrings combo, PerEndpointCredentials+RequireIfMatch deeper combo, logFileEnabled toggle, error response key allowlist, Bulk response key allowlist. Live: section 9z-U (28 sub-tests covering GroupHardDelete, SoftDelete+ETag, Bulk+RequireIfMatch, four-flag combo, PrimaryEnforcement+BooleanStrings, .search body projection, logFileEnabled toggle). **3,429 unit (84 suites), 1,149 E2E (54 suites), ~817 live assertions - ALL PASSING.** |
| 2026-04-28 | feat **Structured SCIM Error Diagnostics Enrichment (v0.39.0):** `attributePaths[]` array, `activeConfig` snapshot, `filterExpression` field on all validation errors. +5 E2E, +17 unit tests. Live: section 9z-T (21 sub-tests). |
| 2026-04-27 | fix **PATCH Scalar Boolean String Coercion (v0.38.1):** RCA'd Entra ID SCIM Validator 400 errors on all PATCH/POST operations against Himanshu-ISV-1 endpoint. Root cause: `coercePatchOpBooleans()` missed scalar string values with explicit paths (e.g. `path:"active", value:"True"`). Entra sends boolean values as strings in path-based PATCH ops. Added `coerceScalarPatchValue()` helper with longest-prefix URN matching for extension paths. Single shared function fix covers Users/Groups/Generic services. +13 unit tests, +4 E2E tests, +5 live tests (section 9z-S). Doc: PATCH_SCALAR_BOOLEAN_COERCION.md. |
| 2026-04-23 | test **Test Gaps Audit #5 (v0.38.0):** Comprehensive audit per `addMissingTests.prompt.md`. Closed 15 E2E gaps, 16 unit gaps, 21 live assertions. E2E: POST duplicate userName 409, POST missing userName/schemas 400, .search returned:always, PATCH case-insensitive uniqueness 409, RequireIfMatch OFF explicit, VerbosePatch OFF dot-notation, PatchOpAllowRemoveAllMembers ON standalone, ETag single GET, 412 stale If-Match, PrimaryEnforcement+StrictSchema combo, Bulk+RequireIfMatch, SchemaDiscovery disabled blocks all 3, IncludeWarning/IgnoreReadOnly combos, error key allowlist, Group uniqueness, 401 auth. Unit: enforceIfMatch edge cases, RequireIfMatch OFF default, POST missing userName 400, PrimaryEnforcement+StrictSchema combo, PatchOpAllowRemoveAllMembers standalone, GroupHardDelete OFF. Live: section 9z-Q (12 sub-tests). |
| 2026-04-21 | test **Test Gap Audit + Manager PATCH String Coercion (v0.37.2):** Comprehensive test gap audit per `addMissingTests.prompt.md` - identified 5 concrete gaps and filled all. (1) Manager PATCH string coercion fix: SchemaValidator pre-PATCH validation now accepts raw strings for complex attrs with `value` sub-attr (Postel's Law / Entra ID compat) and empty values as RFC 7644 §3.5.2.3 removal signals. Fixes 3 SCIM Validator compliance failures (SFComplianceFailed: true → false). (2) New tests: +12 unit (schema-validator: string coercion, empty removal, canonical, safety guards), +2 service-level (raw string + empty string through full service), +8 E2E (manager-patch-string-coercion: strict mode add/replace/remove/persist/lenient), +4 E2E (error response key allowlist), +3 E2E (group sw/pr filters), +1 E2E (group reprovision), +2 E2E (PATCH/PUT projection gaps), +7 live (section 9z-N). Doc: MANAGER_PATCH_STRING_COERCION.md. **3,345 unit (84 suites), ~1,025 E2E (49 suites) - ALL PASSING.** |
| 2026-04-17 | feat **Logging Improvements + Test Gap Audit #6 (v0.37.1):** endpointId now persisted in RequestLog via interceptor (indexed column, replaces fragile urlContains). endpoint-log history uses indexed endpointId filter. Azure Bicep: LOG_LEVEL=DEBUG, LOG_FORMAT=json, LOG_FILE="" (no ephemeral disk writes), LOG_RING_BUFFER_SIZE=5000, LOG_RETENTION_DAYS=30, LOG_SLOW_REQUEST_MS=1000. Fixed 2 pre-existing E2E failures (R-RET-3: emails.value/members.value are returned:default per RFC 7643, not returned:always). +17 E2E tests in test-gaps-audit-4 (Location header, endpointId persistence, Bulk+SoftDelete, GroupHardDelete, 3-flag combos). **3,318 unit (84 suites), 986 E2E (47 suites) -- ALL PASSING.** |
| 2026-04-16 | fix **Azure Production Outage Fix (v0.37.1):** Root-caused Entra provisioning connection timeout. Prisma pool (5 connections) exhausted by slow activity queries (87-129s on B1ms DB). UUID guard bypass in resolveUserName/resolveGroupName sent emails to @db.Uuid column. **Fixes:** UUID guards on 6 code paths, 30-day activity count bounds, Promise.allSettled, filter push-down guard, DB search fix, Dockerfile fix. Azure: LOG_AUTO_PRUNE=true. Post-fix: 108ms (was >60s timeout). **3,265 unit (83 suites), 969 E2E (46 suites) -- ALL PASSING.** |
| 2026-04-13 | � **Logging & Observability Documentation Rewrite (v0.34.0):** Complete work spanning 4 commits: (1) Rewrote 6 logging/error/observability docs from scratch, source-verified - `LOGGING_AND_OBSERVABILITY.md` v4.0 (21 sections, 10 log troubleshooting), `LOGGING_ERROR_HANDLING_IDEAL_DESIGN.md` v3.0 (21 sections, error catalog by status code), `LOGGING_ERROR_HANDLING_QUALITY_AUDIT.md` v3.0 (20-gap register), `REMOTE_DEBUGGING_AND_DIAGNOSIS.md` v3.0 (16 sections, 20 troubleshooting scenarios), `PROMPT_LOGGING_VERIFICATION.md` v3.0 (71/71 PASS), `PROMPT_ERROR_HANDLING_VERIFICATION.md` v3.0 (55/55 PASS). (2) Added troubleshooting & RCA: 20 error scenarios with full HTTP request/response JSON, error catalog by status code, 10 log troubleshooting scenarios. (3) Cross-doc freshness: fixed 13 stale items across 9 files (LOG_LEVEL names, ring buffer default, LoggingModule components, section counts, version refs). (4) Added Observability Quick Start - README.md section + REMOTE_DEBUGGING Section 0 full A–J script with inline URL/header/response JSON for every log access pattern + log file layout for Local/Docker/Azure. Updated INDEX.md, CHANGELOG.md per norms. (5) Fixed 17 doc gaps: route correction (DELETE→POST /logs/clear), prune endpoint, activity feed, web UI, /health, log buffering caveat, COMPLETE_API_REFERENCE route table (5 routes + audit + version response rewrite), query param tables. (6) +11 E2E tests: PATCH diagnostics fix, audit trail, prune, minDurationMs, slowRequestThresholdMs, per-endpoint history. **3,206 unit (80 suites), 950 E2E (45 suites) - ALL PASSING.** |
| 2026-04-13 | �📊 **Design Improvement Deep Analysis (v0.34.0):** Comprehensive source-verified audit of 60+ files across all architectural layers against RFC 7643/7644, SOLID, DRY, security, and maintainability best practices. **Overall rating: B+ (7.5/10).** Identified 5 security issues (S1–S5: hardcoded credential in `scim-auth.guard.ts`, no `timingSafeEqual`, open CORS, `enableImplicitConversion`), 5 SOLID violations (SchemaValidator god class 1,467 lines, service-helpers Swiss army knife 1,230 lines, auth guard OCP violation), 10 DRY violations (~950 lines estimated duplication - Generic service re-implements ~350 lines of `ScimSchemaHelpers`, controller boilerplate ×3, patch engine triplication), 5 DTO validation gaps (ListQueryDto dangerously sparse vs well-hardened SearchRequestDto), 5 data integrity issues (missing `@@unique` on ResourceMember, missing `wrapPrismaError` on `create()`, silent catch blocks in credential repo). Produced 25-item prioritized improvement roadmap across 4 tiers with Gantt timeline. Created `docs/DESIGN_IMPROVEMENT_DEEP_ANALYSIS.md` (19 sections, 8 Mermaid diagrams, full file inventory with line counts). Updated INDEX.md. **3,206 unit (80 suites), 939 E2E (45 suites) - ALL PASSING.** |
| 2026-04-09 | � **Uniqueness Enforcement Alignment with RFC 7643 §2.4 (v0.33.0):** Removed over-enforcement of `uniqueness:server` on attributes declared as `uniqueness:none`. `User.externalId`, `User.displayName`, `Group.externalId` no longer cause 409 Conflict - saved as received. Dropped DB unique constraints on `displayName` and `externalId` (replaced with non-unique indexes). Removed `externalId` from User `findConflict()`, deleted Group `assertUniqueExternalId()`, deleted Generic service `findConflict()`. Only `User.userName` (uniqueness:server) and `Group.displayName` (uniqueness:server) still enforce uniqueness. Updated 15+ files (services, repos, specs, E2E, live tests, 5 docs). New Prisma migration: `remove_uniqueness_displayname_externalid`. All unit tests passing. |
| 2026-04-09 | �🔎 **Deep Latest-Source Audit (v0.33.0 codebase state):** Re-read current settings-v7 refactor and surrounding runtime architecture directly from source. Confirmed strong direction: endpoint config moved toward single-source-of-truth registry in `endpoint-config.interface.ts`, default resolution centralized through `getConfigBoolean()`, Users/Groups/Generic services largely aligned around shared helpers, schema-cache-driven validation/coercion/returned filtering remains a major architectural strength, and logging infrastructure is mature/correlation-aware. Identified principal engineering risks still visible in live source: legacy token + `console.log` in `scim-auth.guard.ts`, permissive CORS + permanent `/scim/v2` rewrite in `main.ts`, version/doc drift (`README` 0.31.0 vs `api/package.json` 0.33.0), and CI fragmentation across multiple container workflows. Overall assessment: strong domain architecture and test discipline, but runtime hardening and delivery simplification should be next priority. |
|------|-------------|| 2026-03-31 | ⚡ **v0.31.0 - URN Dot-Path Cache Keys + Post-Cache Cleanup:** Replaced `__top__` sentinel with URN-qualified dot-path keys in all `*ByParent` cache maps - eliminates name-collision vulnerability at any nesting depth. New cache fields: `coreSchemaUrn`, `schemaUrnSet`. Exported `isSubAttrKey()` utility. Extracted `coercePatchOpBooleans()` and `stripNeverReturnedFromPayload()` shared helpers (replaced 3×12-line + 3×40-line duplicate blocks). Removed dead code: `SCHEMA_CACHE_TOP_LEVEL`, flat `sanitizeBooleanStrings`, `getReturnedCharacteristics`, flatten helpers, `collectBooleanAttributeNames`. Simplified `getCaseExactAttributes`/`getUniqueAttributes` (no fallback branches). Fixed double cache access in `coerceBooleansByParentIfEnabled`. Updated SCHEMA_AND_RESOURCETYPE_DATA_STRUCTURE_ANALYSIS.md. **3,090 unit (74 suites), 817 E2E (37 suites), ~951 live + 112 Lexmark - ALL PASSING.** || 2026-03-26 | 🔧 **v0.30.0 - Admin Endpoint API Improvements:** Envelope response (`{ totalResults, endpoints[] }`), `?view=summary\|full` param with `ProfileSummary` digest, `_links` (HATEOAS), ISO 8601 timestamps, `scimBasePath` rename, `GET /admin/endpoints/presets` + `/:name`, nested stats format. Schema cache wiring into all validators/filters/sort/patch. Fixed 3 generic service filter tests. **3,061 unit (74 suites), 817 E2E (37 suites), ~951 live + 112 Lexmark - ALL PASSING.** |
| 2026-03-20 | ⚡ **Precomputed Schema Characteristics Cache (Option 3 + Parent→Children Maps):** Replaced per-request schema tree walks (40–180 µs/request) with precomputed Parent→Children `Map<string, Set<string>>` cache built once at profile load. 10 maps: booleansByParent, neverReturnedByParent, alwaysReturnedByParent, requestReturnedByParent, readOnlyByParent, immutableByParent, caseExactByParent, alwaysReturnedSubs, uniqueAttrs, extensionUrns. `sanitizeBooleanStringsByParent()` uses parent-key context to prevent name-collision false positives (e.g., core `active` boolean vs extension `active` string). New: `SchemaCharacteristicsCache` interface, `SchemaValidator.buildCharacteristicsCache()`, lazy cache builder in `getSchemaCache()`, `coerceBooleansByParentIfEnabled()`. 12 production files changed (+527/−57 lines). Analysis doc: SCHEMA_AND_RESOURCETYPE_DATA_STRUCTURE_ANALYSIS.md. **2,923 unit (73 suites), 763 E2E (35 suites), 602 live + 112 Lexmark - ALL PASSING. Deployed to Azure as ghcr.io/pranems/scimserver:0.29.2.** |
| 2026-03-16 | 🔧 **v0.29.0 - Legacy Config Removal + Test Fixes + Doc Rewrite:** Fixed 19 test failures (URN dot-split, profile-aware schema, Content-Type 415 middleware). Removed legacy `endpoint.config` admin API field - `profile.settings` is sole input. Added multi-endpoint isolation E2E tests (4 profiles, 21 assertions). Bumped version to v0.29.0. Updated ENDPOINT_PROFILE_ARCHITECTURE.md for v0.29.0. Comprehensive doc audit: version sweep 0.28.0→0.29.0 across ~15 files, test counts updated (2,906 unit / 698 E2E / 621 live), CHANGELOG v0.29.0 entry, PROJECT_HEALTH_AND_STATS updated. **2,906 unit (73 suites), 698 E2E (33 suites), 621 live - ALL PASSING.** |
| 2026-03-13 | 📚 **Documentation Refresh from Source (v0.28.0):** Recreated README.md from scratch - comprehensive table-of-contents README with 18 sections: Why SCIMServer, 4 Quick Start options (Azure/Docker/Local/InMemory), Mermaid architecture diagram, full SCIM compliance matrix (18 RFC features), 3-tier auth reference, 14 config flags table, endpoint profiles & presets, env var reference, Entra setup guide, operations & observability, test suite summary (2,830 unit + 613 E2E + 832 live), repository structure tree, 15-doc documentation index, technology stack table, CI/CD reference. Updated docs/INDEX.md (date → 2026-03-13, README description), DEPLOYMENT.md (date → 2026-03-13, added InMemory deployment section, updated comparison table, version tags), PROJECT_HEALTH_AND_STATS.md (date → 2026-03-13, Prisma 7→5 models ER diagram, added Phases 17–20, fixed /Me endpoint status), Session_starter.md (Current Focus updated). |
| 2026-03-12 | ✅ **Phase 13: Endpoint Profile Configuration (v0.28.0):** Replaced fragmented `Endpoint.config` + `EndpointSchema` + `EndpointResourceType` with unified `Endpoint.profile` JSONB. 5 built-in presets (entra-id default, entra-id-minimal, rfc-standard, minimal, user-only). RFC-native auto-expand + tighten-only validation engine. New `GET /admin/profile-presets` API. Prisma: 7→5 models (dropped EndpointSchema + EndpointResourceType tables + Prisma migration). 28 files deleted (~4,800 lines removed), 13 new files created in `endpoint-profile/` module. Design doc: SCHEMA_TEMPLATES_DESIGN.md (2,349 lines, 47 code blocks, 19 Mermaid diagrams). 8 commits on feat/torfc1stscimsvr. **2,867 unit (73 suites), 591 E2E + 6 skipped (29 suites), 659 live (647 pass, 12 pre-existing) - ALL PASSING.** |
| 2026-03-03 | ✅ **InMemory Bug Fixes + Full Validation Pipeline (v0.27.0):** Fixed 4 bugs discovered during live testing with inmemory backend: Bug #1 - AdminSchemaController used PrismaService directly (switched to EndpointService). Bug #2 - Custom resource types had no stub core schema (added auto-generation in ScimSchemaRegistry.registerResourceType()). Bug #3 - SchemaValidator hardcoded `urn:ietf:params:scim:schemas:core:` prefix to detect core schemas (added `isCoreSchema?: boolean` flag on SchemaDefinition + helper function, 5 locations updated). Bug #4 - RepositoryModule.register() created duplicate InMemoryEndpointCredentialRepository instances when called from AuthModule + ScimModule (added static module caching). Also fixed live test script bug (9x.15 excludedAttributes array→string). All 3 deployment types rebuilt and verified: **2,741 unit (73 suites), 651 E2E (32 suites), 659 live (647 pass, 12 pre-existing feature gaps) - identical across local (inmemory), Docker (Prisma), Azure (Prisma).** GHCR image: sha256:7787e05bbd4fdb. Files modified: validation-types.ts, schema-validator.ts, endpoint-scim-generic.service.ts, scim-service-helpers.ts, scim-service-helpers.spec.ts, repository.module.ts, repository.module.spec.ts. |
| 2026-03-03 | ✅ **Generic Service Parity Fixes + Test Gap Closure (v0.27.0):** Closed top 3 P0 gaps from P3 re-audit delta. Fix #1: Generic PUT/PATCH/DELETE now use `enforceIfMatch()` - honors `RequireIfMatch` config flag for 428 response (was only Users/Groups). Fix #2: `SchemaValidator.validateFilterAttributePaths()` wired into runtime filter paths for all 3 services (Users/Groups/Generic) - unknown filter attributes now return 400 `invalidFilter`. Fix #3: `parseSimpleFilter()` throws 400 `invalidFilter` for unsupported expressions instead of silently returning `undefined`. New `ScimSchemaHelpers.validateFilterPaths()` method + Generic-specific `validateFilterAttributePaths()` private method. **Tests:** 24 unit (3 RequireIfMatch 428 + 2 filter error + 6 validateFilterPaths + 9 generic service + 1 users + 1 groups + 2 helpers[strict]), 15 E2E (10 generic-parity-fixes + 5 generic-parity-fixes[Groups filter, RequireIfMatch 428, DELETE If-Match]), 11 live (section 9y). P3 doc recreated from source. **2,741 unit (73 suites), 651 E2E (32 suites), 659 live (647 pass, 12 gaps) - ALL PASSING.** |
| 2026-03-03 | ✅ **Attribute Characteristics E2E Gap Closure (v0.26.0):** Comprehensive 5-step audit per `addMissingTests.prompt.md` - analyzed all 31 E2E test files against RFC 7643 §2 attribute characteristics matrix. Identified 6 coverage gaps: User uniqueness:server 409 on PUT/PATCH (was only tested for Groups), case-insensitive uniqueness on PUT, returned:request on PATCH response (was tested for GET/LIST/POST/PUT but not PATCH), returned characteristics on `.search` (basic projection tested but no returned characteristic verification), required:true on PUT (was only tested for POST). No implementation bugs found - all gaps were test coverage gaps. **New tests:** 10 E2E (user-uniqueness-required.e2e-spec.ts: 4 PUT + 4 PATCH + 2 required), 9 E2E (returned-request.e2e-spec.ts: 2 PATCH + 7 .search), 16 live (section 9x: PUT/PATCH uniqueness + required + returned on PATCH/.search). **2,717 unit (73 suites), 636 E2E (33 suites), 570 live - ALL PASSING.** |
| 2026-03-03 | 🔎 **Deep Source Re-Audit (One More Pass):** Re-validated latest Generic/custom-resource parity changes directly in source (not docs). Confirmed previously open `GEN-01..GEN-12` items are now implemented in `endpoint-scim-generic.service.ts` + `endpoint-scim-generic.controller.ts` (schema validation, immutable, boolean coercion/sanitization, projection, uniqueness, reprovision, strict URN enforcement, soft-delete guard). Focused tests green: `endpoint-scim-generic.service.spec.ts` (**46/46**) and `scim-attribute-projection.spec.ts` (**45/45**). Residual P0/P1 gaps now narrowed to: generic `RequireIfMatch` parity (428 behavior), generic filter compliance/invalidFilter handling, runtime wiring of `validateFilterAttributePaths()`, and security/runtime hardening (`scim-auth.guard.ts` legacy token/logging, `main.ts` permissive CORS + `/scim/v2` rewrite). Updated `docs/P3_REMAINING_ATTRIBUTE_CHARACTERISTIC_GAPS.md` with re-audit delta. |
| 2026-03-02 | 🧪 **Comprehensive Test Gap Audit (v0.24.0):** Per `addMissingTests.prompt.md` - 4-step audit: inventory, gap identification, implementation, verification. **New unit tests (+108):** 7 missing config flag validation blocks (flags 8, 10-15: RequireIfMatch, ReprovisionOnConflict, CustomResourceTypes, BulkOperations, PerEndpointCredentials, IncludeWarning, IgnoreReadOnly) with 14 tests each (accept bool/string variants, reject invalid types, error messages). All-flags-combined block (4 tests). DEFAULT_ENDPOINT_CONFIG block (2 tests). **New E2E tests (+27):** RFC 7644 §3.12 HTTP error status coverage (6 tests: unknown path, 401 no auth, 404 SCIM format, 400 missing fields). Content-Type acceptance (2 tests). SCIM status always string (3 tests). Write-response projection (4 tests: POST/PUT/PATCH + ?attributes=, POST + ?excludedAttributes=). Attribute projection edge cases (5 tests: password never, id/schemas always, empty attrs, mixed case). Flag combinations (7 tests: StrictSchema+BooleanStrings, MultiOp pairs, RequireIfMatch+VerbosePatch, Reprovision without SoftDelete, invalid config rejection). **2,682 unit (73 suites), 585 E2E (27 suites), 535 live - ALL PASSING.** |
| 2026-03-02 | 📚 **Post-Test-Gap Documentation Freshness Audit (v0.24.0):** Per `auditAndUpdateDocs.prompt.md` - 6-step comprehensive audit after test gap audit bumped counts. Subagent-assisted scan found **59 stale items** across **28 files**. Fixed ALL: test counts propagated (2,682/585/535) to README, CHANGELOG, SCIM_COMPLIANCE, DOCKER_GUIDE, PROJECT_HEALTH_AND_STATS (+ ts-jest 29→30, flag names corrected to canonical), TESTING-WORKFLOW, TECHNICAL_DESIGN_DOCUMENT, SCIM_RFC_COMPLIANCE_LAYER, RFC7643_AUDIT, P2_ENFORCEMENT, COMPLETE_API_REFERENCE (v0.19.3→v0.24.0), ATTRIBUTE_CHARACTERISTICS_GAPS, DISCOVERY_ENDPOINTS_RFC_AUDIT, RFC_ATTRIBUTE_CHARACTERISTICS_ANALYSIS, PHASE_08_REMAINING_ANALYSIS (4 locations), LIVE_TEST_NORMS (ASCII diagram + JSON version), SCIM_VALIDATION_GAP_ANALYSIS (baseline notes + header), SCIM_GROUP_PERFORMANCE_ANALYSIS, RUNTIME_UPGRADE_ANALYSIS, PHASE_08_SCHEMA_VALIDATION, USER_API_CALL_TRACE, README_VISUAL_STUDIO_DEBUG, AZURE_DEPLOYMENT_ISSUES_AND_FIXES, INMEMORY_ARCHITECTURE, ISSUES_BUGS_RCA, MIGRATION_AUTOMATION_STRATEGY, RECOMMENDED_DESIGN_IMPROVEMENTS (2 locations). INDEX.md: fixed broken link (PHASE_04_FILTER_PUSHDOWN→PUSH_DOWN), corrected flag count "15 boolean→14 boolean + logLevel", added 15 missing doc entries (COMPLETE_AGNOSTIC, CURRENT_STATE, DISCOVERY_SCHEMAS, REPOSITORY_INTERFACE, RFC_COMPLIANCE_REQS, scim-extensions (2), UUID, VALIDATOR_RESULTS_26, PHASE_01/02/03, IDEAL_SCIM (2), MIGRATION_PLAN_FRESH). Self-updated `auditAndUpdateDocs.prompt.md` (flag refs 11→14+logLevel, feature patterns expanded). Cross-consistency verified: zero remaining stale "2,357"/"2,573"/"558"/"ts-jest 29"/"15 boolean flags" references. |
| 2026-03-01 | 📚 **Documentation Freshness Audit (v0.24.0):** Comprehensive audit of 20+ documentation files against ground truth. Fixed **50+ stale items** across **18 files**: version headers updated to v0.24.0 / March 2026 (README, DEPLOYMENT, TESTING-WORKFLOW, SCIM_COMPLIANCE, SCIM_RFC_COMPLIANCE_LAYER, LOGGING_AND_OBSERVABILITY, AZURE_DEPLOYMENT_AND_USAGE_GUIDE, SCIM_REFERENCE, TECHNICAL_REQUIREMENTS_DOCUMENT, REMOTE_DEBUGGING_AND_DIAGNOSIS, MULTI_ENDPOINT_GUIDE, COMPLETE_API_REFERENCE, H1_H2, PHASE_08, LIVE_TEST_NORMS). Content fixes: TECHNICAL_DESIGN_DOCUMENT (ER diagram rewrite ScimUser/ScimGroup→ScimResource, model count 5→7, CUID→UUID, rawPayload→payload JSONB, footer SQLite→Historical Note), SCIM_RFC_COMPLIANCE_LAYER (SQLite→PostgreSQL 17, userNameLower→CITEXT, filter push-down updated), SCIM_COMPLIANCE (Entra compat 95→100%, caseExact marked resolved), PROJECT_HEALTH_AND_STATS (test counts 3,517→3,666, flags 11→14+logLevel, models 5→7), CONTEXT_INSTRUCTIONS (DB models ScimUser→ScimResource, rawPayload→payload), REMOTE_DEBUGGING (4× SQLite→PostgreSQL, scimUser→scimResource), MULTI_ENDPOINT_GUIDE (diagram+Prisma block ScimUser/ScimGroup→ScimResource), DOCKER_GUIDE (example versions v0.10.0→v0.24.0), version-latest.json (v0.19.3→v0.24.0). Removed stale BackupService tech debt, updated RFC compliance to 100%. Cross-consistency verified: no stale ScimUser/ScimGroup/GroupMember/rawPayload/userNameLower/SQLite in any living doc. |
| 2026-03-01 | ✅ **P2 Attribute Characteristics - 6 Behavioral Gap Fixes (v0.24.0):** RFC 7643 §2 full audit P2 items implemented with comprehensive tests at all 3 levels. **R-RET-1**: Schema-driven `returned:'always'` at projection level. **R-RET-2**: Group `active` always returned. **R-RET-3**: Sub-attribute `returned:'always'` enforcement (e.g., `emails.value`, `members.value`). **R-MUT-1**: `writeOnly` mutability → `returned:never` defense-in-depth. **R-MUT-2**: readOnly sub-attribute stripping within readWrite parents (core+ext, single+multi-valued). **R-CASE-1**: caseExact-aware in-memory filter evaluation. Modified: `schema-validator.ts`, `scim-attribute-projection.ts`, `scim-service-helpers.ts`, `scim-filter-parser.ts`, `apply-scim-filter.ts`, all 3 controllers, both services. New E2E: `p2-attribute-characteristics.e2e-spec.ts` (13 tests). New live: section 9v (13 tests). **2,573 unit (73 suites), 558 E2E (27 suites), 535 live - ALL PASSING.** |
| 2026-03-01 | 🧹 **Blob/BackupService Dead Code Elimination (v0.23.0):** Complete removal of SQLite-era blob snapshot backup system. Deleted: `api/src/modules/backup/` (BackupModule + BackupService), `api/src/bootstrap/blob-restore.ts`, `infra/blob-storage.bicep`, `LogCategory.BACKUP` enum. Uninstalled `@azure/identity` + `@azure/storage-blob` npm packages. Removed all `BLOB_BACKUP_*` env vars from `infra/containerapp.bicep` + `docker-compose.yml`. Cleaned `AppModule` import. Updated `scripts/deploy-azure.ps1` to clean 5-step flow (no blob RBAC step). TypeScript compile Exit 0. **Comprehensive docs sweep: 9 active docs updated** - README.md (4 changes), DEPLOYMENT.md (5 changes), CONTEXT_INSTRUCTIONS.md, AZURE_DEPLOYMENT_AND_USAGE_GUIDE.md, COMPLETE_API_REFERENCE.md, PROJECT_HEALTH_AND_STATS.md, LOGGING_AND_OBSERVABILITY.md, REMOTE_DEBUGGING_AND_DIAGNOSIS.md, TECHNICAL_DESIGN_DOCUMENT.md (version 1.1→1.2, PostgreSQL 17 tech stack), TECHNICAL_REQUIREMENTS_DOCUMENT.md (FR-600–FR-607 rewritten for PostgreSQL), DOCKER_GUIDE_AND_TEST_REPORT.md (historical banner added). Database persistence: PostgreSQL 17 with Azure-managed PITR backup (replaces blob snapshots). |
| 2026-02-28 | ✅ **ReadOnly Attribute Stripping & Warnings - Full Coverage (v0.22.0):** RFC 7643 §2.2 enforcement for Users, Groups, AND Generic (custom) resource types. POST/PUT payloads automatically strip `mutability: 'readOnly'` attributes (`id`, `meta`, `groups`, custom readOnly). PATCH operations targeting readOnly attributes silently stripped (behavior matrix: strict OFF → strip; strict ON + `IgnoreReadOnlyAttributesInPatch` → strip; strict ON without flag → G8c 400). Warning URN extension (`urn:scimserver:api:messages:2.0:Warning`) on responses when `IncludeWarningAboutIgnoredReadOnlyAttribute` enabled. 2 new config flags (14th, 15th). Generic service wire-up with dynamic schema resolution via `getSchemaDefinitions()`. **Critical bug fix:** `AsyncLocalStorage.enterWith()` loses context across NestJS interceptor pipeline - replaced with Express middleware using `storage.run()` + mutating `setContext()`. `EndpointContextStorage.createMiddleware()` registered on all routes via `ScimModule.configure()`. 17 new E2E tests (`readonly-stripping.e2e-spec.ts`), 10 new live tests (section 9t), 21 `EndpointContextStorage` unit tests (expanded from 11). Bug fix BF-1: Groups `id` was client-controllable, now always `randomUUID()`. **2,532 unit (73 suites), 539 E2E (26 suites) - ALL PASSING.** |
| 2026-02-27 | � **Zero Test Failures - SchemaValidator id catch-22 Fix (v0.21.0):** Resolved ALL 65 pre-existing test failures (24 unit + 41 E2E). **Root Cause A (59 failures):** `id` attribute defined as `required: true` + `mutability: 'readOnly'` created impossible validation - omitting `id` failed required check, including `id` failed readOnly check. Fixed by adding `attr.mutability !== 'readOnly'` guard to required-attribute validation in `schema-validator.ts` (RFC 7643 §2.2: readOnly attributes are server-assigned). Applied to both core and extension attribute checks. **Root Cause B (6 failures):** G8f PUT uniqueness test called `replaceGroupForEndpoint` twice but mocked `findWithMembers` only once - second call got undefined → 404 instead of 409. Added re-mock before second call (5 cascade failures auto-resolved by Root Cause A fix). **Results: 73/73 unit suites (2,508 tests), 25/25 E2E suites (522 tests) - ALL PASSING.** |
| 2026-02-27 | �📚 **3-Tier Auth Documentation Bulk Update (v0.21.0):** Comprehensive audit of 13 files referencing authentication; 8 found stale (only showing 2-tier auth). Updated 10 files to reflect 3-tier fallback chain (per-endpoint bcrypt → OAuth JWT → global shared secret): COMPLETE_API_REFERENCE (auth section rewritten, credential admin routes added), TECHNICAL_DESIGN_DOCUMENT (§3.1 pipeline + §7.1 Dual→3-Tier with full ASCII diagram), DEPLOYMENT (auth section + credential CRUD examples), README (env var table + Entra config with per-endpoint option), CONTEXT_INSTRUCTIONS (§5.5 expanded with credential admin routes + source files), docker-compose.debug.yml (added all auth env vars), REPO_API_UNDERSTANDING_BASELINE (§3 rewritten), AZURE_DEPLOYMENT_AND_USAGE_GUIDE (auth section with 3-tier table + credential workflow), DOCKER_GUIDE (§8 env vars + auth model table), ENDPOINT_CONFIG_FLAGS_REFERENCE (PerEndpointCredentialsEnabled added to applicability matrix). |
| 2026-02-27 | ✅ **Phase 11 - Per-Endpoint Credentials (G11, v0.21.0):** LAST remaining migration gap resolved. `EndpointCredential` Prisma model with bcrypt-hashed tokens, cascade delete. `PerEndpointCredentialsEnabled` per-endpoint config flag (12th boolean flag, default false). `AdminCredentialController` (POST/GET/DELETE) at `/admin/endpoints/{id}/credentials` - generates 32-byte base64url tokens, bcrypt 12 rounds, returns plaintext once. `SharedSecretGuard` extended with 3-tier fallback: per-endpoint bcrypt → OAuth JWT → global SCIM_SHARED_SECRET. Lazy bcrypt loading via dynamic import. Credential repo (Prisma + InMemory) with active + non-expired filtering. **All 27 gaps (G1–G20) now resolved. 33 unit + 16 E2E + 22 live tests. 2,581 unit (77 suites), 522 E2E (25 suites), 485 live - all new tests passing.** |
| 2026-02-27 | ✅ **Phase 10 - /Me Endpoint + Phase 12 - Sorting + G17 Service Dedup (v0.20.0):** Three features delivered simultaneously. **Phase 10 (/Me - RFC 7644 §3.11):** `ScimMeController` resolves JWT `sub` → `userName` lookup → delegates GET/PUT/PATCH/DELETE to Users service. Supports attribute projection. 11 unit + 10 E2E + 15 live tests. **Phase 12 Sorting (G12 - RFC 7644 §3.4.2.3):** `scim-sort.util.ts` maps SCIM attributes to DB columns. Controllers accept `sortBy`/`sortOrder` on GET and POST `/.search`. SPC: `sort.supported: true`. 20 unit + 14 E2E + 11 live tests. **G17 Service Dedup:** Extracted 13+ duplicate methods from Users/Groups services into `scim-service-helpers.ts` (pure functions + `ScimSchemaHelpers` class). Users: −29% (904→640 lines), Groups: −28% (1005→726 lines). 43 unit tests. Created `docs/PHASE_10_ME_ENDPOINT.md`, `docs/PHASE_12_SORTING_AND_DEDUP.md`. Open gaps: 4→**1 (G11 per-endpoint credentials)**. **2,548 unit (75 suites), 506 E2E (24 suites), 463 live - all new tests passing.** |
| 2026-02-27 | ✅ **Phase 11 - Per-Endpoint Credentials (v0.21.0):** Gap G11 CLOSED. `EndpointCredential` Prisma model with bcrypt-hashed storage, optional expiry, cascade delete. `PerEndpointCredentialsEnabled` 12th boolean flag (default: false). `AdminCredentialController` at `/admin/endpoints/{id}/credentials` (POST generate 32-byte token, GET list, DELETE revoke). 3-tier auth fallback in `SharedSecretGuard`: per-endpoint bcrypt → OAuth JWT → global secret. Lazy bcrypt loading with caching. `IEndpointCredentialRepository` with Prisma + InMemory implementations. 33 new unit tests (14 controller + 19 guard), 16 E2E tests, 22 live tests (section 9s). **All 27 migration gaps (G1–G20) now fully resolved. SCIM compliance 100%.** Created `docs/G11_PER_ENDPOINT_CREDENTIALS.md`. **2,581 unit (77 suites), 522 E2E (25 suites), 485 live - all passing.** |
| 2026-02-27 | ✅ **Phase 10 + Phase 12 + G17 (v0.20.0):** (1) `/Me` endpoint (RFC 7644 §3.11) - `ScimMeController`, JWT `sub`→`userName` resolution, full GET/PUT/PATCH/DELETE, attribute projection. 11 unit + 10 E2E + 15 live tests. (2) Sorting (RFC 7644 §3.4.2.3) - `scim-sort.util.ts`, `sortBy`/`sortOrder` on all list + POST /.search, `sort.supported: true`. 20 unit + 14 E2E + 11 live tests. (3) G17 service deduplication - `scim-service-helpers.ts`; Users −29%, Groups −28%; 43 unit tests. Created `docs/PHASE_10_ME_ENDPOINT.md`, `docs/PHASE_12_SORTING_AND_DEDUP.md`. **2,548 unit (75 suites), 506 E2E (24 suites), 463 live - all passing.** |
| 2026-02-26 | ✅ **Multi-Tenant Discovery Architecture (v0.19.3):**** Established two-tier discovery model - root-level routes (`/scim/v2/...`) serve global defaults for admin tooling; endpoint-scoped routes (`/scim/endpoints/{id}/...`) are PRIMARY for multi-tenant consumers (per-tenant schemas, resource types, config via `ScimSchemaRegistry` overlay merging). Updated JSDoc on all 4 discovery controllers. 14 new unit tests (7 controller + 7 service, spy-verified endpointId passthrough). 9 new E2E tests (SPC per-endpoint BulkOperationsEnabled, two endpoints different configs, endpoint-scoped schemas/RTs, unauthenticated access). Updated 4 docs (DISCOVERY_ENDPOINTS_RFC_AUDIT §3.5, COMPLETE_API_REFERENCE multi-tenant restructure, CONTEXT_INSTRUCTIONS, CHANGELOG). **124 discovery unit tests (5 suites), 35 discovery E2E tests (1 suite) - all passing.** |
| 2026-02-26 | ✅ **Discovery Endpoints D1–D6 Remediation (v0.19.3):** All 6 RFC audit gaps resolved. D1: `@Public()` on 4 discovery controllers (auth bypass). D2: `GET /Schemas/{uri}` individual lookup + SCIM 404. D3: `GET /ResourceTypes/{id}` individual lookup + SCIM 404. D4: `schemas[]` on Schema resources. D5: `schemas[]` on ResourceType resources. D6: `primary:true` on auth scheme. New constants `SCIM_SCHEMA_SCHEMA`, `SCIM_RESOURCE_TYPE_SCHEMA`. New service methods `getSchemaByUrn()`, `getResourceTypeById()`. Interface updates for `schemas?` property. 26 new unit tests + 16 new E2E tests. Discovery compliance: 85% → **100%**. |
| 2026-02-26 | 📋 **Discovery Endpoints RFC Audit (D1–D6):** Comprehensive audit of `/ServiceProviderConfig`, `/ResourceTypes`, `/Schemas` against RFC 7643 §5–§7 + RFC 7644 §4. Found 6 gaps: D1 (HIGH - discovery endpoints behind auth, RFC says SHALL NOT require auth), D2 (MEDIUM - no `GET /Schemas/{uri}` individual lookup), D3 (MEDIUM - no `GET /ResourceTypes/{id}`), D4 (LOW - Schema resources missing `schemas` array), D5 (LOW - ResourceType resources missing `schemas` array), D6 (VERY LOW - SPC `authenticationSchemes` missing `primary` flag). SCIM_COMPLIANCE discovery score downgraded 100%→85%. Created `docs/DISCOVERY_ENDPOINTS_RFC_AUDIT.md`. Updated INDEX, SCIM_COMPLIANCE, RFC gap matrix, CONTEXT_INSTRUCTIONS. |
| 2026-02-26 | ✅ **G8g - Write-Response Attribute Projection (v0.19.2):** RFC 7644 §3.9 compliance - `attributes`/`excludedAttributes` query parameters now honored on POST/PUT/PATCH write responses. Replaced 6 inline `returned:'request'` stripping loops with `applyAttributeProjection()` calls in both controllers. Removed unused `stripReturnedNever` import. 23 new unit tests (precedence, returned:request interaction, always-returned protection, dotted sub-attrs), 14 E2E tests, 33 live tests (section 9p). Created `docs/G8G_WRITE_RESPONSE_ATTRIBUTE_PROJECTION.md`. Updated 5 docs (SCIM_COMPLIANCE, CONTEXT_INSTRUCTIONS, RFC_ATTRIBUTE_CHARACTERISTICS_ANALYSIS). Gap G8g: ✅ CLOSED. **2,353 unit (69 suites), 455 E2E (22 suites), 444 live - all passing.** |
| 2026-02-26 | ✅ **G8f - Group Uniqueness on PUT/PATCH (v0.19.1):** Fixed data integrity bug - `assertUniqueDisplayName()` and `assertUniqueExternalId()` were defined but never called on PUT/PATCH paths (only POST had inline checks). Both methods now called with `excludeScimId` self-exclusion on `replaceGroupForEndpoint` and `patchGroupForEndpoint`. 10 new unit tests, 6 E2E tests, 10 live tests (section 9o). Created `docs/G8F_GROUP_UNIQUENESS_PUT_PATCH.md`. Gap G8f: ✅ CLOSED. **2,330 unit (69 suites), 441 E2E (22 suites), 411 live - all passing.** |
| 2026-02-26 | ✅ **Phase 9 - Bulk Operations (RFC 7644 §3.7, v0.19.0):** `POST /endpoints/:endpointId/Bulk` for batch SCIM processing. Per-endpoint `BulkOperationsEnabled` config flag (default: false, 11th boolean flag). `BulkProcessorService` (395 lines): sequential operation processing with `bulkId` cross-referencing (`Map<string, string>`), `failOnErrors` threshold, per-operation error isolation. `BulkController` with config flag gate, schema URN validation, payload size guard (1MB). DTOs: `BulkOperationDto`, `BulkRequestDto`, `BulkOperationResult`, `BulkResponse`. SPC: `bulk.supported=true, maxOperations=1000, maxPayloadSize=1048576`. New error type: `TOO_LARGE: 'tooLarge'`. Created `docs/PHASE_09_BULK_OPERATIONS.md`. Updated `docs/ENDPOINT_CONFIG_FLAGS_REFERENCE.md` (now 11 boolean flags + logLevel). **2,320 unit (69 suites), 435 E2E (22 suites), 401+ live - all passing.** |
| 2026-02-26 | ✅ **G8b - Custom Resource Type Registration (v0.18.0):** Data-driven extensibility beyond User/Group. Per-endpoint `CustomResourceTypesEnabled` config flag (default: false). Admin API (POST/GET/GET/:name/DELETE/:name) at `/admin/endpoints/:endpointId/resource-types`. Generic SCIM CRUD controller with wildcard `:resourceType` routing (registered LAST in module). `GenericPatchEngine` for JSONB-based PATCH with URN-aware paths. `EndpointResourceType` DB table with cascade-delete. `ScimSchemaRegistry` enhanced with per-endpoint resource type overlay. Reserved name/path protection. 15 new implementation files, 6 modified files. Created `docs/G8B_CUSTOM_RESOURCE_TYPE_REGISTRATION.md`. **2,277 unit (67 suites), 411 E2E (21 suites), 20 new live tests - all passing.** |
| 2026-02-25 | ✅ **G8e - Response `returned` Characteristic Filtering (v0.17.4):** Two-layer RFC 7643 §2.4 compliance: (1) Service layer strips `returned:'never'` in `toScim*Resource()` for ALL responses (POST/PUT/PATCH/GET/LIST). (2) Controller layer strips `returned:'request'` via enhanced `applyAttributeProjection()` for read ops, direct stripping for write ops. Added `password` to User schema constants (`returned:'never'`, `mutability:'writeOnly'`). Added `SchemaValidator.collectReturnedCharacteristics()`, `stripReturnedNever()` export, `getRequestOnlyAttributes()` on both services. 40 new unit + 8 E2E tests (service-level + controller-level + projection + validator). Created `docs/G8E_RETURNED_CHARACTERISTIC_FILTERING.md`. Gap G8e: ✅ CLOSED. **2,156 unit (61 suites), 382 E2E (20 suites) - all passing.** |
| 2026-02-25 | ✅ **G8c - PATCH readOnly Pre-Validation (v0.17.3):** Implemented readOnly mutability enforcement in `SchemaValidator.validatePatchOperationValue()` - rejects PATCH add/replace/remove on readOnly attributes (e.g., `groups`) with 400. Added `resolveRootAttribute()` for value-filter paths. Added `groups` attribute to `USER_SCHEMA_ATTRIBUTES` (RFC 7643 §4.1 - was missing entirely). Gated behind `StrictSchemaValidation`. 25 new unit tests + 7 new E2E tests. Created `docs/G8C_PATCH_READONLY_PREVALIDATION.md`. Gap G8c: ✅ CLOSED. **2,116 unit (61 suites), 374 E2E (19 suites), 334/334 live - all passing.** |
| 2026-02-25 | ✅ **Parallel E2E Test Execution:** Removed `resetDatabase()` from 17 E2E specs, switched to worker-prefixed fixtures (`w${JEST_WORKER_ID}-`) and dynamic endpoint names for test isolation. Updated `global-teardown.ts` to PostgreSQL TRUNCATE. Increased `maxWorkers` from 1→4. E2E runtime dropped from ~64s→~22s (65% faster). Updated `docs/MIGRATION_PLAN_CURRENT_TO_IDEAL_v3` gap table (17 gaps marked resolved), heat map, phase overview. Comprehensive doc staleness audit across README, CONTEXT_INSTRUCTIONS, Session_starter, CHANGELOG, package.json. **2,096 unit (61 suites), 368 E2E (19 suites), 334/334 live - all passing.** |
| 2026-02-25 | ✅ **externalId CITEXT→TEXT (RFC 7643 §3.1 caseExact compliance):** Changed `externalId` column from `@db.Citext` to `@db.Text` in Prisma schema. Added `'text'` column type to filter engine - `co`/`sw`/`ew` operators on `text` columns are now case-sensitive (no `mode: 'insensitive'`). Migration: `20260225181836_externalid_citext_to_text`. Updated 5 E2E, 5 unit, 4 live tests. Created `docs/EXTERNALID_CITEXT_TO_TEXT_RFC_COMPLIANCE.md` (350+ lines). Resolved gap G8f. **2,096 unit (61 suites), 368 E2E (19 suites), 334/334 live - all passing, 0 known failures.** |
| 2026-02-25 | ✅ **v0.17.2 - AllowAndCoerceBooleanStrings + ReprovisionOnConflict + Soft-Delete deletedAt + In-Memory Support + RFC Fixes:** (1) Implemented `AllowAndCoerceBooleanStrings` config flag (default `true`) - coerces boolean-typed string values ("True"/"False") to native booleans before schema validation on all write paths. Schema-aware: only coerces attributes whose schema type is "boolean" (V16/V17 fix). Fixes all 17 SCIM Validator #26 failures. Added `getConfigBooleanWithDefault()` helper. (2) Implemented `ReprovisionOnConflictForSoftDeletedResource` config flag (default `false`) - when enabled alongside SoftDeleteEnabled, POST conflicts with soft-deleted resources re-activate the existing resource (clears `deletedAt`, sets `active=true`, replaces payload) instead of 409. 10th boolean flag. (3) Soft-delete now tracks `deletedAt` timestamp - guard uses `deletedAt != null` (not `active === false`) to distinguish soft-deleted from PATCH-disabled resources. New Prisma column: `deletedAt DateTime? @db.Timestamptz`. Domain models updated: `UserRecord`, `GroupRecord`, `UserUpdateInput`, `GroupUpdateInput`, `UserConflictResult`. (4) Group `active` field - Groups now include `active: boolean` in domain models and SCIM responses. (5) In-memory persistence for `EndpointService` and `LoggingService` - both detect `PERSISTENCE_BACKEND=inmemory` and use in-memory stores instead of Prisma. (6) Resource-type-aware attribute projection - `displayName` always-returned only for Groups per RFC 7643. (7) externalId case sensitivity - per RFC 7643 §2.4 `caseExact: true`, case-variant externalId values are distinct. (8) New `SchemaValidator.collectBooleanAttributeNames()` + `validateFilterAttributePaths()` (V32). (9) New `scim-filter-parser.ts` for filter AST attribute extraction. (10) Startup warning when StrictSchemaValidation is OFF. Created `docs/ENDPOINT_CONFIG_FLAGS_REFERENCE.md` - all 10 boolean flags + logLevel documented. **2063 unit tests (61 suites), 358 E2E tests (19 suites), 334/334 live tests - all passing on both local and Docker in-memory instances.** |
| 2026-02-24 | 📚 **Documentation Staleness Audit & Bulk Update (v0.17.1):** Comprehensive audit of 14+ documentation files against ground truth (1962 unit/59 suites, 342 E2E/19 suites, 318 live, SchemaValidator 816 lines, PostgreSQL 17, version-based ETags). Fixed ~73 stale items: README (version, architecture diagram SQLite→PostgreSQL, test counts), CHANGELOG (added v0.17.1 + v0.17.1-fix1 entries), CONTEXT_INSTRUCTIONS (12 fixes: SQLite→PostgreSQL, service line counts, Prisma models, test counts, gotchas), SCIM_COMPLIANCE (ETag 95→100%, compliance 95→96%, new v0.17.1 section), COMPLETE_API_REFERENCE (ETag format, version), DEPLOYMENT (Docker Compose rewritten with postgres:17-alpine), TESTING-WORKFLOW, REPO_API_UNDERSTANDING_BASELINE, DOCKER_GUIDE_AND_TEST_REPORT, STORAGE_AND_BACKUP (marked historical), PHASE_08_SCHEMA_VALIDATION, PHASE_08_REMAINING_ANALYSIS, INDEX. Bumped `api/package.json` to 0.17.1. |
| 2026-02-24 | ✅ **V2-V31 Adversarial Validation Gap Closure (commit 1ae3453):** Closed 30 of 33 adversarial validation gaps with schema + patch + DTO hardening. `SchemaValidator` grew from 594→816 lines: added `canonicalValues` enforcement, string/array size limits (`maxStringLength`, `maxArrayLength`), payload size limit (`maxPayloadSize`), `uniqueness` checking, deep sub-attribute recursion fixes. DTO hardening: `SearchRequestDto` (filter length, startIndex/count range), `CreateUserDto`/`PatchUserDto`/`CreateGroupDto`/`PatchGroupDto` (class-validator decorators). Patch engine limits: `maxPatchOps` (100), `maxPatchValueSize` (100KB). 5 new test files (2853 lines): `schema-validator-adversarial.spec.ts`, `schema-validator-edge-cases.spec.ts`, `dto-validation.spec.ts`, `patch-engine-hardening.spec.ts`, `group-patch-engine-hardening.spec.ts`. **1962 unit tests (59 suites) - all passing.** |
| 2026-02-24 | ✅ **H-1 & H-2 - PATCH Validation + Immutable Enforcement:** Implemented `SchemaValidator.checkImmutable()` - pure domain method for RFC 7643 §2.2 immutable attribute enforcement (old-vs-new comparison). Added post-PATCH `SchemaValidator.validate()` in both user and group services (H-1). Added `checkImmutable()` calls in PUT + PATCH for both services (H-2). Refactored `validatePayloadSchema()` to support `'patch'` mode. Added `buildSchemaDefinitions()` and `buildExistingPayload()` shared helpers. Added 14 new `checkImmutable` unit tests (schema-validator.spec.ts). Created `docs/H1_H2_ARCHITECTURE_AND_IMPLEMENTATION.md` - architecture analysis, design deliberation, implementation plan. Updated gaps doc, INDEX.md. All gated behind `StrictSchemaValidation` flag. **1711 unit tests (54 suites) - all passing.** |
| 2026-02-24 | 🔄 **Adversarial Gap Reanalysis (33→28 open):** Reanalyzed all 33 validation gaps against latest codebase (1711 unit tests, 54 suites). **2 RESOLVED:** V4 (payload size - `json({ limit: '5mb' })` in `main.ts`), V8 (immutable enforcement - `SchemaValidator.checkImmutable()` + service integration). **3 PARTIALLY FIXED:** V2 (post-PATCH `validatePayloadSchema()` added but input values still not pre-validated), V9 (sub-attr type checking works but required sub-attr enforcement missing), V25 (core schema URN presence checked but no format/duplicate validation). **28 STILL OPEN.** Updated `PHASE_08_REMAINING_ANALYSIS.md` §7.5 with status columns, reanalysis section, revised effort estimates (~27-35 hrs). Updated Migration Plan gaps G8d (✅ resolved), G8g (V4 resolved), heat map. |
| 2026-02-24 | 📊 **Adversarial Client Validation Gap Analysis (33 Gaps):** Comprehensive security/validation audit of SchemaValidator, PatchEngine, DTOs, filter parser, and service layer assuming adversarial SCIM clients (not just Entra ID). Identified **33 validation gaps** (8 HIGH, 12 MEDIUM, 13 LOW): schema validation disabled by default, PATCH ops bypass schema validation, prototype pollution risk, required sub-attrs not checked, `canonicalValues` not enforced, filter parser no recursion depth limit, `sanitizeBooleanStrings` corrupts data, group `id` client-assignable, `dateTime` permissive. Root cause: (1) validation opt-in by default, (2) PATCH bypasses all schema checks. Mapped to sub-phases 8.5-8.8. Documented in `PHASE_08_REMAINING_ANALYSIS.md` §7.5 and migration plan gaps G8g-G8i. |
| 2026-02-24 | 📊 **RFC Attribute Characteristics Gap Analysis:** Created comprehensive `docs/RFC_ATTRIBUTE_CHARACTERISTICS_ANALYSIS.md` - 10 sections with Mermaid diagrams analyzing all 11 RFC 7643/7644 attribute characteristics (type, multiValued, required, mutability, returned, uniqueness, caseExact, canonicalValues, referenceTypes, description, subAttributes) against current project implementation. Identified **15 gaps (G1-G15)** with severity ratings, HTTP request/response examples, DB value representations, and remediation code snippets. Defined **sub-phases 8.1-8.5** for remaining attribute characteristic enforcement (~22-30 hours estimated). Updated Migration Plan v3 with new gaps (G8c-G8f), Phase 8 completion status, and sub-phase timeline. Updated docs INDEX with new entries. |
| 2026-02-24 | ✅ **Phase 8 - Schema Validation Engine Tests - COMPLETE (v0.17.0):** Created `SchemaValidator` domain class (383 lines) + `validation-types.ts` (70 lines) for pure RFC 7643 payload validation. Added **247 new tests**: 179 comprehensive unit tests (`schema-validator-comprehensive.spec.ts`, 20 describe blocks covering all 8 SCIM types, mutability, required attrs, unknown attrs, extensions, sub-attrs, multi-valued, edge cases), 19 service-level tests (11 Users + 8 Groups), 49 E2E tests (`schema-validation.e2e-spec.ts`, 14 sections). Key discovery: NestJS `ValidationPipe` with `enableImplicitConversion: true` coerces DTO-declared properties before schema validator runs - documented in E2E §13-§14. Final counts: **1685 unit (54 suites), 342 E2E (19 suites), 318 live** - all passing. Docker rebuilt and verified. |
| 2026-02-24 | ✅ **Phase 7 - ETag & Conditional Requests - COMPLETE (v0.16.0):** Replaced timestamp-based ETags (`W/"<ISO>"`) with version-based monotonic ETags (`W/"v{N}"`). Added pre-write `enforceIfMatch()` in both user and group services (412 on mismatch). New `RequireIfMatch` per-endpoint config flag (428 on missing header when enabled). Atomic version increment via `version: { increment: 1 }` in Prisma repos, `(existing.version ?? 1) + 1` in InMemory repos. Simplified ETag interceptor to read-side only (ETag header + If-None-Match→304). Added `version: number` to `UserRecord` and `GroupRecord`. Controllers extract `If-Match` header, pass to services. 19 files changed (617 insertions, 53 deletions). **41 new tests:** 24 unit (13 user + 11 group: If-Match match/mismatch/428/wildcard + ETag format) + 17 E2E (version format, If-Match enforcement, RequireIfMatch). Final counts: **1429 unit (52 suites), 293 E2E (18 suites)** - all passing. Phase doc: `docs/phases/PHASE_07_ETAG_CONDITIONAL_REQUESTS.md`. Gaps resolved: G7 (HIGH), G13 (MEDIUM). |
| 2026-02-23 | 🧪 **v0.15.0 Test Expansion - COMPLETE:** Added 59 new tests (34 unit + 25 E2E) for soft-delete, config flag combinations, and PATCH path patterns. **Unit tests:** 12 user service (soft-delete GET/LIST/filter, config combos), 8 group service (soft-delete interactions, flag combos), 14 user-patch-engine (soft-deleted PATCH, valuePath patterns, dot-notation combos). **E2E tests:** New `soft-delete-flags.e2e-spec.ts` - SoftDeleteEnabled Users (6), Groups (3), PATCH on soft-deleted users (4), config flag combinations (5), StrictSchemaValidation (3), PATCH path patterns (4). Fixed 2 pre-existing discovery-endpoints E2E failures (schema count assertions stale after custom extension URNs). Fixed `package.json` version 0.13.0→0.15.0. Fixed live-test parameter name mismatch (`-ClientSecret` not `-OAuthSecret`). Added 3 new issues (#9-#11) to RCA doc. Final counts: **1405 unit (52 suites), 276 E2E (18 suites), 318 live** - all passing. Updated CHANGELOG, INDEX, Session_starter, ISSUES_BUGS_ROOT_CAUSE_ANALYSIS. |
| 2026-02-23 | 📝 **Phase 8 Part 2 - Custom Resource Type Registration - DOCUMENTED:** Designed and documented comprehensive Phase 8.2 (Custom Resource Type Registration) in migration plan and architecture v3 docs. Covers: `EndpointResourceType` DB model + Prisma migration, `ENDPOINT_RESOURCE_TYPE_REPOSITORY` token + interface + Prisma/InMemory implementations, `ScimSchemaRegistry.registerResourceType()` with per-endpoint overlay + hydration, Admin API `POST/GET/DELETE /admin/endpoints/:id/resource-types`, `customResourceTypesEnabled` config flag (default false), generic wildcard `EndpointScimGenericController` (catches `:resourceType` after dedicated User/Group controllers), `EndpointScimGenericService` with JSONB-only storage + schema-guided validation, `GenericPatchEngine` for JSONB payloads, JSONB-only filter fallback. Full request/response examples, Mermaid sequence/architecture diagrams, discovery impact examples, runtime impact assessment, ~105 test plan, ~9 day effort estimate. Updated: migration plan (TOC, gap table G8b, phase overview, timeline, Gantt, dependency graph P8b node, deployment matrix, summary table), architecture v3 (config flag matrix, ER diagram columns, API route map with admin + generic routes, Appendix B.7b examples). No code changes - documentation only. |
| 2026-02-23 | ✅ **Phase 6 - Data-Driven Discovery - COMPLETE (v0.14.0):** Centralized all SCIM discovery endpoints into injectable `ScimDiscoveryService`, replacing ~280 lines of hardcoded JSON across 4 controllers. Added Enterprise User Extension schema (RFC 7643 §4.3) to `/Schemas` (3 schemas, was 2). Enterprise extension declared on User ResourceType. Dynamic `schemas[]` in User responses (G19). `meta` object on ServiceProviderConfig (RFC 7644 §4). Centralized `KNOWN_EXTENSION_URNS` (G16). Removed 7 dead config flags (G20). Rich RFC 7643 schema constants with full attribute definitions. Full validation: **1171/1171 unit tests** (47 suites, +36 new), **196/196 E2E tests** (15 suites, +3 new). Phase doc: `docs/phases/PHASE_06_DATA_DRIVEN_DISCOVERY.md`. |
| 2026-02-21 | ✅ **Phase 5 - Domain-Layer PATCH Engine - COMPLETE (v0.13.0):** Extracted inline SCIM PATCH logic into pure-domain `UserPatchEngine` (~290 lines) and `GroupPatchEngine` (~240 lines) with zero NestJS/Prisma dependencies. Created `PatchError` domain error class, typed `PatchConfig`/`GroupMemberPatchConfig` interfaces, barrel export. Refactored both services to thin orchestrators (34%/31% line reduction). Added 73 new unit tests (36+37). Full validation: **984/984 unit tests** (29 suites), **193/193 E2E tests** (15 suites). Docker build + container healthy. Phase doc: `docs/phases/PHASE_05_PATCH_ENGINE.md`. |
| 2026-02-21 | 🧪 **Comprehensive Test Gap Analysis & Expansion:** Identified 19 untested source files (59.6% gap rate). Created 14 new test files: `PatchError`, `createScimError`, `buildBaseUrl`, `ScimMetadataService`, `OAuthService` (16 tests), `OAuthController`, `SharedSecretGuard` (11 tests), `ScimAuthGuard`, `RequestLoggingInterceptor`, `ServiceProviderConfigController`, `ResourceTypesController`, `SchemasController`, `DatabaseController`, `AdminController` (18 tests), `WebController`. **Tests: 1135/1135 unit (46 suites) + 193/193 E2E (15 suites). +151 net new tests, +17 new suites.** |
| 2026-02-21 | ✅ **Phase 4 Verification Complete (v0.12.0):** Fixed `displayName` column population (create/replace/patch now write first-class DB column). No-cache Docker rebuild (`scimserver:latest` v0.12.0). Full validation: **911/911 unit tests** (29 suites), **193/193 E2E tests** (15 suites), **302/302 live tests** (Docker container). CHANGELOG Verified section added. |
| 2026-02-21 | 🔍 **Phase 4 - Filter Push-Down Expansion - COMPLETE:** Expanded SCIM filter push-down from eq-only on 3 columns to all 10 operators (eq/ne/co/sw/ew/gt/ge/lt/le/pr) on 5 columns (userName, displayName, externalId, scimId, active). Added column type annotations (citext/varchar/boolean/uuid) for operator validation. Implemented compound AND/OR recursive push-down. Created `prisma-filter-evaluator.ts` for InMemory backend parity. Updated InMemory user/group repos to use shared `matchesPrismaFilter()`. Comprehensive test updates: filter tests verify DB push-down for previously in-memory operators. Created Phase 4 docs. CHANGELOG updated to v0.12.0. |
| 2026-02-21 | ✅ **Full Validation Pipeline Re-Run (Clean Build + Local + Docker):** Completed clean API rebuild and full E2E verification (**193/193 passing**). Ran local instance live validation with latest flow-trace script (**302/302 passing**). Performed no-cache Docker rebuild (`scimserver:latest`), started container against host PostgreSQL, and ran full live suite again (**302/302 passing**). Updated changelog verification status to reflect fully green live matrix. |
| 2026-02-20 | 🌐 **Root SPA + Test Trace JSON Enhancements:** Refreshed root SPA static bundle sync (`web/dist` → `api/public`) to eliminate stale hashed asset mismatch and ensure `/` renders correctly in latest repo state. Added root non-SPA fallback section with quick links (version endpoint, recent logs, README, docs index). Enhanced E2E + live test JSON outputs with step-level flow tracing (request method/url/headers/body, response status/headers/body, timing, action-step IDs linked to assertions). Validated runs: live tests now **302/302 passed** with `totalFlowSteps` and per-test `actionStepIds` in `test-results/live-results-latest.json`; E2E JSON includes `flowSteps` and linked `actionStepIds` in `test-results/e2e-results-latest.json`. |
| 2026-02-21 | 🔧 **Version Endpoint Enhancement (Issue 19):** Added `container` block (app id/name/image/runtime/platform + database host/port/name/provider) when containerized, `utcOffset` field (`±HH:MM`), fixed `maskSensitiveUrl()` to mask userinfo in connection strings (`://user:pass@` → `://***:***@`). Updated web client `VersionInfo` type (sqlite→postgresql + new fields). E2E tests updated with `utcOffset` regex + userinfo masking assertions. SCIM Validator verified: **25/25 passed + 7 preview, 0 false positives** (all 4 previous FPs confirmed fixed). Updated docs: version-latest.json/html, COMPLETE_API_REFERENCE.md, SCIM_VALIDATOR_FALSE_POSITIVES_REPORT.md, PHASE_03_ISSUES_AND_RESOLUTIONS.md. All tests green: **862 unit** (28 suites), **193 E2E** (15 suites). |
| 2026-02-21 | 🔍 **False Positive Test Audit (Issue 18):** Comprehensive audit of all 1,357 tests across 3 levels. Found and fixed 29 false positives: 8 E2E (tautological assertions, empty-loop skips, conditional guards, overly permissive assertions across 5 files), 10 unit (weak `toBeDefined` assertions, no-assertion tests in 2 files), 11 live (hardcoded `$true`, unguarded deletes, vacuously-true loops in `live-test.ps1`). All tests validated green: **862 unit** (28 suites), **193 E2E** (15 suites). Updated 10 source files + 6 documentation files. |
| 2026-02-21 | �🐛 **SCIM ID Leak Fix + Comprehensive Test Coverage (Issues 16-17):** Fixed critical SCIM ID leak bug (client-supplied `id` in POST/PATCH body could override server-assigned `scimId` via `rawPayload` spread). Added `'id'` to `stripReservedAttributes()` reserved set. Updated version endpoint for Phase 3 (removed blob fields, added `persistenceBackend`, `connectionPool`, `migratePhase`). Bumped to v0.11.0. Added 24 new tests: 5 unit tests (ID leak prevention in create/get/patch), 4 E2E tests (POST/PUT/PATCH with client id, GET by server id), 15 live tests (Section 3e). All validation passed: **862 unit tests** (28 suites), **193 E2E tests** (15 suites), **301/302 live tests** on both local server + Docker container (1 pre-existing failure). Updated PHASE_03_ISSUES_AND_RESOLUTIONS.md (Issues 16-17) and PHASE_03_POSTGRESQL_MIGRATION.md (test counts, known issues). |
| 2026-02-21 | 🏗️ **Phase 1 Repository Pattern - COMPLETE:** Extracted full Repository Pattern abstraction from Prisma-direct services. Created 10 new files: domain models (`UserRecord`, `GroupRecord`, `MemberRecord`, `GroupWithMembers`), repository interfaces (`IUserRepository`, `IGroupRepository`), string tokens, Prisma implementations (thin wrappers), in-memory implementations (`Map`-based with secondary indexes), and `RepositoryModule.register()` dynamic module with `PERSISTENCE_BACKEND` env toggle (`prisma` default / `inmemory`). Refactored both SCIM services (`endpoint-scim-users.service.ts`, `endpoint-scim-groups.service.ts`) to use `@Inject(TOKEN)` pattern - zero Prisma imports remain in service layer. Transformed both spec files (~3000 lines total) from `mockPrismaService` to flat repository mocks with context-dependent method mapping. Updated filter module to `Record<string, unknown>`. All verification passed: 666 unit tests (19 suites), 184 e2e tests (15 suites), Docker build + container live tests (admin API, user CRUD, group with member, list operations). |
| 2026-02-20 | � **Migration Automation Strategy Doc:** Created comprehensive `docs/MIGRATION_AUTOMATION_STRATEGY_v1_2026-02-20.md` - 14-section automation analysis covering both PostgreSQL + In-Memory migration paths. Includes full codebase inventory (46 Prisma call sites across 4 services mapped to repository methods), per-phase automation assessment heatmap (60-95% automatable), AI vs Human effort breakdown tables, Copilot Agent session workflow with exact prompt templates, 2-developer parallel execution plan with conflict-free ownership matrix, risk quadrant chart with 8 risks + mitigations, CI pipeline changes for dual-driver testing, quality gate verification workflow, complete Prisma call-site inventory (4 appendix tables), generated file map (31 new + 17 modified files). Timeline: manual 16 weeks → automated 5-6 weeks for both paths (65% reduction). 12 Mermaid diagrams. |
| 2026-02-20 | �🧠 **In-Memory Architecture & Plan Doc:** Created comprehensive `docs/INMEMORY_ARCHITECTURE_AND_PLAN_v1_2026-02-20.md` - 16-section combined architecture, comparison, and migration plan for in-memory (`Map`-based) repository implementation. Includes full PostgreSQL vs In-Memory comparison matrices (feature parity, performance, operational), `InMemoryStore` with secondary indexes, complete `InMemoryResourceRepository`/`InMemoryMembershipRepository`/`InMemoryTenantRepository` implementations, filter/sort/pagination comparison, ETag/concurrency analysis, optional snapshot persistence, decision framework flowchart, 12-phase effort comparison (37% less LOC than PostgreSQL path), side-by-side code examples, 14 Mermaid diagrams, and deployment topology differences. |
| 2026-02-20 | 🚢 **Deployment Modes Analysis:** Added comprehensive Section 17 to Migration Plan v3 - 10 subsections covering all 4 deployment modes (Local Dev, Docker Debug, Docker Production, Azure Container Apps). Includes full file inventory, per-phase impact matrix, Phase 3 PostgreSQL diff-level changes for every deployment artifact (Dockerfile WASM reversal, entrypoint simplification, Bicep rewrite, new `postgresql.bicep`), Dockerfile consolidation plan (5→2), post-migration env var matrix, deployment testing strategy, and 10-step Azure cutover sequence with rollback plan. |
| 2026-02-20 | 🧭 **Architecture v3 + Migration Plan v3:** Deep re-analysis of all 28+ API source files, then created two comprehensive v3 design documents: `docs/IDEAL_SCIM_ARCHITECTURE_v3_2026-02-20.md` (18 sections: layered N-tier architecture, unified `scim_resource` table with `resource_type` discriminator, PostgreSQL JSONB/CITEXT/GIN schema, data-driven discovery via `tenant_schema`/`tenant_resource_type`, pure domain PatchEngine, monotonic integer ETags, Bulk/Me endpoints, per-tenant credentials, full API route map, Mermaid sequence diagrams, example JSON payloads) and `docs/MIGRATION_PLAN_CURRENT_TO_IDEAL_v3_2026-02-20.md` (18 gaps mapped, 12 phases with dependency graph, code-level before/after examples, line-of-code impact analysis, risk matrix, critical path P1→P2→P3→P5). |
| 2026-02-20 | 🧭 **Fresh RFC-First Architecture + Migration Blueprint:** Created two new from-scratch design artifacts independent of prior drafts: `docs/IDEAL_SCIM_ARCHITECTURE_RFC_FIRST_FRESH_2026-02-20.md` (complete ideal multi-tenant SCIM architecture, dynamic discovery, schema/resource-type persistence model, flows/diagrams, RFC rationale) and `docs/CURRENT_TO_IDEAL_MIGRATION_PLAN_FRESH_2026-02-20.md` (deep current-API gap mapping and phased implementation plan with concrete file-level workstreams, examples, and rollout guidance). |
| 2026-02-19 | 🔐 **Deploy Secret Reuse on Rerun:** Enhanced `scripts/deploy-azure.ps1` to cache SCIM/JWT/OAuth secrets per `ResourceGroup + AppName` in `scripts/state/deploy-state-<rg>-<app>.json`, auto-reuse them on subsequent reruns when parameters are omitted, and persist cache immediately after prompt/validation so intermediate deployment failures do not lose initial secret inputs. |
| 2026-02-19 | 🧾 **Formatted Version Output in Deploy Summary:** Enhanced `scripts/deploy-azure.ps1` to print the verified `GET /scim/admin/version` response in a readable, formatted block at the end of deployment output (key runtime fields + full pretty JSON payload) alongside existing summary details. |
| 2026-02-19 | ✅ **Post-Deploy Runtime Verification Added:** `scripts/deploy-azure.ps1` now verifies the deployed instance by calling `GET /scim/admin/version` with bearer auth (`SCIM_SHARED_SECRET`) using retry/backoff before declaring success. Deployment now hard-fails if version endpoint never becomes ready. |
| 2026-02-19 | 🗂️ **Per-Run Deploy Logging Added:** `scripts/deploy-azure.ps1` now writes a unique local transcript log for every execution under `scripts/logs/deploy-azure-YYYYMMDD-HHMMSS.log`, capturing full console/runtime output. Failure path now also prints the log file path and closes transcript before exit. |
| 2026-02-19 | 🔁 **Wrapper Deploy Flow Aligned to Local Script:** Updated `deploy.ps1` to prefer local `scripts/deploy-azure.ps1` when running from repo, avoiding stale downloaded script behavior. This ensures current GHCR logic is used: anonymous-by-default for public image, prompt for GH credentials only when anonymous pull is unavailable (private image fallback). |
| 2026-02-19 | 🌐 **Anonymous Public GHCR Deploy Support:** Updated `scripts/deploy-azure.ps1` + `infra/containerapp.bicep` so public `ghcr.io/pranems/scimserver` images deploy without GH username/PAT prompts. GHCR auth is now strictly conditional (both username + password required), defaults to anonymous pull otherwise, and validated via deploy script run showing `GHCR Pull Mode: Anonymous (public image)` with no credential prompts. |
| 2026-02-19 | 🛡️ **Azure Deploy Failure Hardening + Root Cause Isolation:** Confirmed active subscription lacks `Microsoft.Resources/subscriptions/providers/read` (and related RG read) permissions, which blocks provider checks/deployment. Hardened `scripts/deploy-azure.ps1` to fail fast with explicit RBAC diagnostics and non-zero exit (`exit 1`) instead of silent `return` paths, and validated behavior with terminal run (now exits code 1 with actionable message). |
| 2026-02-18 | 🧠 **Repo/API Understanding Docs Update:** Performed code-verified pass over core runtime/auth/routing files (`api/src/main.ts`, OAuth/auth guard, Docker runtime + entrypoint), added a canonical implementation baseline doc (`docs/REPO_API_UNDERSTANDING_BASELINE.md`), updated docs index linkage, and corrected stale Docker guide port guidance from `:80` to current `:8080` runtime behavior. |
| 2026-02-18 | 🏷️ **Final Docs Metadata Normalization:** Completed standardized `Status / Last Updated / Baseline` header blocks for remaining weak-header docs (`docs/SCIM_VALIDATOR_FALSE_POSITIVES_REPORT.md`, `docs/STORAGE_AND_BACKUP.md`, `docs/TEST_ORGANIZATION_RECOMMENDATIONS.md`) and revalidated touched files with no markdown/editor errors. |
| 2026-02-18 | 🧭 **Diagrams + JSON Artifacts Refresh:** Repaired invalid docs JSON exports (`docs/postman/SCIM_v2_Postman_Collection.json`, `docs/openapi/SCIM_v2_openapi_full.json`), validated all `docs/**/*.json` artifacts parse cleanly, and refreshed sequence flow diagrams (`docs/create-user-sequence.mmd`, `docs/list-get-user-sequence.mmd`, `docs/USER_API_CALL_TRACE.md`) to current request-correlation and logging behavior. |
| 2026-02-18 | 📚 **Repository-Wide Docs Reorganization Pass:** Standardized core docs theme (metadata headers, onboarding-first flow, living-vs-historical labeling), corrected deployment/runtime facts (Node 24, Prisma 7 baseline, Docker port `8080`), fixed encoding artifacts in high-traffic docs, refreshed compliance/testing snapshots, and removed AI-conversational leftovers from reference documents. |
| 2026-02-18 | 📘 **README Best-Practice Reorganization:** Reworked README into a modern onboarding-first flow (why, quick start options, prerequisites, configuration, Entra setup, operations, quality status, docs index), corrected Docker run guidance to container port `8080`, and streamlined operational links for maintainability. |
| 2026-02-18 | 🧾 **README Image Removal + Text/JSON Replacement:** Removed all README image embeds (including badges) and replaced visual snapshot sections with formatted metadata table plus structured JSON samples for admin version and recent log outputs, while retaining links to captured raw JSON artifacts. |
| 2026-02-18 | 🧹 **Docs + JSON Current-State Sweep:** Normalized release-facing docs/examples to `v0.10.0`, removed remaining legacy repo naming references, refreshed OpenAPI description/version wording, and aligned long-form analysis docs to current baseline while preserving historical fix context. |
| 2026-02-18 | ✅ **Admin Version Rollout + Full Validation Pipeline:** Updated docs and API collections for expanded `GET /scim/admin/version` payload (`docs/COMPLETE_API_REFERENCE.md`, Postman/Insomnia JSON), aligned web `VersionInfo` typing, and validated end-to-end: clean builds (API + web), lint (0 errors / 74 warnings), unit (666/666), e2e (184/184), live local instance (280/280), fresh `scimserver:latest` Docker build + container live tests (280/280). |
| 2026-02-18 | 🧭 **Admin Version Endpoint Expanded:** `GET /scim/admin/version` now returns full running-instance metadata (service timing, runtime host/process/memory, auth configuration flags, storage details, deployment context) with sensitive values masked. Added e2e coverage in `api/test/e2e/admin-version.e2e-spec.ts` for auth requirement and response contract validation. |
| 2026-02-18 | 🔎 **Log Access UX Improvements:** Added easy log-access output to Azure deploy flow (`scripts/deploy-azure.ps1`) and bootstrap wrapper (`setup.ps1`) with copy/paste commands for recent, stream (SSE), and download endpoints. Added startup console hints in `api/src/main.ts` and updated deployment docs (`DEPLOYMENT.md`, `docs/AZURE_DEPLOYMENT_AND_USAGE_GUIDE.md`) to mirror the same quick commands. |
| 2026-02-18 | 🧪 **Test Coverage Expansion + Full Matrix Validation:** Added missing e2e coverage for `GET /scim/admin/log-config/download` and auth coverage for `GET /scim/admin/log-config/stream`; added live tests for log download formats/filters and SSE stream connectivity. Clean rebuild + full validation completed: 666 unit, 182 e2e, and 280 live tests passing on both local instance (`:6000`) and Docker latest (`:8080`). |
| 2026-02-18 | 🔧 **Remote Debugging & Diagnosis:** SSE live log tailing endpoint (`GET /stream`), log file download (`GET /download`), `scripts/remote-logs.ps1` (4-mode PowerShell script), comprehensive `docs/REMOTE_DEBUGGING_AND_DIAGNOSIS.md` (14 sections, Mermaid diagrams, curl/PS examples). 18 new unit tests (134 logging tests total). Postman v1.4 + Insomnia updated. |
| 2026-02-18 | 🚀 **v0.10.0 - Full Stack Upgrade:** Prisma 6→7 (driver adapter, prisma-client generator, prisma.config.ts), ESLint 8→10 (flat config), Jest 29→30, React 18→19, Vite 5→7. All 6 Dockerfiles updated node:22→24. 666 unit + 184 e2e + 280 live tests passing (local + Docker) |
| 2026-02-15 | 📖 **AZURE DEPLOYMENT GUIDE:** Created comprehensive `docs/AZURE_DEPLOYMENT_AND_USAGE_GUIDE.md` with architecture diagrams, step-by-step deployment, Entra ID setup, usage guide, troubleshooting, and cost estimates |
| 2026-02-15 | 📝 **README.md REWRITTEN:** Complete rewrite with architecture diagram, feature tables, Docker/Azure sections, documentation index, and project structure |
| 2026-02-15 | 📝 **DEPLOYMENT.md REWRITTEN:** Streamlined all deployment methods (Azure/Docker/Local) with comparison table, CI/CD pipeline info, and links to detailed guide |
| 2026-02-15 | 🔧 **VS CODE DEBUG CONFIGS:** Added `.vscode/launch.json` with 3 debug configurations (launch, launch+log, attach) and `start:debug:log` npm script |
| 2026-02-15 | ✅ **CI/CD VERIFIED:** GitHub Actions `build-test.yml` and `publish-ghcr.yml` confirmed pointing to `pranems/scimserver` |
| 2026-02-14 | 🧹 **ESLint Hardened:** `.eslintrc.cjs` updated for @typescript-eslint 8.x - 223→0 errors (8 source fixes + config overrides), 48 remaining warnings (intentional `any` + test scaffolding). `fast-xml-parser` CVE patched. |
| 2026-02-14 | 🚀 **Major Dependency Upgrade:** Node 22-alpine Docker, NestJS 10→11, Prisma 5→6, TypeScript 5.4→5.9, tsconfig targets es2022. 492 unit + 154 e2e + 212 live tests passing (local & Docker) |
| 2026-02-14 | 📝 **Docs Updated to Current State:** SCIM_COMPLIANCE (filtering 85→100%), RECOMMENDED_DESIGN_IMPROVEMENTS (§17.1 gap analysis + §18 roadmap refreshed), INDEX.md, TESTING-WORKFLOW.md |
| 2026-02-14 | 📦 **JSON Consolidation:** 32→19 JSON files (41% reduction) - merged PATCH examples, removed duplicates, fixed OpenAPI /Bulk + bulk.supported, fixed update-endpoint strictMode |
| 2026-02-11 | 📚 **Docs Consolidation:** 34→21 markdown files (~45% line reduction) - merged redundant guides, removed stale files |
| 2026-02-11 | 🔊 **Verbose Live Tests:** `live-test.ps1 -Verbose` cmdlet overrides transparently intercept all API calls; 183/183 tests at the time (later expanded to 212) |
| 2026-02-10 | ✅ **Phase 1 RFC Compliance Complete:** SCIM filter parser (10 operators + and/or/not + grouping), POST /.search, ETag/If-None-Match→304, attributes/excludedAttributes projection, centralized error handling, SCIM content-type on all responses |
| 2026-02-10 | 🧪 **492 unit tests + 183 live integration tests passing** (later expanded to 212) - all 25 Microsoft SCIM Validator tests pass (including 7 preview) |
| 2025-11-21 | 🎯 **PAGINATION FIX:** Backend-driven keepalive filtering (TDD implementation) - accurate counts, no empty pages when hideKeepalive enabled |
| 2025-11-21 | Extended executive/technical wiki created (`wiki.md`) for management evaluation |
| 2025-11-21 | Added beginner quickstart & Azure CLI prerequisites to wiki (`wiki.md`) |
| 2025-11-21 | Relocated prerequisites to top of wiki and renumbered sections for clarity |
| 2025-11-21 | Added legacy self-hosted lab reference + GitHub issues guidance to wiki (`wiki.md`) |
| 2025-11-21 | Added emoji heading refresh + optional Bicep CLI prerequisite & note to wiki (`wiki.md`) |
| 2025-10-28 | v0.8.13 tagged (direct update script envvars fix) |
| 2025-10-28 | v0.8.12 tagged (direct update script env fix) |
| 2025-10-27 | v0.8.11 tagged (direct update script auto-secrets + restart) |
| 2025-10-28 | Diagnosed prod SQLite corruption; initiated blob snapshot restore workflow |
| 2025-10-27 | v0.8.10 tagged (runtime secret enforcement + deployment script updates) |
| 2025-10-20 | OAuth module now requires JWT/OAuth secrets from environment; dev auto-generation logs warnings |
| 2025-10-20 | Azure deploy/setup scripts emit JWT & OAuth secrets and pass to Container Apps template |
| 2025-10-20 | Activity feed aggregates multiple pages when hiding keepalive checks, keeping page numbering intuitive |
| 2025-10-20 | v0.8.9 tagged (activity feed keepalive pagination fix) |
| 2025-10-20 | Activity feed pagination skips keepalive-only pages when hide toggle is on |
| 2025-10-20 | publish-ghcr workflow description updated; YAML lint passing with version 0.8.8 example |
| 2025-10-20 | v0.8.8 tagged (keepalive suppression across logs + activity metrics) |
| 2025-10-20 | Activity feed shares keepalive suppression toggle; summary metrics exclude Entra ping checks |
| 2025-10-20 | Raw log viewer can hide Entra keepalive GET pings (toggle + suppression banner) |
| 2025-10-05 | Git tag v0.8.7 created and pushed to origin (manual provisioning release) |
| 2025-10-05 | Web UI upgrade helper now strips leading 'v' from version parameter; GHCR image 0.8.7 published via workflow_dispatch |
| 2025-10-05 | Blob snapshot bootstrap added to Docker entrypoint (restores /tmp DB before migrations) |
| 2025-10-05 | Initiated SCIM duplicate handling refinement: schema uniqueness enforced & service helpers in progress |
| 2025-10-05 | Private storage endpoint rollout: VNet + DNS automation baked into deploy-azure.ps1 |
| 2025-10-05 | Deploy script now reuses existing ACA virtual network & DNS when already configured |
| 2025-10-05 | Setup auto-registers Microsoft.App & Microsoft.ContainerService providers before deployment |
| 2025-10-05 | Networking template no longer pre-delegates subnets (consumption environment compatibility) |
| 2025-10-05 | Deployment script can now reuse existing VNets/DNS by creating only missing sub-resources |
| 2025-10-05 | Interactive prompt now defaults to existing Container App name to avoid accidental redeploys |
| 2025-10-05 | Bootstrap setup script auto-detects existing app/env names per resource group |
| 2025-10-05 | Resource discovery now uses az resource list to avoid extension noise and ensure reuse |
| 2025-10-05 | Web footer fallback version synced with package.json (0.8.3) |
| 2025-10-05 | Manual provisioning console (UI + admin API) for SCIM collision testing |
| 2025-10-05 | Version bumped to v0.8.6 (blob restore bootstrap + duplicate guardrails prep) |
| 2025-10-05 | Version bumped to v0.8.5 across API + Web + docs |
| 2025-10-05 | Version bumped to v0.8.4 across web assets |
| 2025-10-04 | Backup service telemetry + blob snapshot compile fix |
| 2025-10-04 | Upgrade command now auto-fills RG/App and acknowledges blob backups |
| 2025-10-04 | Added manual GHCR publish workflow (publish-ghcr.yml) |
| 2025-10-03 | v0.8.4 released: structured membership change data (addedMembers/removedMembers) & UI rendering; case-insensitive PATCH ops. |
| 2025-10-02 | Unified image build (root Dockerfile ships API + Web) |
| 2025-10-02 | Token resilience: frontend clears bearer on 401 + modal guidance |
| 2025-10-01 | Runtime token enforcement (no build-time secrets) |
| 2025-09-30 | Hybrid storage architecture: local SQLite + timed Azure Files backups |
| 2025-09-30 | Environment / workload profile + timeout & PS5 compatibility fixes |
| 2025-09-30 | Backup route & persistence verification (v0.7.2) |
| 2025-09-28 | Favicon / activity badge system finalized |
| 2025-09-28 | PATCH Add operation fix (Entra compatibility) |
| 2025-09-27 | v0.3.0: Full SCIM 2.0 compliance baseline |

Current Version: **see [api/package.json](api/package.json)** (Prisma 7 + PostgreSQL 17 + ESLint 10 + Jest 30 + React 19 + Vite 7 + Node 24 Docker)

---

## Status
Production Ready (v0.38.0) - **All 27 migration gaps (G1-G20) fully resolved. SCIM RFC 7643/7644 compliance: 100%.** v0.38.0: G8h Primary Sub-Attribute Enforcement (tri-state PrimaryEnforcement: passthrough default, normalize, reject), RFC 7643 S8.7.1 schema compliance (55 characteristic fixes), test-gap audit #5. v0.37.3: Manager PATCH string coercion + full validation pipeline. v0.37.1: Logging improvements + Azure outage fix + endpointId persistence. Per-endpoint profile settings (13 persisted boolean + logLevel + PrimaryEnforcement) + 2 derived capabilities (Bulk from SPC, CustomResourceTypes from profile.resourceTypes). SPC: `bulk.supported: true, sort.supported: true`. 3-tier auth: per-endpoint bcrypt -> OAuth JWT -> global secret. Parallel E2E (maxWorkers=4). **All unit (3,378 / 84 suites), E2E (1,074 / 51 suites) tests passing. Live tests: ~789 assertions** - see [PROJECT_HEALTH_AND_STATS.md](docs/PROJECT_HEALTH_AND_STATS.md#test-suite-summary) for counts. 25 Microsoft SCIM Validator tests + 7 preview (0 false positives). Stack: Prisma 5 models, PostgreSQL 17, ESLint 10, Jest 30, React 19, Vite 7, Node 24. (April 2026).

## Quick Commands
```powershell
# Publish latest image
pwsh ./scripts/publish-acr.ps1 -Registry scimserverpublic -ResourceGroup scimserver-rg -Latest

# Customer update to latest (example)
iex (irm 'https://raw.githubusercontent.com/pranems/SCIMServer/master/scripts/update-scimserver-direct.ps1'); Update-SCIMServerDirect -Version v0.29.0 -ResourceGroup <rg> -AppName <app> -NoPrompt

> NOTE: Direct upgrade one‑liner integrated into UI copy button; user has not yet tested the copied command end‑to‑end.
```

## Project Summary

**Purpose:** SCIM 2.0 server with Microsoft Entra provisioning integration + real-time logging UI

**Key Components:**
- ✅ NestJS SCIM 2.0 server (all operations working)
- ✅ OAuth 2.0 + Bearer token authentication
- ✅ React log viewer UI
- ✅ Dev tunnel integration for public HTTPS
- ✅ Microsoft Entra provisioning compatible

## Deployment Entry Points

| Entry Point | Usage |
|-------------|-------|
| `bootstrap.ps1` → `setup.ps1` | `iex (iwr .../bootstrap.ps1).Content` - downloads scripts from GitHub, provisions Azure resources |
| `deploy.ps1` | `iex (irm .../deploy.ps1)` - one-click wrapper, prompts for config, auto-provisions PostgreSQL |
| `scripts/deploy-azure.ps1` | Core engine - full parameter control (`-ProvisionPostgres` or `-DatabaseUrl`) |
| Local dev | `cd api && PERSISTENCE_BACKEND=inmemory npm run start:dev` |

**Core Technologies:**
- Node.js 24 & TypeScript 5.9
- NestJS 11 service layer with Prisma 7 ORM (pg driver adapter)
- PostgreSQL 17 (Docker postgres:17-alpine) for persistence
- React 19 + Vite 7 frontend
- ESLint 10 (flat config) + Jest 30
- Docker (node:24-alpine) & Azure Container Apps (deployment target)

AI Assist Notes: Microsoft Docs MCP consulted for SCIM spec alignment when needed.

---

## 🔧 Technical Implementation Notes

### Pagination Fix (2025-11-21)
**Problem:** When `hideKeepalive` toggle was enabled, pagination showed incorrect counts and empty pages. Frontend filtered keepalive requests post-fetch, but backend counted all logs including keepalive, causing mismatch (e.g., "Total 1444 • Page 2 / 29" with empty visible results).

**Root Cause:** Backend `count()` included all logs; frontend filtered keepalive after pagination, resulting in:
- Inaccurate `pagination.total` and `pagination.pages`
- Empty pages when all fetched logs were keepalive requests
- Complex frontend workaround with multi-page aggregation (lines 185-230 in ActivityFeed.tsx)

**Solution (Backend-Driven Filtering):**
Implemented TDD approach with comprehensive test coverage:
1. ✅ **Tests First:** Created 9 test scenarios in `activity.controller.spec.ts`
2. ✅ **Backend Implementation:** Added `hideKeepalive` query param to:
   - `activity.controller.ts` - `/admin/activity` endpoint
   - `admin.controller.ts` - `/admin/logs` endpoint
   - `logging.service.ts` - Core logging service
3. ✅ **Prisma WHERE Clause:** Exclude keepalive using inverse logic:
   ```typescript
   OR: [
     { method: { not: 'GET' } },           // Not a GET request
     { identifier: { not: null } },        // Has an identifier
     { status: { gte: 400 } },             // Error status
     { NOT: { url: { contains: '?filter=' } } }  // No filter parameter
   ]
   ```
4. ✅ **Frontend Simplification:**
   - Removed multi-page aggregation workaround from `ActivityFeed.tsx`
   - Removed `visibleItems` useMemo filtering from `App.tsx`
   - Trust backend pagination metadata completely
   - Simplified code by ~50 lines

**Result:**
- ✅ Accurate pagination counts when `hideKeepalive=true`
- ✅ No empty pages - backend returns only non-keepalive logs
- ✅ Cleaner frontend code - trusts backend pagination
- ✅ All 9 tests passing with TDD green phase
- ✅ Works for both Activity Feed and Raw Logs views

---

## Current Focus
All 27 migration gaps (G1-G20) fully resolved. v0.38.0 shipped with G8h Primary Sub-Attribute Enforcement (tri-state PrimaryEnforcement flag: passthrough default + normalize/reject), RFC 7643 S8.7.1 schema compliance (55 characteristic fixes), and comprehensive test-gap audit #5. **Current version: v0.38.0 with 3,378 unit (84 suites), 1,074 E2E (51 suites), ~789 live assertions - ALL PASSING.** Top remaining work is operational hardening + simplification: delete dead `ScimAuthGuard`, add timing-safe secret comparison, restrict CORS, replace shallow `/health` with dependency-aware readiness/liveness.

### Known Technical Debt (from code audit)
- **SECURITY:** ~~Hardcoded legacy bearer token `S@g@r!2011` in `scim-auth.guard.ts`~~ **CLOSED 2026-04-30** - dead guard deleted; permanent regression guard at `api/src/security/forbidden-source-patterns.spec.ts`
- **LOGGING:** ~~5 `console.log`/`console.error` calls in `scim-auth.guard.ts`~~ **CLOSED 2026-04-30** - file deleted as part of S-1
- **AUTH:** `SharedSecretGuard` + `OAuthService` still use direct string equality for secret comparison - should use timing-safe compare (S-2, open)
- **CORS:** `origin: true` (wildcard) in `main.ts` - should be restricted for production (S-4, open)
- **HEALTH/OPS:** `/health` only returns process uptime/timestamp - no DB/readiness signal for ACA probes or troubleshooting
- **DOC FRESHNESS:** Living docs currently disagree on version/test counts/schema findings; P5 audit findings are partially stale against current `scim-schemas.constants.ts`
- **NAMING:** `resultPayloadPlaceholder` variable names in User/Group services - minor cleanup
- **TESTS:** Zero skipped tests, zero TODO/FIXME markers in source code

## Next Steps / Backlog
- [x] ✅ COMPLETED - Phase 1 Repository Pattern extraction (domain models, interfaces, Prisma + in-memory implementations, RepositoryModule, service + spec refactoring)
- [x] ✅ COMPLETED - Finalize docs metadata normalization for remaining weak-header files
- [x] ✅ COMPLETED - Migrate all repo references from kayasax to pranems
- [x] ✅ COMPLETED - Create comprehensive Azure Deployment & Usage Guide (docs/AZURE_DEPLOYMENT_AND_USAGE_GUIDE.md)
- [x] ✅ COMPLETED - Rewrite README.md for current project state
- [x] ✅ COMPLETED - Rewrite DEPLOYMENT.md with all deployment methods
- [x] ✅ COMPLETED - Add VS Code debug configurations (.vscode/launch.json)
- [x] ✅ COMPLETED - Verify GitHub Actions workflows point to pranems/scimserver
- [ ] Delete dead legacy `ScimAuthGuard` + spec and keep `SharedSecretGuard` as sole auth guard
- [ ] Add `timingSafeEqual()` helper for shared secret and OAuth client secret validation
- [ ] Replace shallow `/health` with DB-aware readiness/liveness endpoints and wire ACA probes to readiness
- [ ] Fix logging/error-handling gaps: redact `DATABASE_URL`, mask unexpected bulk errors, raise failed OAuth validation to WARN
- [ ] Freshness sweep: sync README/PROJECT_HEALTH/CONTEXT/P5 with current source + test counts
- [ ] Simplify duplicated Users/Groups controller flows and reduce Generic service/helper duplication hotspots
- [ ] Validate copied direct upgrade command in production environment
- [ ] Send guidance to existing customers on the v0.8.13 direct-update changes and the need to store generated JWT/OAuth secrets
- [ ] Add rollback command generation (capture previous image tag)
- [ ] Expose deployment metadata via API endpoint (optional runtime flexibility)
- [ ] CI checks: version/tag sync, BOM detection, lockfile sync
- [ ] Parameterize backup interval & retention (env + doc)
- [ ] Add release automation (GitHub Action) for drafts on tag push
- [ ] Provide migration helper to rebuild the Container Apps environment when moving to the private VNet baseline
- [ ] Add SCIM duplicate-handling regression tests (POST + PATCH scenarios)
- [ ] Obtain Azure RBAC on target subscription (`providers/read`, `providers/register/action`, `resourceGroups/*`) or switch to a subscription with deploy rights
## 🏗️ Architecture

**SCIM 2.0 Server:**
- NestJS controllers for `/Users`, `/Groups`, `/ServiceProviderConfig`, `/Schemas`
- Full CRUD operations: POST, GET, PUT, PATCH, DELETE
- Prisma + PostgreSQL for data persistence and request logging
- Bearer token + OAuth 2.0 dual authentication

**Web UI:**
- React frontend with theme support (light/dark)
- Real-time log viewer with search, filtering, and detailed inspection
- Upgrade notifications with GitHub release integration
- Admin tools for log management and system monitoring

**Deployment:**
- Docker multi-stage build with proper permissions
- GitHub Container Registry (`ghcr.io/pranems/scimserver`, public, anonymous pull)
- Azure Container Apps for production hosting
- PowerShell automation for customer updates
- Comprehensive deployment guide: `docs/AZURE_DEPLOYMENT_AND_USAGE_GUIDE.md`

## Architecture Snapshot
| Date | Achievement |
|------|-------------|
| 2025-09-27 | ✅ **v0.3.0 Released** - Full SCIM 2.0 compliance + enhanced UX |
| 2025-09-27 | ✅ **Production Deployed** - Azure Container Apps updated with CORS fixes |
| 2025-09-27 | ✅ **Customer Tools** - PowerShell update function tested and working |
| 2025-09-25 | ✅ README.md created with setup & API documentation |
| 2025-09-25 | 🧪 Initial e2e test (Users create/list/get) added |
| 2025-09-25 | ✅ Logs listing endpoint & e2e test added |
| 2025-09-25 | 🎨 Frontend (React + Vite) scaffolded with log viewer |
| 2025-09-25 | 🚀 Log detail modal (headers/bodies + copy) added |
| 2025-09-25 | 🔍 Full‑text search expanded to include headers/bodies |
| 2025-09-25 | 🏷️ Identifier derivation (user/email/group displayName) implemented (ephemeral) |
| 2025-09-25 | ⚙️ Performance optimization: removed large body columns from list query |
| 2025-09-25 | 🧪 Iterated on persisted identifier column (rolled back pending stable client generation) |
| 2025-09-25 | 📉 Reduced log list latency from ~10s to sub‑second in local tests |
| 2025-09-25 | 📚 Added external exposure (tunnel) deployment guidance drafting |
| 2025-09-26 | 🚀 Azure Container Apps deployment successful - SCIM server running in production |
| 2025-09-26 | 🔧 CORS configuration added to enable web client connection to deployed API |
| 2025-09-26 | ✅ Production web UI monitoring working - full end-to-end deployment complete |
| 2025-09-26 | 🎯 **CONTAINERIZED CLIENT IMPLEMENTED** - Single URL for SCIM + Web UI for teams |
| 2025-09-26 | 📦 Complete containerized solution: SCIM API + monitoring UI in one deployment |
| 2025-09-26 | 🔧 **STATIC ASSETS FIX** - Web UI fully functional with proper CSS/JS serving |
| 2025-09-26 | ✅ **FINAL VERIFICATION** - Web UI accessible without authentication, assets working |
| 2025-09-26 | 🔧 **API URL FIX** - Resolved double /scim prefix issue in web client API calls |
| 2025-09-26 | 🎉 **COMPLETE SUCCESS** - Containerized SCIMServer fully functional and ready for teams |
| 2025-09-26 | 🌿 `feature/acr-automation` branch created and pushed to start Azure Container Registry automation work |
| 2025-09-26 | 🆕 Added /scim/admin/version endpoint (backend version reporting) |
| 2025-09-26 | 🔔 Frontend upgrade banner + remote manifest polling (L1+L2) implemented |
| 2025-09-26 | 🧩 Added dynamic upgrade helper script (GitHub Releases based) |
| 2025-09-26 | 🎨 Microsoft-inspired theming completed (dark/light parity, refined filters, log modal polish) |
| 2025-09-26 | 🔍 Admin log noise hidden from UI; SCIM request list now focused on provisioning traffic |
| 2025-12-29 | 🔁 **GITHUB REGISTRY MIGRATION** - Migrated from ACR to ghcr.io/pranems/scimserver with automated builds |
| 2025-12-26 | 🛠️ **UNIFIED DOCKERFILE** - Multi-stage build (web+API) with fixed SQLite permissions |
| 2025-12-26 | 🚀 **CONTAINER DEPLOYMENT** - Production deployment working via public registry |
| 2025-12-26 | 🔧 **SQLITE PERMISSIONS FIX** - Resolved readonly database errors with proper user ownership |
| 2025-12-26 | 📋 **AUTOMATION SCRIPTS** - publish-acr.ps1, tag-and-release.ps1, update-scimserver.ps1 created |
| 2025-12-26 | 🎯 **UPGRADE BANNER COMPLETE** - Compact banner with modal, hosted PowerShell script integration |
| 2025-12-26 | 📖 **ADMIN DOCUMENTATION** - Complete release workflow and user update process documented |


---

## Priorities (Condensed)
Done: Activity parser, Database browser, Hybrid storage, Update automation.
Near-Term:
- WebSocket/live activity feed (optional)
- Identifier column stabilization
- Minimal health/diagnostics endpoint
Deferred:
- Advanced analytics + FTS
- Mobile polish

---

## Dev Quick Ref
Backend: `cd api && npm run start:dev`
Frontend: `cd web && npm run dev`
Unit Tests: `cd api && npm test` - see [PROJECT_HEALTH_AND_STATS.md](docs/PROJECT_HEALTH_AND_STATS.md#test-suite-summary) for expected counts
Unit Coverage: `cd api && npm run test:cov` → coverage/
E2E Tests: `cd api && npm run test:e2e` - see PROJECT_HEALTH_AND_STATS.md
E2E Coverage: `cd api && npm run test:e2e:cov` → coverage-e2e/
All Coverage: `cd api && npm run test:cov:all` (unit + E2E)
Full Pipeline: `cd api && npm run test:all` (unit + E2E + smoke)
Live Tests: `.\scripts\live-test.ps1` - see PROJECT_HEALTH_AND_STATS.md
Live Tests (verbose): `.\scripts\live-test.ps1 -Verbose`

---

*This file serves as persistent project memory for enhanced AI assistant session continuity with MCP server integration.*
## Key Features (Snapshot)

**SCIM 2.0 Compliance (100% RFC 7643/7644):**
- Complete CRUD operations (POST, GET, PUT, PATCH, DELETE)
- Microsoft Entra ID provisioning compatible (all 25 validator tests pass)
- ServiceProviderConfig, Schemas, ResourceTypes discovery endpoints
- Full SCIM filter parser: 10 operators (`eq`,`ne`,`co`,`sw`,`ew`,`gt`,`lt`,`ge`,`le`,`pr`) + `and`/`or`/`not` + grouping
- POST /.search for Users and Groups
- ETag / If-None-Match → 304 conditional requests
- `attributes` / `excludedAttributes` projection on all GET and .search endpoints
- PATCH: add/replace/remove, valuePath filter, extension URN, no-path merge, boolean coercion
- Centralized SCIM error handling (`scim-exception.filter.ts`)
- `application/scim+json` content-type on all responses including errors
- Proper filtering, pagination, and error handling

**Monitoring & Debugging:**
- Real-time request/response logging
- Searchable log viewer with detailed inspection
- Admin endpoint filtering (hide non-SCIM traffic)
- Performance optimized (<1s load times)

**User Experience:**
- Light/dark theme support
- Upgrade notifications with GitHub integration
- Footer with credits and version info
- Responsive design for mobile/desktop

**DevOps Ready:**
- Docker containerization with proper permissions
- GitHub Container Registry (`ghcr.io/pranems/scimserver`)
- GitHub Actions CI/CD (`build-test.yml`, `publish-ghcr.yml`)
- One-click customer updates via PowerShell
- Automated CI/CD with GitHub releases
- VS Code debug configurations (launch, attach, log-to-file)

**Performance Insights:**
- Expected request volume is low; focus on clarity of logs over throughput.
- PostgreSQL provides robust concurrent access and production-grade persistence.
- Microsoft docs MCP confirmed Entra request patterns to optimize initial test coverage.
- Removing large text columns from primary list query yields major latency reduction.
- Persisting identifiers removes need to parse bodies repeatedly (final integration pending).
- Potential future improvements: FTS5 virtual table for deep search, cursor pagination, optional gzip.

**Known Constraints:**
- Must stay compliant with Microsoft Entra SCIM validator scenarios.
- Deployment must remain low-cost and easily reproducible for Microsoft engineers (Docker + optional ACA).
- Single-user admin workflow; no RBAC planned for MVP.
- Sensitive payload data retained in logs by design; rely on manual purge for case isolation.
- Rely on Microsoft docs MCP for authoritative SCIM updates; monitor for spec changes.
- Identifier persistence currently best-effort; older rows may lack derived names until backfilled.

---

## TODO (Lean)
[-] Investigate lighter framework (Fastify) for image size (deferred)
[ ] Health/diagnostics endpoint
[ ] Optional WebSocket live updates
[ ] Identifier persistence finalization/backfill
[ ] Consider distroless base image

---
*Condensed: older verbose narrative & future-dated/hallucinated entries removed for clarity.*