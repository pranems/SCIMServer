# Authentication Build - Execution Ledger

> **Cross-session source of truth** for the auth build that executes [AUTHENTICATION_ARCHITECTURE.md section 13](AUTHENTICATION_ARCHITECTURE.md#13-step-by-step-execution-plan--estimates--dependencies). Companion detail: [WIF section 13](WIF_JWT_BEARER_ASSERTION_FOR_SCIM.md#13-step-by-step-implementation-plan), [ISV section 5](ISV_AUTH_PATTERNS_AND_SCIMSERVER_GAP_PLAN.md#5-phased-implementation-plan-phase-q). Numbering map: [README.md](README.md#numbering-reconciliation).
>
> **Introspection companions:** this ledger records *what shipped, per step*. The [EXECUTION_ISSUES_AND_RCA.md](EXECUTION_ISSUES_AND_RCA.md) records *what went wrong on the way and what was learned* (issue -> RCA -> fix). The [EXECUTION_DECISIONS_AND_RATIONALE.md](EXECUTION_DECISIONS_AND_RATIONALE.md) records *what was decided and why* (the autonomous judgment calls: question -> options -> choice -> rationale).

**Branch:** `feat/wif`
**Status legend:** NOT-STARTED | IN-PROGRESS | DONE | BLOCKED | DEFERRED

## Step status

| # | Step | Status | Commit SHA | Notes |
|---|------|--------|-----------|-------|
| 1 | **Pre-Q.A** structured config flag-type + validator (10-cell matrix) | DONE | ee2ba5c | structured flag-type + validateStructuredFlag + getConfigStructured + injectable definitions; 22 unit tests (RED->GREEN); doc updated. Build/lint/unit/E2E(inmemory) green. UI/live N/A (no flag registered yet) |
| 2 | **Pre-Q.B** RS256/ES256 externalized signing key + published JWKS + guard verify | DONE | _pending_ | OAuthSigningKeyService + buildJwtModuleOptions (alg-pin) + JwksController (GET /scim/oauth/jwks) + OAuthSigningModule. 9 unit + 4 E2E + 7 live (9z-AM). HS256 alg-confusion rejected. Local node live 1041/0. Docker/dev-Azure live -> next checkpoint |
| 3 | **A0** profile.authentication.methods[] + schemaVersion, INERT | DONE | 4ef4b76 | AuthenticationMethod + ProfileAuthentication types; expandAuthentication threaded through expandProfile (schemaVersion default + secret-strip + field-pick). 12 unit + 5 E2E + 8 live (9z-AN). No-secret invariant enforced. Backend-agnostic (profile JSONB opaque). Local live 1049/0. Docker/dev-Azure -> next checkpoint |
| 4 | **Q0** enrich WWW-Authenticate + aud claim + RFC 8414 metadata + 3-tier chain doc | DONE | b549d8d | WWW-Authenticate error/error_description (RFC 6750 s3) on invalid-token 401; aud claim on issued tokens; GET /.well-known/oauth-authorization-server (RFC 8414, root path); OAUTH_ISSUER constant. 2 unit + 5 E2E + 9 live (9z-AO). Local live 1058/0. Docker/dev-Azure -> THIS is the foundational-cluster checkpoint |
| 5 | **Q1** per-endpoint oauth-client credential + per-endpoint issuer + endpoint_id/aud claims | DONE | 3527df5 | oauth_client credential (clientId+clientSecret, secret-once) + POST /endpoints/:id/oauth/token issuer + generateEndpointAccessToken (endpoint_id + per-endpoint aud) + guard scoping (mine-but-invalid-stop, no legacy fall-through). 11 unit + 7 E2E + 9 live (9z-AP). Local live 1067/0. Docker/dev-Azure -> next critical-path checkpoint |
| 6 | **Q2** jose external JWKS validator: alg-pin, cache by kid, fail-closed, SSRF allowlist | DONE | 37f992c | ExternalJwksValidatorService (jose@^5, dynamic import for ESM); alg-pin (rejects none+HMAC), SSRF JWKS_HOST_ALLOWLIST (https-only, pre-fetch), cache-by-URI, fail-closed. 8 unit. dist runtime smoke OK. No HTTP surface yet (wired in Q6). jose clean; only audit High is dev-only form-data |
| 7 | **A1** admin /authentication/methods CRUD; secret-once; orthogonal create gate | DONE | 4956cb8 | Admin auth-methods CRUD (GET/POST/DELETE) + WifCredentialsEnabled flag (17th) + orthogonal gate (wif rides WifCredentialsEnabled; bearer still needs PerEndpointCredentialsEnabled) + wif credential (public trust only, no secret). 7 unit + 7 E2E + 9 live (9z-AQ). Local live 1076/0. UI cells of the 10-cell flag matrix deferred to Q6. Docker/dev-Azure -> next checkpoint |
| 8 | **A2** computed authenticationSchemes + RFC 8414 metadata + JWKS publication | DONE | c75cb42 | computeAuthenticationSchemes wired into getSpcFromProfile: baseline always + enabled methods add schemes + primary on defaultMethodId. (RFC 8414 + JWKS shipped Pre-Q.B/Q0.) 8 unit + 4 E2E + 5 live (9z-AR). Backend-agnostic. Local live 1081/0. Docker/dev-Azure -> next checkpoint |
| 9 | **A3** token-endpoint form-urlencoded intake + self-describing routing cascade + 3-outcome acceptor | DONE | 524e75e | express.urlencoded intake + */oauth/token content-type exemption + routing cascade (client_assertion -> WIF path not secret; both -> invalid_request; wrong assertion_type -> invalid_request) + 3-outcome acceptor via IAssertionTokenProvider seam (Q6 binds it). 7 unit + 4 E2E + 4 live (9z-AS). Local live 1085/0. Docker/dev-Azure -> next checkpoint |
| 10 | **Q6** wif-7523 provider (+ wif-8693 seam) + reciprocal CredentialsTab WIF UI | DONE | 8fe8b9b | WifAssertionValidatorService (iss/sub/aud/tid/roles on Q2 JWKS core) + WifAssertionTokenProvider (binds ASSERTION_TOKEN_PROVIDER; 3-outcome) + generateEndpointAccessToken({ttlSec,trustedScope}) + Q6.6 WIF discovery scheme + CredentialsTab WIF section (R9 primitives) + WifCredentialsEnabled Switch. 24 unit + 8 E2E + 6 live (9z-AT) + 6 vitest + 3 Playwright. API unit 3928->3952. Docker/dev-Azure -> next checkpoint |
| 11 | **A4** identityModel / roleScopeMap / grantedScopes seams (enforcement OFF) + shadow telemetry | DONE | 481bd38 | computeShadowDecision (pure, enforced=false always) + 4 seams on wif trust metadata (identityModel/roleScopeMap/grantedScopes/roleEnforcement, A4 ships off) + provider emits shadow log AFTER mint (never affects issuance) + WIF_TRUST_KEYS allowlist + CWE-1321 guard on roleScopeMap. INERT like A0. 10 unit (9 shadow + 1 provider) + 5 live (9z-AU). API unit 3952->3962. Docker/dev-Azure -> final checkpoint |
| - | **Q3** probe-direction OAuth client | DEFERRED | - | SKIP unless operator explicitly asks |
| - | **Q4** auth-code + refresh + PKCE | DEFERRED | - | SKIP unless operator explicitly asks |
| - | **Q5** mTLS + DPoP | DEFERRED | - | SKIP unless operator explicitly asks |

## Critical path
`Pre-Q.B -> {Q1 || Q2} -> A3 -> Q6 -> A4`, with `Pre-Q.A -> A0 -> {A1 || A2}` feeding in. Q3/Q4/Q5 are independent and do NOT gate WIF.

## Validation cadence (decision, 2026-06-18)
Per-step: Stage 1 static + Stage 2 tests (unit + E2E inmemory) + local-node live-test (form factor 1). **Docker live-test + dev-Azure deploy/live-test are batched to integration checkpoints** (after foundational clusters and after each WIF-critical milestone), NOT per micro-step. Rationale: deploying all 11 steps to the shared dev Azure is disproportionate; steps are backend-agnostic or unit/E2E-covered; Docker/Azure run identical code. Each row notes its validation tier.

## Run log
| Date | Event |
|------|-------|
| 2026-06-18 | Ledger created. Branch feat/wif at 640418c (docs-only baseline). No auth code implemented yet. Starting Pre-Q.A. |
| 2026-06-18 | Pre-Q.A DONE. Added `structured` flag-type machinery (validateStructuredFlag + getConfigStructured + injectable definitions param) to endpoint-config.interface.ts. 22 new unit tests. API unit 3825->3847, build 0 err, lint baseline. Version 0.53.2 -> 0.54.0-alpha.1. Committed ee2ba5c (rebased onto remote hono CVE bump 58ca63b), pushed to feat/wif. |
| 2026-06-18 | Pre-Q.B DONE. Asymmetric RS256/ES256 signing + published JWKS (GET /scim/oauth/jwks) + algorithm-confusion pinning. New OAuthSigningKeyService/Module, JwksController, buildJwtModuleOptions factory. 9 unit + 4 E2E + 7 live (9z-AM). API unit 3847->3856; E2E inmemory 1223; local live 1041/0. Version -> 0.54.0-alpha.2. Committed 7baa330, pushed to feat/wif. |
| 2026-06-18 | A0 DONE. profile.authentication model (AuthenticationMethod + ProfileAuthentication) threaded through expandProfile with no-secret strip. 12 unit + 5 E2E + 8 live (9z-AN). API unit 3856->3866; endpoint/profile E2E 87; local live 1049/0. Version -> 0.54.0-alpha.3. Committed 4ef4b76, pushed. |
| 2026-06-18 | Q0 DONE. WWW-Authenticate enrichment (RFC 6750 s3) + aud claim + RFC 8414 metadata (/.well-known/oauth-authorization-server) + 3-tier chain doc. 2 unit + 5 E2E + 9 live (9z-AO). API unit 3866->3868; full E2E inmemory 1233; local live 1058/0. Version -> 0.54.0-alpha.4. Committed b549d8d, pushed. |
| 2026-06-18 | **INTEGRATION CHECKPOINT (foundational cluster Pre-Q.A -> Q0) PASSED on all 3 form factors.** Docker compose (Prisma backend) live-test 1058/0; dev Azure (deployed SHA b549d8d to scimserver-dev via ACR acrscimserver20622, revision vb549d8d) live-test 1058/0. Cross-backend parity (inmemory == Prisma) confirmed: all 24 new auth assertions (9z-AM/AN/AO) green on inmemory + Prisma + Azure. Q3/Q4/Q5 remain DEFERRED. Next: Q1. |
| 2026-06-18 | Q1 DONE. Per-endpoint oauth_client credential + per-endpoint token issuer (POST /endpoints/:id/oauth/token) + endpoint_id/aud claims + guard scoping (mine-but-invalid-stop). 11 unit + 7 E2E + 9 live (9z-AP). API unit 3868->3878; full E2E inmemory 1240; local live 1067/0. Version -> 0.54.0-alpha.5. |
| 2026-06-18 | Q2 DONE. ExternalJwksValidatorService (jose@^5 dynamic import) - alg-pin RS256/ES256, SSRF host allowlist (https-only pre-fetch), cache-by-URI, fail-closed. 8 unit. dist runtime smoke OK. No HTTP surface (wired in Q6). API unit 3878->3886. Version -> 0.54.0-alpha.6. |
| 2026-06-18 | A1 DONE. Admin auth-methods CRUD + WifCredentialsEnabled flag (17th) + orthogonal create gate + wif credential (public trust only). 7 unit + 7 E2E + 9 live (9z-AQ). API unit 3886->3907; local live 1076/0. UI flag cells deferred to Q6. Version -> 0.54.0-alpha.7. |
| 2026-06-18 | A2 DONE. computeAuthenticationSchemes wired into getSpcFromProfile (baseline always + enabled methods add schemes + primary on defaultMethodId). 8 unit + 4 E2E + 5 live (9z-AR). API unit 3907->3915; discovery specs 98 green; local live 1081/0. Version -> 0.54.0-alpha.8. |
| 2026-06-19 | A3 DONE. Token-endpoint form-urlencoded intake (express.urlencoded + */oauth/token content-type exemption) + self-describing routing cascade + 3-outcome acceptor (IAssertionTokenProvider seam; Q6 binds). 7 unit + 4 E2E + 4 live (9z-AS). API unit 3915->3922; local live 1085/0. Version -> 0.54.0-alpha.9. Picked up multer 2.2.0 from master merge (npm ci). |
| 2026-06-19 | SECURITY INTERSTITIAL (between A3 and Q6). CWE-1321 prototype-pollution hardening from CodeQL triage: new isUnsafeObjectKey() guard (safe-object-key.ts) wired into auto-expand stripUndefined + stripSecretsFromConfig; in-sink DANGEROUS_KEYS guard added to generic-patch-engine URN write + setNested. CodeQL 68/184/235 fixed (auto-close on rescan); 234/236 dismissed false-positive (allowlist/defense-in-depth filters, not authZ gates). 4 new + 2 new unit tests. API unit 3922->3928 (108 suites). Build 0 err; lint 0 err/464 warn. multer 2.2.0 carried. Version -> 0.54.0-alpha.10. Committed ab943ab, pushed. |
| 2026-06-19 | Q6 DONE. WIF validate+issue+UI. WifAssertionValidatorService (Q6.3, iss/sub/aud/tid/roles on Q2 JWKS core, throws WifAssertionInvalidError) + generateEndpointAccessToken options{ttlSec clamp 1-6h, trustedScope verbatim} (Q6.4) + WifAssertionTokenProvider binds ASSERTION_TOKEN_PROVIDER (3-outcome: null=not-mine, throw=mine-but-invalid, accept=mint endpoint_id token) + Q6.6 WIF discovery scheme gated by WifCredentialsEnabled + CredentialsTab Federated Identity (WIF) section (EditableField/CopyableField/CopyJsonButton + client-side Test Connection) + WifCredentialsEnabled SettingsTab Switch. 24 unit + 8 E2E + 6 live (9z-AT) + 6 vitest + 3 Playwright. API unit 3928->3952 (110 suites); local inmemory E2E + web vitest green; web build+size pass. Build 0 err; lint 0 err/464 warn. Version -> 0.54.0-alpha.11. Committed 8fe8b9b, pushed. Docker/dev-Azure checkpoint (batched Q1-Q6) next. |
| 2026-06-19 | A4 DONE (final build step). computeShadowDecision (wif-shadow-telemetry.ts, pure, enforced=false always; would-have-rejected role->scope gate) + 4 inert seams on wif trust metadata (identityModel per-app/first-party, roleScopeMap, grantedScopes, roleEnforcement off/shadow/enforce - ships off) added to WIF_TRUST_KEYS allowlist + provider emits shadow log AFTER mint (LogCategory.AUTH, never affects issuance) + roleScopeMap write guarded vs CWE-1321 (isUnsafeObjectKey). INERT like A0. 9 shadow unit + 1 provider unit (headline: token STILL minted when shadow wouldReject) + 5 live (9z-AU). Q6 WIF E2E 8 stays green. API unit 3952->3962 (111 suites). Build 0 err; lint 0 err/464 warn. Version -> 0.54.0-alpha.12. Committed 481bd38, pushed. ALL 11 BUILD STEPS DONE - final Docker+dev-Azure checkpoint (Q1-A4) next. |
| 2026-06-19 | **FINAL INTEGRATION CHECKPOINT (Q1->A4 batch) PASSED on all 3 form factors.** Local node inmemory: full E2E 1263/71 suites + per-step live green. Docker compose (Prisma backend, image ffc4133 / A4 code): live-test 1096/0. Dev Azure (deployed ffc4133 to scimserver-dev, revision vffc4133, RS256 token + kid confirmed): live-test 1096/0. All 9 auth sections green on every backend: 9z-AM(7) Pre-Q.B + 9z-AN(8) A0 + 9z-AO(9) Q0 + 9z-AP(9) Q1 + 9z-AQ(9) A1 + 9z-AR(5) A2 + 9z-AS(4) A3 + 9z-AT(6) Q6 + 9z-AU(5) A4 = 62 auth assertions. Cross-backend parity (inmemory == Prisma == Azure) confirmed for the entire WIF stack. Fixed one live-test false-positive (loose token regex matched issuedTokenTtlSec) in ffc4133. ALL 11 BUILD STEPS COMPLETE + VALIDATED. Q3/Q4/Q5 remain DEFERRED. Plan section 13 execution DONE. |
