{
  "name": "scimserver-repo-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level tooling only. The shippable projects live in api/ and web/ and have their own package.json + lockfile; this file exists solely so documentation/quality scripts under scripts/ have declared, lockable entry points instead of ad-hoc global installs.",
  "type": "module",
  "scripts": {
    "rfcs:check": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/sync-rfcs.ps1",
    "rfcs:check:online": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/sync-rfcs.ps1 -Online",
    "rfcs:update": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/sync-rfcs.ps1 -Update"
  }
}
